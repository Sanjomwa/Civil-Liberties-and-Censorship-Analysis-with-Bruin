# CLIO ERD and Dataset Lineage

Status: rewritten 2026-07-12, replacing the archived pre-restructure version at `Archive/erd-lineage.md` (TD-23). This lineage diagram was generated by extracting every asset's `name:` and `depends:` fields directly from `Bruin/assets/**/*.sql` and `*.py` (54 assets, matching a clean `bruin validate` run at time of writing), not hand-drawn or recalled from a prior document. Re-verified 2026-08-02 (`bruin validate`, 55/55 assets clean — the one-asset growth since 2026-07-12 is `marts.dim_tls_failure_evidence`, TD-71, an OONI-side dimension internal to `int.ooni_experiment_results` that does not change this document's per-source diagrams); the ACLED diagram below was independently confirmed accurate against a live ingestion-fidelity audit the same day (see the note under the ACLED diagram). **Re-verified again 2026-09-03: `bruin validate` now reports 68 assets clean, not 55 — real growth, not just an asset renumbering, confirmed via direct `depends:` extraction, not assumed.** The ACLED, Google Transparency, and Lumen diagrams below were re-checked against every relevant asset's current `depends:` field and found unchanged (`int.acled_event_classification`, `features.acled_pressure_signals`, `intelligence.acled_pressure_regimes`/`_precondition_check`, `int.google_pressure_periodized`, and `stg.lumen_requests`'s consumer chain all still match what's drawn). The OONI diagram did not hold up the same way — see the new second-path section added below it, which was missing entirely before this pass. See `data-modelling.md` for the entity/schema-level view; this document is the pipeline dependency graph.

## Per-source lineage

Four sources, traced raw → load → staging → intermediate → features → intelligence → marts → reporting. Where a source's chain stops early (no features/intelligence layer), that reflects the real DAG — not an omission.

### OONI

```mermaid
flowchart LR
    raw_ooni["raw.ooni_conflict_measurements"] --> load_ooni["load.ooni_to_gcs"]
    load_ooni --> stg_ooni["stg.ooni_measurements"]
    stg_ooni --> stg_dns["stg.ooni_dns_observations"]
    stg_ooni --> stg_tcp["stg.ooni_tcp_observations"]
    stg_ooni --> stg_tls["stg.ooni_tls_observations"]
    stg_ooni --> stg_http["stg.ooni_http_observations"]
    stg_dns & stg_tcp & stg_tls & stg_http --> int_ooni["int.ooni_experiment_results"]
    int_ooni --> fact_ooni["marts.fact_ooni_censorship_signals"]
    int_ooni -.-> dim_country["marts.dim_country (repointed here 2026-07-06)"]
    fact_ooni --> feat_protocol["features.protocol_daily_signals"]
    feat_protocol --> int_regimes["intelligence.protocol_signal_regimes"]
    feat_protocol --> int_lag["intelligence.protocol_lag_relationships"]
    int_regimes & int_lag --> int_rel["intelligence.protocol_relationships"]
    feat_protocol --> mart_trends["reporting.mart_protocol_interference_trends"]
    feat_protocol & int_rel --> mart_asn["reporting.asn_behavior_profile_mart"]
    fact_ooni --> mart_ooni_attr["reporting.mart_pressure_attribution_ooni_daily"]
    mart_trends --> mart_corr["reporting.protocol_repression_correlation_mart"]
```

**Added 2026-09-03: a second, independent OONI classification path exists and was entirely missing from this document before this pass** (the TD-87/TD-91 verdict/candidate-guard branch, using OONI's own submitted verdict vocabulary rather than CLIO's `result_state` derivation above). It converges with the diagram above only at `features.ooni_weekly_signals`, and the two series stay non-merged even there (ADR-0011):

```mermaid
flowchart LR
    stg_ooni2["stg.ooni_measurements"] --> stg_summary["stg.ooni_measurement_summary\n(TD-87 Phase 1, OONI's own submitted\nverdict fields, wide passthrough)"]
    stg_summary --> verdicts_candidate["int.ooni_measurement_verdicts_candidate\n(real computation; also depends on\nmarts.dim_ooni_probe_version_accuracy)"]
    dim_probe["marts.dim_ooni_probe_version_accuracy"] --> verdicts_candidate
    verdicts_candidate --> guard["int.ooni_measurement_verdicts_confirmed_guard\n(TD-87 Phase 2 layer 2: ERRORs if CONFIRMED\nappears outside web_connectivity)"]
    guard --> verdicts_published["int.ooni_measurement_verdicts\n(TD-91: trivial passthrough of the candidate,\npublished only after the guard passes)"]
    verdicts_published --> feat_weekly["features.ooni_weekly_signals\n(anomalous_* series, Saturday-anchored)"]
    int_ooni2["int.ooni_experiment_results\n(same node as the diagram above)"] --> feat_weekly
    feat_weekly --> lag_corr["intelligence.ooni_acled_lag_correlation\n(vs. acled_pressure_regimes,\nseries_type partitioned separately;\nblocked_* and anomalous_* series never pooled, ADR-0011)"]
```

### ACLED

```mermaid
flowchart LR
    raw_acled["raw.acled_conflict_events"] --> load_acled["load.acled_conflict_events_to_gcs"]
    load_acled --> stg_acled["stg.acled_conflict_events"]
    stg_acled --> int_class["int.acled_event_classification"]
    int_class --> feat_acled["features.acled_pressure_signals"]
    feat_acled --> precheck["intelligence.acled_pressure_regimes_precondition_check\n(EXECUTION CONTRACT guard, TD-04)"]
    precheck --> intel_regimes["intelligence.acled_pressure_regimes\n(Path A: STABLE/ESCALATION/CONFLICT/CRISIS/MOBILISATION)"]
    intel_regimes --> country_status["intelligence.country_initialization_status"]
    intel_regimes --> country_readiness["intelligence.country_readiness"]
    intel_regimes --> fact_country["marts.fact_country_pressure_daily\n(regime_* columns, Saturday-anchored broadcast)"]
    stg_acled -.-> fact_country
    int_class --> mart_conflict_drivers["reporting.mart_pressure_attribution_conflict_drivers\n(event grain, not weekly)"]

    subgraph retired["Retired 2026-07-05, path B satellite (TD-41)"]
        direction LR
        stg_acled -.->|"filtered on a permanently-NULL column\nafter commit 6dbe7ab"| fact_conflict_events["marts.fact_conflict_events\n(DELETED - zero consumers found)"]
    end
```

**Ingestion-fidelity audit, 2026-08-02:** the `raw.acled_conflict_events` → `load.acled_conflict_events_to_gcs` → `stg.acled_conflict_events` chain shown above was checked field-by-field against ACLED's own export data, not just diagrammed from `depends:` fields — row counts, fatality sums, event/sub-event-type coding, date/week-anchor alignment, and county (`admin1`) coverage all matched exactly (0 mismatches, including a full row-level equality diff beyond the requested aggregate checks) for the Finance Bill 2024 window (2024-05-11 to 2024-07-13) and two quiet control windows (the Aug 2010 referendum and Sept 2017 court-annulment weeks), and held at the full 267,956-row/58-country dataset level too. One disclosed gap: the original ACLED CSV that `raw.acled_conflict_events` reads wasn't available in that session's environment, so the CSV→parquet rename step itself was verified by code inspection (a pure 1:1 column rename, no computation) rather than an executed diff — the staging transformation downstream of it (`COALESCE`, date parsing, quality flags) was verified with full row-level fidelity. See `decision-log.md`'s 2026-08-02 entry for the audit's full methodology and per-window results, and its TD-71/TD-72 entries (`technical-debt-inventory.md`) for the adjacent OONI-side external-validation work done the same week.

### Google Transparency Report

```mermaid
flowchart LR
    raw_gtr["raw.google_transparency_requests"] --> load_gtr["load.google_transparency_requests_to_gcs"]
    load_gtr --> stg_gtr["stg.google_transparency_requests"]
    raw_gtd["raw.google_transparency_detailed"] --> load_gtd["load.google_transparency_detailed_to_gcs"]
    load_gtd --> stg_gtd["stg.google_transparency_detailed"]
    stg_gtr & stg_gtd --> int_periodized["int.google_pressure_periodized\n(semiannual grain, ADR-0003 Contextual/Backdrop tier)"]
    int_periodized --> fact_country2["marts.fact_country_pressure_daily\n(platform_pressure_score)"]
    stg_gtr & stg_gtd --> mart_platform_drivers["reporting.mart_pressure_attribution_platform_drivers"]
    fact_country2 --> mart_attr_daily["reporting.mart_pressure_attribution_daily"]
```

### Lumen — synthetic, benched

```mermaid
flowchart LR
    raw_lumen["raw.lumen_requests\n(scripts/lumen_parquet.py fabricates rows)"] -.->|"declared depends:\nbut code reads a\ndisconnected local file (TD-42)"| load_lumen["load.lumen_requests_to_gcs"]
    load_lumen --> stg_lumen["stg.lumen_requests\n(is_synthetic = TRUE on every row, TD-01)"]

    subgraph deadend["Dead-end branch A - zero downstream consumers"]
        direction LR
        stg_lumen --> int_lumen["int.lumen_pressure_daily"]
    end

    subgraph liveBranch["Branch B - materializes, but nothing live reads it"]
        direction LR
        stg_lumen --> fact_takedown["marts.fact_takedown_activity"]
        fact_takedown --> fact_takedown_daily["marts.fact_takedown_pressure_daily"]
    end

    stg_lumen -.->|"is_synthetic propagated via LOGICAL_OR\nthrough every grain change, TD-01"| fact_country3["marts.fact_country_pressure_daily\n(legal_pressure_score column exists,\nbut carries ZERO weight since ADR-0004)"]
```

## Where the sources converge

```mermaid
flowchart LR
    ACLED["ACLED chain\n(regime_*, conflict_pressure_score)"] --> fact_country["marts.fact_country_pressure_daily"]
    Google["Google Transparency chain\n(platform_pressure_score)"] --> fact_country
    Lumen["Lumen chain\n(legal_pressure_score, zero weight)"] -.-> fact_country

    fact_country --> stress_windows["reporting.mart_political_stress_windows\n(page 1 - direct passthrough of fact table's composite, TD-45/TD-66 RESOLVED 2026-07-18)"]
    fact_country --> corr_mart["reporting.protocol_repression_correlation_mart\n(pages 4, 6, 7 - raw composite passthrough)"]
    fact_country --> attr_daily["reporting.mart_pressure_attribution_daily\n(page 9 - decomposed into named drivers, ADR-0006)"]

    OONI["OONI chain"] --> corr_mart
    OONI -.->|"independent same-day corroboration,\nNOT a composite input"| attr_daily
```

## Live guardrails this lineage depends on

Two guardrails make this DAG legible at a glance rather than silently corruptible — both are enforced in the DAG itself, not just documented:

1. **The ACLED regime engine's EXECUTION CONTRACT precondition (TD-04).** `intelligence.acled_pressure_regimes_precondition_check` sits upstream of `intelligence.acled_pressure_regimes` in the `depends:` graph (see the ACLED diagram above) and calls `ERROR()` if more than one distinct week is present for the target country/week filter. This blocks Bruin from invoking the regime asset at all on violation, rather than catching corruption after a `MERGE` already committed it — the regime engine's correctness depends on exactly-one-country-one-new-week-per-execution, and this is what actually enforces that, not just a comment.
2. **The materialization-staleness guardrail (ADR-0005).** A daily CI check (`.github/workflows/staleness-check.yml`, script at `Bruin/scripts/staleness_check/`) compares every asset's materialization timestamp against its upstream dependencies' timestamps and fails on any *new, undocumented* case where a downstream table is older than what feeds it — the general failure category behind TD-38, TD-40, and TD-54, mechanized rather than caught by hand each time. A TD-referenced allowlist (`staleness_allowlist.json`) exists for deliberate, temporary holds; resolving a TD referenced there must prune its allowlist entry in the same commit.

## Retired branches, kept visible rather than silently omitted

- **`marts.fact_conflict_events`** (ACLED path B satellite) — asset file deleted 2026-07-05, orphaned table dropped 2026-07-06 (TD-41). Had been silently producing zero rows for about a month after a commit broke its filter column; decided not worth fixing since it predates the point where ACLED's pipeline (Path A) was brought up to the same rigor already applied to OONI.
- **`marts.dim_platforms`, `dim_reasons`, `dim_regions`, `dim_requestors`** — deleted 2026-07-06 (TD-56), zero external consumers found via direct grep across `Bruin/` and `streamlit/`.
- **`marts.fact_asn_repression_index`, `fact_network_blocking_daily`** — deleted 2026-07-06 (TD-56), a dead two-asset chain with zero consumers (a prior record incorrectly believed `fact_asn_repression_index` fed dashboard pages 5/7; direct reading of `asn_behavior_profile_mart.sql`'s actual `FROM`/`depends:` disproved this before the deletion).
- **`int.ooni_signals`** — deleted 2026-07-06 (TD-56); its one consumer, `dim_country`, was repointed to `int.ooni_experiment_results` instead (same diagram above).

Verify this document against a live `bruin validate` / re-extraction of `depends:` fields before relying on it for a pipeline change — the DAG has changed materially several times in this project's history (most recently the 2026-07-06 cost-audit cleanup), and this file is a snapshot, not a live query.

# CLIO Technical Debt Inventory

Status: Phase 0 deliverable, CLIO Engineering Reset. Itemized, with severity and recommended action. No code has been changed. Severity reflects a combination of correctness risk, security exposure, and blast radius if the item causes a wrong number to reach a client — not effort to fix. See `implementation-roadmap.md` for sequencing.

## Critical

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-01 | `stg.lumen_requests` → two parallel branches, corrected 2026-07-05 (prior chain description was wrong): **Branch A (dead end, unconsumed)** `int.lumen_pressure_daily` → `fact_takedown_activity` → `fact_takedown_pressure_daily`, confirmed nothing reads the last table; **Branch B (live, the actual consumed path)** `stg.lumen_requests` directly (bypasses `int.lumen_pressure_daily` entirely) → `fact_country_pressure_daily`'s `legal_pressure_score` (weight 0.25 in the composite) → both reporting marts | Lumen data is entirely synthetic (`scripts/lumen_parquet.py` fabricates 5,000 rows via `np.random.seed(42)`), with no `is_synthetic` flag anywhere downstream. `legal_pressure_score = ROUND(LOG(1 + takedown_requests + takedown_items), 4)`, baked into `composite_pressure_score` before any later stage can dilute it. Confirmed live UI surfaces: `1_National_Stress_Observatory.py` (KPI + trend chart), `6_Suppression_Event_Explorer.py` (KPI + scatter + table), `7_Finance_Bill_2024_Incident_Report.py` (scatter) — the same flagship page already known to be distorted by this during the Finance Bill 2024 window. Four dimension tables (`dim_reasons`, `dim_platforms`, `dim_country`, `dim_requestors`) also union in Lumen category values tagged `'LUMEN'` — lower priority, not fixed in this pass. **Separately discovered, not part of this fix, logged as its own item:** the Bruin asset `lumen_requests_to_gcs` declares `depends: raw.lumen_requests` but its actual code reads a hardcoded local file path, ignoring its declared dependency entirely — the live BigQuery table was seeded at some point disconnected from anything currently wired — see new TD-42. | **RESOLVED, 2026-07-05, verified and pushed (commit `69233b4`).** Hardcoded `TRUE AS is_synthetic` literal originating in `stg.lumen_requests.sql` (same pattern as `stg.acled_conflict_events.sql`'s `'WEEKLY_AGGREGATE'` literal), propagated via `LOGICAL_OR(is_synthetic)` at every `GROUP BY` grain change through both branches (Branch A included for consistency, even though still unconsumed), renamed `legal_pressure_is_synthetic` at the `fact_country_pressure_daily` join (`COALESCE(..., FALSE)`, matching the existing "no Lumen row this date" convention), unchanged passthrough through both reporting marts. `synthetic_data_notice(sources)` added to `components/trust.py`, live on pages 1, 6, and 7, gated on the real per-row flag; a permanent "Data Sources & Known Limitations" section added to page 8. Materialized bottom-up via `bruin run` (same bootstrapping discipline as TD-40) and verified against live tables: `stg.lumen_requests` 100% `TRUE` (3,736 rows); `fact_takedown_activity` `lumen=TRUE`/`google=FALSE` exactly by source; `fact_country_pressure_daily` 752 `TRUE` / 9 `FALSE` (the 9 correctly being dates with no Lumen row that date); both reporting marts passthrough exactly (`protocol_repression_correlation_mart`: 3,008/36 = 4 protocols × 752/9). `bruin validate` 56/56 clean throughout. UI verified via headless-Chromium screenshot on all four affected pages, including the flagship Finance Bill 2024 report specifically. Vocabulary-table tagging and the `raw.lumen_requests` ingest disconnect were deliberately kept out of scope — the latter logged as **TD-42**. **Annotation, 2026-07-05 (ADR-0004 / TD-44 / TD-45):** the "weight 0.25 in the composite" language earlier in this row's Location column, and this cell's own "baked into `composite_pressure_score`" framing, describe `composite_pressure_score`'s formula *before* ADR-0004's reweighting — accurate when written, not rewritten here to avoid erasing the historical record. As of commit `ab998e3` (2026-07-05), `legal_pressure_score` is no longer a term in `composite_pressure_score` at all (Lumen formally benched, see ADR-0004); the composite is `conflict_pressure_score * 0.75 + platform_pressure_score * 0.25`. See TD-44 and TD-45 below for the current state. |
| TD-02 | `streamlit/services/marts.py`, `get_finance_bill_incident()`, lines ~474–497; feeds from `marts.fact_asn_repression_index` → `asn_behavior_profile_mart` — a fourth consumer of `fact_country_pressure_daily` found 2026-07-04 during ADR-0002 step (e) investigation, not previously on record alongside the two reporting marts. **Diagnosis corrected 2026-07-05, before implementation, by direct investigation of both marts' actual grain.** | Confirmed `CROSS JOIN` between `protocol_repression_correlation_mart` (grain: `measurement_date, protocol` — a real daily time series) and `asn_behavior_profile_mart` (grain: one row per ASN, full-history snapshot — every CTE `GROUP BY asn` only, no date dimension anywhere in the mart). The original framing of this bug ("add a proper join key on measurement date") is wrong: there is no join key to add, because `asn_behavior_profile_mart` has no per-date grain at all to join on. Any join predicate connecting a specific date to a specific ASN row would fabricate a date-specific meaning for data that is actually a lifetime summary — the same category of dishonesty this project already rejected at TD-40 (broadcast-not-interpolate). Currently 3 ASNs qualify as `MAJOR_KENYA_PROVIDER` (AS37061, AS36926, AS33771), so the CROSS JOIN triples every protocol/date row today; page 7's "Observed Alignment State Distribution" chart and "High Confidence Suppression Windows" table are directly tripled/duplicated as a result ("Major Provider ASN Spike Analysis" survives only by accident, since it groups by `display_asn` and averages a constant). | **RESOLVED 2026-07-05, verified and pushed (commit `eb6993e`).** Rejected "add a join key" and rejected pre-collapsing the ASN rows into one before crossing (still implies false date-specific meaning for a lifetime summary). Fix: `get_finance_bill_incident()` now returns two independent, non-joined dataframes — `correlation_df` (real `(measurement_date, protocol)` grain, unchanged) and `asn_df` (natural one-row-per-ASN grain, filtered to `MAJOR_KENYA_PROVIDER`, no date filter since none applies). `streamlit/pages/7_Finance_Bill_2024_Incident_Report.py` updated so "Observed Alignment State Distribution" and "High Confidence Suppression Windows" read `correlation_df` only, "Major Provider ASN Spike Analysis" reads `asn_df` directly, with a corrected caption stating the ASN profile is a full-history snapshot, not scoped to the incident window (the old copy falsely implied otherwise). Verified two ways: direct SQL comparison (old `CROSS JOIN` produced 372 rows = 124 real × 3 ASNs; new queries produce the real 124 + 3 separately; `alignment_state` counts undistorted exactly 3x — `NO_CLEAR_ALIGNMENT` 276→92, `PROTOCOL_DIVERGENCE` 78→26, `PRESSURE_ONLY` 18→6) and by rendering the live page in headless Chromium (bar chart shows 92/26/6, ASN chart shows the 3 real ASNs unchanged, zero tracebacks, zero console errors). `bruin validate` unaffected (Streamlit-only change, 56/56 clean). |
| TD-03 | `intelligence.acled_pressure_regimes` vs. `reporting/*` | **Confirmed 2026-07-04** (Architecture Assessment Section 6): no `reporting/*` asset references the ACLED path A regime engine or `intelligence.country_readiness`, in header or SQL body. The dashboard's national pressure score is built entirely from the simpler, unguarded ACLED path B (`marts.fact_country_pressure_daily`, which itself reads straight from `stg.acled_conflict_events`). | Decide, per Step 3 of the Implementation Roadmap: wire path A into a reporting mart, replace path B with it, or formally deprecate path A. No longer an open investigation. |
| TD-04 | `intelligence.acled_pressure_regimes.sql`, lines 36-72, documented as **"EXECUTION CONTRACT (AUTHORITATIVE)"** — corrected 2026-07-04; previously misattributed to "ARCHITECTURE FREEZE"/BLOCKER-002, which are two different, unrelated comment blocks in the same file ("ARCHITECTURE FREEZE" covers not changing regime/hierarchy logic; BLOCKER-002 is an already-resolved STABLE-classification edge case, not this precondition) | The regime engine's correctness depends on an unenforced precondition — exactly one country, one new week, per execution — enforced only by a comment and operator discipline, not by the system. Root cause, per the file's own rationale: the self-referential lag join in CTE-03 reads only from the already-materialized output table as it existed before execution began, so a second new week in the same execution finds no predecessor row and silently falls back to first-observation defaults. Produces no error and no NULL output, invisible to standard data-quality checks. **RESOLVED, 2026-07-04.** A new upstream Bruin asset, `intelligence.acled_pressure_regimes_precondition_check`, calls `ERROR()` when more than one distinct week is present for the target country/week filter, and is added to the regime asset's `depends:` list — this blocks Bruin's DAG from ever invoking the regime asset on violation, rather than a post-hoc `custom_check` (confirmed those run after execution, too late to prevent the MERGE from committing corrupted state). Applied and validated: `bruin validate` (57/57 assets clean) and `bruin lineage` (new check confirmed upstream of the regime asset) both pass. Committed `2080b64`, additive only — no change to existing MERGE logic, backfill data untouched. See TD-38 for a related, more urgent finding surfaced during this design work. | Closed. Keep the duplicated country/week filter logic in the new check asset in sync with CTE-02's filter if that ever changes. |
| TD-05 | `int.ooni_experiment_results.sql`, `features.protocol_daily_signals.sql`, `marts.fact_platform_blocking_summary` (declared name `marts.fact_protocol_blocking_summary`), `int.ooni_signals.sql`, `marts.dim_censorship_confidence` | **RESOLVED, 2026-07-05 (see ADR-0001, fully rewritten).** Original framing ("two schemes: governed dimension table vs. hardcoded inline literals, migrate the literals into the table") was wrong, discovered the same way TD-02's was wrong — by reading the SQL bodies and consumer chains directly. `int.ooni_experiment_results.sql`'s 6 raw per-branch `confidence_score` literals (0.90/0.80/0.75/0.70/0.45/0.40) are continuous statistical inputs feeding weighted averages and z-scores throughout the intelligence layer, not categorical labels — collapsing them into `dim_censorship_confidence`'s 4-row scale would have silently changed real downstream numbers and was never actually the ambiguity. The real problem: three independent, hardcoded, **disagreeing** categorical re-bucketing sites, none referencing `dim_censorship_confidence` (dead code before this fix) at all — `int.ooni_signals.sql` (dead, HIGH≥0.80/MEDIUM≥0.60), `features.protocol_daily_signals.sql` (live, agreed with the dead file), and `marts.fact_platform_blocking_summary` (live, **disagreed**: HIGH≥0.90/MEDIUM≥0.70). **Fix:** left the 6 raw literals untouched; added a `min_score` column to `dim_censorship_confidence` (LOW=0.00, MEDIUM=0.60, HIGH=0.80 — canonicalizing on the newer, majority `protocol_daily_signals.sql` scheme over `fact_platform_blocking_summary`'s older one, per git-history evidence in ADR-0001); repointed both live sites to derive `confidence_level` via `LEFT JOIN ... QUALIFY ROW_NUMBER()` against the dimension table (a correlated-subquery form was tried first and rejected by BigQuery's validator); deleted `int.ooni_signals.sql`'s redundant dead `blocking_confidence` column outright (confirmed via grep that its only consumer, `dim_country.sql`, reads only the unrelated `country` column — clean surgical removal, not a partial fix). Verified via `bruin validate` (56/56 clean) and real `bruin run` materialization of all 5 affected assets, then a live BigQuery query confirming both live sites now assign identical `confidence_level` for identical `confidence_score` (both report exactly 50,236 HIGH-confidence events, where they previously used disagreeing thresholds). **Important caveat:** no currently-materialized report actually changes as a result — the disputed threshold band (`[0.60,0.70)` and `[0.80,0.90)`) contains zero real rows in the live Kenya OONI dataset today (the 0.80 TCP-reset and 0.75 TLS-failure literals exist in code but have never fired against real data), so this closes a **latent** risk, not a currently-visible mislabeling. Streamlit pages consuming `protocol_daily_signals`/`fact_protocol_blocking_summary` reverified rendering correctly after the change. A stray doc comment in `int.acled_event_classification.sql:267` referencing `int.ooni_signals.sql`'s now-removed bucketing is logged as TD-43, not fixed here.

**Historical note, preserved from this item's original text — a separate, unrelated subsystem (do not conflate with the above):** during the 2026-07-04 ACLED path A backfill, batches over weeks 466–665 (1997–2009) showed every multi-week ESCALATION regime decaying from a real `MEDIUM`/`HIGH` `classification_confidence` in its first week to `INSUFFICIENT_DATA`/`UNKNOWN` in every subsequent week of the same transition — initially looked like a structural flaw tied to persistence length. Batch over weeks 666–1165 (2009–2019) contradicted that reading: most post-2015 multi-week windows (2015-02-21→04-04, 7 weeks; 2017-10-07→10-28, 4 weeks; 2018-08-04→10-13, 10 weeks) held `HIGH`/`MEDIUM` confidence throughout with zero decay, and one window (2011-01-29→03-05) oscillated non-monotonically. The pattern looks tied to genuine ACLED event-reporting sparsity in Kenya's earlier (1997–2014) coverage, not "week N of a persistent regime" — but this was an inference from batch behavior, never a confirmed root cause, and is **not resolved by this OONI fix**. This is ACLED path A's `classification_confidence`/`methodology_risk_level` field, a wholly separate subsystem from the OONI `confidence_score`/`dim_censorship_confidence` scheme this item otherwise covers; they happen to share the word "confidence" only. | Closed for the OONI categorical-bucketing ambiguity this item was originally opened for. The ACLED path A confidence-decay question (historical note above) remains open and untracked by any other TD item — if revisited, test both the early-period sparsity-linked decay (2008-01, 2009-02/03 windows) and the later non-decaying windows (2015, 2017, 2018) to confirm the sparsity explanation rather than assume it, and open a new TD item rather than reusing this one, since the subsystems are unrelated. |
| TD-13 | `infra/setup-gcp.sh` lines 10, 50–52; formerly present at `infra/terraform-key.json` and `infra/terraform-key.toml` | **RESOLVED, 2026-07-04.** Two on-disk copies of `terraform-sa` credentials existed, not one: `infra/terraform-key.json` (the originally-found key) and a second, later-discovered `infra/terraform-key.toml` (same service account, re-encoded — field layout matched Streamlit's `secrets.toml` `[gcp_service_account]` convention, apparently a staging artifact for Streamlit Cloud secrets that was never moved to its correct path and never wired into any runtime code path). Neither file was ever committed to git (`git log --all --full-history` confirmed empty for both). Both local files are now deleted. `gcloud iam service-accounts keys list --iam-account=terraform-sa@encoded-joy-485413-k5.iam.gserviceaccount.com` confirms zero `USER_MANAGED` keys remain server-side (only Google's own `SYSTEM_MANAGED` keys, which cannot and need not be deleted) — the rotation had already removed the live key before the `.toml` copy was found, so there was nothing further to revoke, only the local file to remove. CI authentication runs through Workload Identity Federation: pool `clio-dev`, provider `github` (issuer `token.actions.githubusercontent.com`, condition scoped to `Sanjomwa/Civil-Liberties-and-Censorship-Analysis-with-Bruin`), impersonating a narrowly-scoped `bruin-ci` service account (`bigquery.dataEditor`, `bigquery.jobUser`, `storage.objectAdmin` — not the broad `terraform-sa`). Verified working end to end in `.github/workflows/gcp-auth.yml` (commit `b9417fb`): `bq ls` returned the real reporting marts with no static key anywhere in the chain. Interactive Codespace/local dev auth uses `gcloud auth application-default login` (for client libraries/ADC) plus `gcloud auth login` (for the `gcloud`/`gsutil` CLI's own operations) — no key file needed for either. **Annotation, 2026-07-09 (repo rename):** the `github` WIF provider's `attributeCondition` quoted directly above (`Sanjomwa/Civil-Liberties-and-Censorship-Analysis-with-Bruin`) went stale the moment the GitHub repository was renamed to `Civil-Liberties-Intelligence-Observatory` (see CLAUDE.md's 2026-07-06 naming note) — GitHub's OIDC token `repository` claim changes to match the new name immediately on rename, so the condition would have silently rejected every CI auth attempt. Confirmed live before changing anything (`gcloud iam workload-identity-pools providers describe github --workload-identity-pool=clio-dev --location=global --format="value(attributeCondition)"` returned exactly the stale string above), then updated via `gcloud iam workload-identity-pools providers update-oidc github --workload-identity-pool=clio-dev --location=global --attribute-condition="assertion.repository == 'Sanjomwa/Civil-Liberties-Intelligence-Observatory'"` — confirmed applied by re-describing the provider immediately after. Local `origin` remote and `README.md`'s clone instructions updated to the new URL in the same pass. **A second, independent stale binding was found and fixed in the same pass, before committing — WIF gates auth in two places, not one.** The provider's `attributeCondition` (fixed above) only controls which OIDC tokens are admitted to the pool; a separate `roles/iam.workloadIdentityUser` binding on the `bruin-ci` service account itself controls which admitted identities may *impersonate* it, via a `principalSet://.../attribute.repository/OWNER/REPO` member — and `gcloud iam service-accounts get-iam-policy bruin-ci@encoded-joy-485413-k5.iam.gserviceaccount.com` showed that binding still named the old repo. Had this gone unfixed, CI would have passed the provider condition and then failed impersonation — broken in exactly the same silent way this task set out to prevent, just one step later in the chain. Fixed via `add-iam-policy-binding` (new-repo member) then `remove-iam-policy-binding` (old-repo member); confirmed via `get-iam-policy` that exactly one binding remains, scoped to `Sanjomwa/Civil-Liberties-Intelligence-Observatory`. `.github/workflows/gcp-auth.yml`'s `google-github-actions/auth@v2` step confirmed to reference this same provider and SA by project number/email, not by repo name — no third hardcoded site. **CONFIRMED end-to-end, 2026-07-09 (same day, after push):** this Codespace's own `workflow_dispatch` attempt was blocked by the Codespace's scoped `GITHUB_TOKEN` (403, permissions only, not a sign of a broken fix — recorded above) — the project owner triggered `staleness-check.yml` manually from the GitHub UI post-rename instead. Result: **Success**, commit `02e416c`, 19s duration, zero staleness violations — real BigQuery access through the updated WIF condition and the repointed `bruin-ci` impersonation binding, not just a config write that was assumed to work. Both halves of the WIF trust chain are now proven live against the renamed repository, not merely applied. One harmless, unrelated platform note from that run, logged for awareness only: GitHub's runners are auto-forcing `actions/checkout@v4`, `actions/setup-python@v5`, and `google-github-actions/auth@v2` from their targeted Node.js 20 onto Node.js 24 (a deprecation notice, not a failure, not caused by the rename) — see new **TD-62**. | Remaining, non-blocking: `infra/setup-gcp.sh` itself still documents the old key-download flow and should be updated or removed to match; `terraform-sa` still holds broad `roles/bigquery.admin`/`roles/storage.admin`/`roles/iam.serviceAccountUser` (see TD-14) and should eventually be narrowed the same way `bruin-ci` was; Streamlit's own `secrets.toml`-based GCP auth path is currently unconfigured (no file exists at `streamlit/.streamlit/secrets.toml`), which is likely fine if the dashboard authenticates some other way, but is worth a quick confirmation — see new TD-37. |

## High

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-06 | `Bruin/requirements.txt` vs. `pyproject.toml` / `uv.lock` | `duckdb==0.10.0` pinned in one lockfile, `duckdb>=1.5.0` (resolving to 1.5.0) in the other. Two authoritative dependency mechanisms disagree by more than a major version. | Regenerate `Bruin/requirements.txt` from the same source as `uv.lock`, or eliminate the duplicate lockfile. |
| TD-07 | `.devcontainer/devcontainer.json` | Base image is Python 3.11; `pyproject.toml` requires `>=3.12.3`. Anyone using the documented devcontainer is on the wrong interpreter version. | Bump the devcontainer base image. |
| TD-08 | `tests/test_contracts.py`; `tests/test_acled_pressure_regimes_golden.py` (Layer B, landed 2026-07-04) | The only test file in the repository was `tests/test_contracts.py` (~70 lines, four tests), covering only the Streamlit contract-validation helper — the classification and regime logic, carrying the most business and legal risk, had zero behavioral test coverage. **RESOLVED for Layer B, 2026-07-04.** `tests/test_acled_pressure_regimes_golden.py` asserts the confirmed backfill output for the Finance Bill 2024 window and the Jan-Feb 2008 post-election violence against recorded JSON fixtures (`tests/fixtures/acled_regimes_golden/`). Confirmed passing against live data (`RUN_BIGQUERY_TESTS=1 pytest ... -v`: 2 passed in 20s) and skipping cleanly without the opt-in var (`pytest -q`: 4 passed, 2 skipped in 0.7s) — safe to leave in the repo before CI is wired, since it won't break the existing suite. Committed `b3ae94d`, not yet pushed. This is **Layer B only** (a drift check against already-validated materialized BigQuery output, not a rerun of the SQL logic against frozen input) — see TD-39 for why the stronger Layer A design is deferred. | Push `b3ae94d`. Wire into `.github/workflows/tests.yml` (reusing the WIF credentials `gcp-auth.yml` already proves work) as a separate follow-up, per `testing-strategy.md`'s instruction that this belongs in the existing test workflow. TD-39 remains the path to Layer A. |
| TD-09 | Multiple mart SQL files (e.g. `fact_country_pressure_daily.sql`, `protocol_repression_correlation_mart.sql`, `asn_behavior_profile_mart.sql`) | Hardcoded project ID (`encoded-joy-485413-k5`) and country literal (`iso2 = 'KE'` / `country = 'KE'`) scattered across mart SQL. Blocks multi-country expansion. | **RESOLVED for the SQL layer, 2026-07-06 (multi-country scaffolding pass).** The project-ID half turned out to be already done (every asset templates `{{ var.project_id }}`; the row's claim was stale). The country half: a full sweep found **20** literal sites, not the 16 previously on record (additions: `dim_country.sql:53`, `fact_country_pressure_daily.sql:228`, `stg.lumen_requests.sql:41`, plus the recount). 17 scope-filter sites routed through the already-declared `{{ var.country }}`/`{{ var.iso2 }}` pipeline variables (the idiom 40+ existing sites already used — this was completing an established pattern, not adding one); the 3 remaining literals live in `dim_country.sql`'s normalization CTE, formally designated the single legitimate location (they are raw-value→canonical mapping *data*, not scope filters; the file's header now says so). A `dim_countries` reference table was **rejected at n=1 country** per the two-concrete-cases bar — the variable layer is sufficient until a second country's ingestion actually starts. Durability: a new CI check (`Bruin/scripts/country_literal_check/check_country_literals.py` + `.github/workflows/country-literal-check.yml`, mirroring ADR-0005's script+allowlist pattern, comment-aware, verified against both a clean tree and a planted literal) fails any new quoted `'KE'`/`'KEN'`/`'KENYA'`/`'Kenya'` literal in a SQL asset outside the allowlisted `dim_country.sql`. **Residual, logged as TD-60:** the Python ingestion layer (`ooni_raw.py:49`, `PROBE_CC = "KE"`) still hardcodes Kenya and is outside this check's reach. |
| TD-10 | ACLED, Lumen, OONI actor/requestor/ASN identifiers | No entity resolution across sources — the same real-world actor can appear under different labels in different tables. | A `dim_entities` canonical mapping table, deferred until the pressure-attribution work that needs it is underway. |
| TD-11 | `scripts/local_ingest_ooni.py` line 20, `scripts/download_ooni.ps1` line 2 | Both hardcode `C:\ooni-kenya-censorship` as the default/base local ingestion path. The local-ingestion story is Windows-only by convention, in a project that also runs in Linux Codespaces. | Make the root path environment-driven with a documented default per platform. |
| TD-12 | `scripts/download_ooni.ps1` line 5 | Manual resume checkpoint (`$ResumeFromDate = "2025-06-29"`) with an inline "CHANGE THIS to resume" comment — no persisted state file, relies on the operator remembering to edit the script before each run. | Persist the last-synced date to a small state file the script reads and updates automatically. |
| TD-14 | `infra/modules/iam/main.tf` | Grants `roles/editor` (broad, not least-privilege) to a single hardcoded human admin identity at the module level. | Scope to specific roles needed, separate from the more carefully scoped `terraform-sa` service account already set up in `setup-gcp.sh`. |
| TD-15 | `Bruin/scripts/historical_initializer/backfill_acled_pressure_regimes.py` line 84 | `METHODOLOGY_VERSION = "ACLED_REGIME_ENGINE_V1"` must manually match a literal inside the SQL asset's CTE-01, with no automated check that they agree. | Centralize into the methodology governance layer already recommended elsewhere (see Architecture Assessment Section 7); until then, add a CI check comparing the two literals. |

## Medium

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-16 | `streamlit/services/marts.py` lines 163–165 | **RESOLVED, 2026-07-12.** `get_protocol_stress_intelligence()` was a trivial alias for `get_protocol_regimes()` — pages 2 and 3 rendered the identical underlying query with no distinct data. Re-confirmed still true before merging, not assumed from the 2026-07-06 decision-log recommendation. Merged into `streamlit/pages/protocol_intelligence.py` (two tabs), the alias deleted from `marts.py`. See the decision-log's 2026-07-12 entry for the full merge reasoning. | Closed. |
| TD-17 | `streamlit/services/freshness.py` | Entirely unused dead code (confirmed via repo-wide search — no page or service imports it). Internal header comment (`# utils/validate.py`) does not match its filename, and its schema constants reference the stale dimensional-model vocabulary, not the real reporting marts. | Delete. |
| TD-18 | `streamlit/components/charts.py`, `streamlit/components/tables.py` | Both self-documented as "aspirational, kept for future use" and confirmed unused by any page. | Delete, or move to a clearly labeled `future/` or `Archive/` location if genuinely intended for near-term use. |
| TD-19 | `streamlit/services/bq.py` lines 28–29 | `_get_streamlit_service_account_info()` swallows any secret-parsing error via a bare `except Exception: return None`, which could silently hide a misconfigured secret. | Log the specific exception even if the function still falls back to `None`. |
| TD-20 | `streamlit/services/bq.py`, `run_query()` bare-exception branch | Full stack traces are shown to end users via `st.exception(exc)` on unexpected query failure in a public-facing app — an information-disclosure smell (could reveal project IDs, dataset names, or query text). | Log the traceback server-side; show a generic message to the user. |
| TD-21 | `Bruin/assets/marts/facts/fact_platform_blocking_summary.sql` (now `fact_protocol_blocking_summary.sql`) | **RESOLVED, 2026-07-05.** Filename said "platform," the internal `name:` field said "protocol." **Diagnosis corrected before fixing, same discipline as TD-02/TD-40/ADR-0001:** the initial assumption (filename is the better anchor, "since it's what other assets would `depends:` reference by convention") was checked directly against the repo and found **wrong** — every cross-asset `depends:` reference in this codebase resolves by the declared `name:` field, which is also the literal materialized BigQuery table name (e.g. `reporting.mart_protocol_interference_trends`, `stg.ooni_measurements` — both already diverge from their filenames with zero issue, an established pattern, not a first offender). A live `bq ls`/`bq show` check surfaced a second, independent finding: **two** tables already existed in `marts` — `fact_protocol_blocking_summary` (375 rows, current, matching the live `name:` field) and an orphaned `fact_platform_blocking_summary` (163 rows, stale, last written before an earlier rename that changed the `name:` field but never touched the file's own historical content) — logged as new TD-53, not fixed here. | Renamed the **file** to `fact_protocol_blocking_summary.sql` to match the declared `name:` field (zero BigQuery side effects, since nothing depended on the old filename), rather than renaming the field to match the file (which would have silently resurrected/overwritten the orphaned stale table on next run). Updated the one stray in-repo comment (`dim_censorship_confidence.sql`) that referenced the old "platform" name. `grep` confirms zero remaining references to `fact_platform_blocking_summary` anywhere in the live repo. `bruin validate` clean (56/56) before and after. |
| TD-22 | Top-level `README.md`, "Repository Structure" section | **RESOLVED, 2026-07-12.** Was stale on two more axes than originally scoped: it listed one of nine Streamlit pages and omitted several actually-used `core/`/`components/` modules, but it was also generated from a working-tree view rather than the public repository — it implied `docs/00-overview` through `docs/07-governance` all ship publicly, when `.gitignore` deliberately excludes most of them (only `docs/02-architecture` and `docs/03-development` are tracked, alongside legacy flat `docs/*.md` files predating the numbered restructure). Regenerated from `git ls-files` (the actual public tree), not `find` against the local working directory — verified directly rather than assumed correct, per this project's own diagnose-before-fixing discipline. Part of the broader A6 (public repository readiness) pass — see `decision-log.md`'s 2026-07-12 entry. | Closed. |
| TD-23 | `docs/data-modelling.md`, `docs/civil-liberties-reporting-playbook-Kenya.md` | **RESOLVED, 2026-07-12.** Both described a Kimball/CLRI dimensional architecture inconsistent with the actual implemented reporting layer's vocabulary. Archived (not deleted) to `Archive/`, alongside four other pre-restructure `docs/` root files with the same problem (`data_sources.md`, `erd-lineage.md`, `analysts-questions-playbook.md`, `project_difficulties.md`) — see `Archive/README.md` for the full disposition. `data-modelling.md`, `data_sources.md`, and `erd-lineage.md` were rewritten fresh at `docs/02-architecture/` grounded in the live repo/BigQuery schema, not the archived drafts; `analysts-questions-playbook.md` and `civil-liberties-reporting-playbook-Kenya.md` have no direct replacement (their ground covered by the new `data_sources.md` and the README's own per-page dashboard descriptions). | Closed. |
| TD-24 | `docs/readme1_1.md` | **RESOLVED, 2026-07-12.** Reconciled before archiving, per this row's own recommended action: `readme1_1.md`'s Ethics and Responsible Use section was read in full and compared against the current `README.md`. Real, still-missing content was found and folded in — an explicit no-PII/no-deanonymization commitment, a no-exploitation-tooling harm-minimization statement, and an explicit "don't use outputs to justify harm or discrimination" clause — three genuine safeguards that had not been superseded by anything added since TD-24 was logged. The rest of the draft (architecture diagrams, dashboard descriptions matching a much earlier UI, a Cloud Run deployment target never built) is fully superseded by the current `README.md` and needed no reconciliation. Archived to `Archive/readme1_1.md`. | Closed. |
| TD-25 | `Bruin/scripts/historical_initializer/backfill_acled_pressure_regimes.py` lines 95–96 | Debug SQL logging (`DEBUG_SQL_LOG`) writes every MERGE statement to disk, left enabled in the shipped script despite a comment saying to disable it once debugging is complete. | Disable by default; make opt-in via a flag. |
| TD-26 | `streamlit/pages/3_Protocol__Stress_Intelligence_Observatory.py` (filename) | **RESOLVED, 2026-07-12.** Moot: the file was merged into `protocol_intelligence.py` as part of TD-16's fix, and the 2026-07-12 `st.navigation`/`st.Page` migration removed numeric filename prefixes from every page — there is no longer a filename-typo axis for this bug to exist on. | Closed. |
| TD-27 | `streamlit/pages/3_Protocol__Stress_Intelligence_Observatory.py` vs. all other pages | **RESOLVED, 2026-07-12.** The merged `protocol_intelligence.py` inherited page 3's `width="stretch"` calls; converted all 8 to `use_container_width=True` to match every other page, closing the inconsistency for real rather than just relocating it into the merged file. | Closed. |
| TD-28 | `streamlit/pages/5_ASN_Behavioral_Intelligence.py` line 67 | Passes a literal string (`"Latest Profile Snapshot"`) into a trust-strip parameter that every other page fills with an actual date value — likely intentional (this mart has no date column) but undocumented as such. | Add a code comment explaining why this page's contract differs. |
| TD-29 | `upgraded_assets/features/acled_pressure_signals.sql` (unsuffixed variant) | Reverts several deliberate fixes already made in the canonical file: removes per-family baseline gating, removes the calendar spine (re-introducing the exact non-adjacent-week corruption the canonical version's own comments warn against), and reverts the `stddev_floor` to the value the canonical version explicitly rejected as invalid for ACLED data. | Must not be merged as-is. Treat as a historical/earlier draft, not an upgrade, when deciding what to do with `upgraded_assets/`. |
| TD-30 | `upgraded_assets/intermediate/int.acled_event_classification.sql` (unsuffixed variant) | Reintroduces a bug the canonical file's own comments describe fixing: computes `methodology_risk_level` using `WHEN event_type = 'UNCLASSIFIED'`, a condition that can never be true, silently defaulting every unclassified event to `LOW` risk instead of the intended `HIGH`. | Must not be merged as-is; flag clearly in any archival note so a future contributor does not reintroduce this regression. |
| TD-31 | `Bruin/assets/marts/dims/`, `marts/facts/` documentation vs. code | The code is real and load-bearing (Architecture Assessment Section 5); the documentation describing it is stale/aspirational. This is a documentation debt, not a code debt, but is listed here because it actively misleads. | See Repository Documentation Plan. |
| TD-32 | `logs/query_log.sql` | Empty (zero lines), no apparent write path. | Delete. |

## Low

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-33 | `infra/modules/gcs/main.tf` | `force_destroy = true` on the storage bucket — fine for dev/demo, a data-loss risk if carried unmodified into a production environment. | Revisit before any production deployment. |
| TD-34 | `infra/verify-gcp.sh` | Not wired into CI; minimal exit-code handling. | Low priority; wire in once CI covers infra changes at all. |
| TD-35 | `infra/modules/iam/main.tf` vs. `infra/setup-gcp.sh` | Two overlapping but different identity-management approaches (a broadly-scoped human admin grant in Terraform, a more narrowly-scoped `terraform-sa` service account in the shell script) with no written explanation of which governs what. | Document the intended division of responsibility, or consolidate. |

## TD-36 — new, discovered 2026-07-04

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-36 | Codespace environment | **RESOLVED, 2026-07-04.** The canonical Codespace was missing the tooling needed to actually run the pipeline against real data. Now fixed: `gcloud` 575.0.0 and `bq` 2.1.33 installed via Google's official apt repo; `bruin` v0.11.664 installed via the official install script; `google-cloud-bigquery==3.41.0` confirmed already pinned in `Bruin/requirements.txt`/`pyproject.toml` and installed into the active environment; `.env`'s `GCP_PROJECT_ID` updated from the placeholder to the real value `encoded-joy-485413-k5`. Both `gcloud auth application-default login` (ADC, for client libraries) and `gcloud auth login` (for the CLI's own IAM/key-management operations) have been run successfully and interactively by the project owner. | Closed. No further action needed for this item. |

## TD-37 — new, discovered 2026-07-04

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-37 | `streamlit/services/bq.py`, `_get_streamlit_service_account_info()`; expected `streamlit/.streamlit/secrets.toml` | **RESOLVED AND VERIFIED LIVE, 2026-07-12.** Community Cloud has no WIF equivalent (confirmed: it isn't a GitHub Actions-style OIDC-capable runner), so a static, dedicated, least-privilege key is the correct exception to the WIF-preferred standard in `coding-standards.md`, not a regression from it. Built: a new dedicated service account `clio-streamlit-dashboard@encoded-joy-485413-k5.iam.gserviceaccount.com`, granted `roles/bigquery.dataViewer` at the **dataset level** (not project-wide) on exactly the 4 datasets `streamlit/core/config.py`'s `DATASETS` declares — `reporting`, `marts`, `features`, `intelligence` — via `bq update`'s dataset ACL, plus `roles/bigquery.jobUser` at the project level (unavoidably project-scoped). Verified locally end-to-end before handoff (real query against all 4 datasets, all returned rows), then the project owner pasted the secret into Streamlit Community Cloud's Settings → Secrets UI and made the app's sharing setting public (it had defaulted to private/restricted, a second, independent blocker caught live — see decision-log). **Live re-verification, same day, after both fixes:** all 9 dashboard pages loaded with zero credential errors and real query results (e.g. National Stress Observatory: 761 records; Protocol Repression Correlation: real per-protocol rolling-correlation series through 2025-06-30); confirmed via headless-Chromium screenshots at the app's corrected URL (`civil-lliberties-intelligence-observatory-toafjdj5xoc.streamlit.app` — the project owner renamed the deployment slug to match CLIO's naming; the old `civil-liberties-and-censorship-analysis-with-bruin-...` URL is dead, updated in `README.md` and `Bruin/README.md`). Also confirmed as a secondary check: every `marts.py` query function is `@st.cache_data(ttl=3600)`, so the public dashboard on this key is not a fresh-query-per-pageview cost/abuse surface. | Closed. Remaining non-blocking follow-up: rotate or delete the generated key (id `a4df8e5e...`) if the local scratchpad copy is ever suspected exposed; it was never committed to the repo. |

## TD-38 — new, discovered 2026-07-04, Critical — RESOLVED 2026-07-04

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-38 | `Bruin/pipeline.yml` (`target_week` variable) vs. `Bruin/assets/features/acled_pressure_signals.sql` (`strategy: create+replace`) vs. `intelligence.acled_pressure_regimes.sql`'s EXECUTION CONTRACT; resolved via new `Bruin/scripts/steady_state/run_acled_regime_week.py` | **RESOLVED, 2026-07-04.** `pipeline.yml` documented `target_week=""` as the correct default for "all normal steady-state weekly pipeline runs," but `features.acled_pressure_signals` (the regime engine's upstream source) uses `strategy: create+replace` with no incremental filter — every run recomputes the full calendar spine for the country's entire history, not just the newest week, silently violating the regime engine's own EXECUTION CONTRACT (exactly one new week per execution) on every steady-state invocation. Fixed by adding `Bruin/scripts/steady_state/run_acled_regime_week.py`, which resolves the single next unprocessed week itself — `MAX(week_start_date)` in `intelligence.acled_pressure_regimes` as ground truth, the next row in `features.acled_pressure_signals` — and always invokes Bruin with that explicit `target_week`, never `""`. When no next week is available, it logs a clean no-op and exits 0 rather than erroring or falling back to the unsafe default. Reuses (imports, doesn't duplicate) `sql_get_max_committed_week`, `sql_get_ordered_weeks`, `invoke_bruin`, `confirm_week_committed`, `sql_upsert_status`, and `_write_complete` from `backfill_acled_pressure_regimes.py`. `pipeline.yml`'s comment corrected to remove both false claims and document the new script's role; any other manual/ad-hoc invocation must still set `target_week` explicitly. Verified live against Kenya: took the no-op path correctly (no week exists yet beyond the backfill's 2026-03-14), exit 0, `country_initialization_status` confirmed byte-for-byte unchanged afterward — run twice to confirm no side effects. Committed `d0ffefa`, pushed. | Closed. The script is what any future cron/CI steady-state schedule must invoke — never a bare `bruin run` against the pipeline with defaults. |

## TD-39 — new, discovered 2026-07-04, Low

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-39 | `Bruin/assets/intelligence/acled_pressure_regimes.sql` CTE-02 (hardcoded dataset names); `.github/workflows/tests.yml` (no GCP credentials in the pytest job) | A true golden-file regression test (TD-08's "Layer A" — rerun the classification SQL against a frozen input snapshot, catching a logic bug before it's ever executed against real data) is not achievable in this repo today. Two concrete blockers, found while designing TD-08: (1) `acled_pressure_regimes.sql` templates `{{ var.project_id }}` but hardcodes the dataset names (`features.acled_pressure_signals`, and the self-referential `intelligence.acled_pressure_regimes` join) — only the project is swappable, not the dataset, so isolating a test run requires either a second GCP project or parameterizing the dataset in this ARCHITECTURE-FREEZE'd, deliberately-preserved SQL body (out of scope to edit for test convenience alone). (2) `.github/workflows/tests.yml` runs `pytest -q` with no GCP credentials at all — the WIF auth proven in the separate `gcp-auth.yml` workflow has never been wired into the test job. Neither blocker is expensive to fix, but both require a real infrastructure decision (a dedicated test dataset/project, a fixture-loading mechanism, and CI credential wiring), not something to improvise as a side effect of writing two test files. | Decide: stand up an isolated BigQuery test dataset (same project, separate dataset, or a fully separate project) and a fixture-loading mechanism, then parameterize `acled_pressure_regimes.sql`'s dataset references (carefully — this file is otherwise frozen) and wire WIF credentials into `tests.yml`. Until decided, TD-08 ships as Layer B only (a real, if narrower, improvement over zero coverage). |

## TD-40 — RESOLVED 2026-07-05

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-40 | `Bruin/assets/marts/facts/fact_country_pressure_daily.sql`'s `acled` CTE (`GROUP BY event_date`, joined via `measurement_date`) vs. `Bruin/assets/staging/stg.acled_conflict_events.sql` (whose own header states `event_date` is NULL for all rows, "reserved for future event-level ACLED ingestion") | **Root cause confirmed via git history:** commit `6dbe7ab` ("Upgrade acled to pressure signals", 2026-06-06) renamed the real, parsed weekly date field from `event_date` to `week_start_date` (correct — it is a weekly value) and, in the same commit, introduced a *new* column reusing the name `event_date`, hardcoded to `CAST(NULL AS DATE)`, reserved for a future event-level pipeline. `fact_country_pressure_daily.sql` (written earlier, `aa01c3a`) was never updated to match, so its `acled` CTE kept referencing a name that now pointed at a permanently-empty column. See TD-41 for a second, independent casualty of the same commit. **RESOLVED, 2026-07-05.** The `acled` CTE now does `GROUP BY week_start_date`, joined via `DATE_TRUNC(measurement_date, WEEK(SATURDAY)) = week_start_date` — the same key established for the ADR-0002 regime join in this same file — with the weekly-broadcast consequence for `composite_pressure_score` documented inline. `stg.acled_conflict_events.sql`'s stale "Monday anchor" comment corrected to Saturday in both the header and the column description. `bruin validate` passed 57/57 both before and after. `fact_country_pressure_daily` was then actually materialized via `bruin run` for the first time since ADR-0002 step (e) added the `regime_*` columns — which also resolved a pre-existing `bruin validate` regression (the reporting marts couldn't find those columns because they had never actually been materialized into the live table). Verified against the live table: 761/761 rows now non-zero; the Finance Bill 2024 window reproduces exactly (`STABLE` through 2024-06-21, steps to `CRISIS` precisely on Saturday 2024-06-22 — the week Parliament was stormed — through three further weekly steps, reverting to `STABLE` on 2024-07-13); the broadcast mechanic confirmed directly via `LAG()` (`conflict_pressure_score` bit-for-bit constant Saturday–Friday, stepping only on Saturdays). **Caveat, not a fix failure:** the Jan-Feb 2008 window could not be re-verified against this table — `marts.dim_dates` (and so this asset's entire date spine) only covers 2023-06-01 to 2025-06-30, a pre-existing, TD-40-unrelated limitation; that period remains verified only against `intelligence.acled_pressure_regimes` directly (TD-08's golden-file tests, full history). Committed and pushed. | Closed. If `marts.dim_dates`'s narrow 2023–2025 window is ever revisited, re-verify the Jan-Feb 2008 and Aug 2022 windows against `fact_country_pressure_daily` directly, not just against the regime table. |

## TD-41 — DECIDED 2026-07-05: retire (asset file deleted then); orphaned table dropped 2026-07-06

**Follow-up, 2026-07-06 (cost-audit cleanup pass):** the asset SQL file was deleted when this item was decided, but the materialized `marts.fact_conflict_events` table (267,956 rows, ~0.05 GiB) was left behind — the cost audit's orphan sweep (live tables with no producing asset) resurfaced it. Dropped via `bq rm` in the same pass that resolved TD-53/TD-56, after confirming no producing asset, no repo reference, no scheduled query, and no view depends on it.

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-41 | `Bruin/assets/marts/facts/fact_conflict_events.sql` (`WHERE event_date IS NOT NULL`) | A second, independent casualty of the same commit `6dbe7ab` that caused TD-40 — different symptom, same root cause. This asset filters on `event_date IS NOT NULL` against `stg.acled_conflict_events`, and since `event_date` is hardcoded `NULL` on 100% of rows since `6dbe7ab` (2026-06-06), this asset has been silently producing a **zero-row, empty table** on every materialization for about a month. A comprehensive grep of `Bruin/` and `streamlit/` found nothing that reads from `fact_conflict_events` at all today. **Decided, 2026-07-05, per the project owner's own account of the project's history:** retire, don't fix. Path B and its satellite assets (this one among them) predate the point where the same analytical rigor already applied to OONI was extended to ACLED (path A) — they are residue of the project's earlier phase, not a component with an intended future role. This is the same situation as TD-17, TD-18, and TD-32 (confirmed dead code, deleted rather than repaired), not a new pattern. | Delete `Bruin/assets/marts/facts/fact_conflict_events.sql`. Confirm no `@bruin depends:` reference to it exists in any other asset before removing (the grep already run for TD-40/41 found none, but re-confirm as part of the deletion itself). |

## TD-42 — discovered 2026-07-05 during TD-01's investigation; RESOLVED 2026-07-06 (cost-audit cleanup pass)

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-42 | Bruin asset `lumen_requests_to_gcs` (declares `@bruin depends: raw.lumen_requests`) vs. its actual code, which reads a hardcoded local file path (`data/dev/lumen/lumen_requests.parquet`) | The asset's declared Bruin dependency and its actual data source have silently diverged: the code ignores `raw.lumen_requests` entirely and reads a local dev-fixture file instead. The `raw` dataset does not even exist in BigQuery (the staleness-check script's own mechanism notes already record this). Same category of finding as TD-38 (declared pipeline behavior silently diverging from actual behavior), surfaced as a side effect of TD-01's consumer-chain trace. | **RESOLVED, 2026-07-06 — retired the false declaration rather than reconciling it, exactly as this task framed the choice ("don't spend effort making this technically correct if the honest answer is 'this pathway isn't going anywhere'").** Lumen is formally benched (ADR-0004), so wiring the code to a real `raw.lumen_requests` upstream would be effort spent dignifying a pathway with no near-term future. Removed the `depends: raw.lumen_requests` line from `load/lumen_requests.py`'s `@bruin` header and replaced it with a docstring note stating plainly that the asset reads a local dev fixture (not a Bruin asset), that its former declaration named a lineage that never existed, and that a future real Lumen source should declare an accurate upstream at that time. The asset itself is retained (benching is not deletion, per ADR-0004). `bruin validate` clean after the change (the staleness check already skips this asset — it is a `type: python` load asset, not a monitored `bq.sql` table). |

## TD-43 — new, discovered 2026-07-05 during TD-05's resolution

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-43 | `Bruin/assets/intermediate/int.acled_event_classification.sql`, line 267 (`classification_confidence` column description: "Mirrors HIGH/MEDIUM/LOW in int.ooni_signals.sql.") | **RESOLVED, 2026-07-05.** A doc-comment analogy, not a functional dependency — ACLED's `classification_confidence` field has always been computed by its own logic, this comment only pointed at OONI's `int.ooni_signals.sql` HIGH/MEDIUM/LOW scheme for a reader's intuition. As part of resolving TD-05, that scheme's implementation was deleted from `int.ooni_signals.sql` (dead code, confirmed unconsumed) and now lives as `dim_censorship_confidence.min_score` instead. | Updated the comment to reference `marts.dim_censorship_confidence` (the tier's real current home) and made explicit that this is a description-only analogy, not a functional dependency, to prevent the same staleness recurring. Metadata-only change (a `description:` field, not executable SQL) — no materialization needed. |

## TD-44 — new, discovered 2026-07-05 during ADR-0004's composite-score investigation

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-44 | `Bruin/assets/marts/facts/fact_country_pressure_daily.sql`'s `pressure_level` CASE thresholds (`>=6.5` SEVERE, `>=4.0` ELEVATED, `>=2.0` MODERATE) | These thresholds were never empirically re-derived against real score distributions — they predate this repo's current data and were not revisited when ADR-0004's reweighting (dropping Lumen's term, `conflict_pressure_score*0.60+legal_pressure_score*0.25+platform_pressure_score*0.15` → `conflict_pressure_score*0.75+platform_pressure_score*0.25`) shifted the mean composite score from 4.6336 to 4.1791 across 761 live Kenya days. Confirmed via live materialized data: 225/761 days (29.6%) changed `pressure_level` category as a direct result of the reweighting alone (218 ELEVATED→MODERATE, 7 MODERATE→ELEVATED), with the thresholds themselves untouched. This is not a bug in the reweighting — Option B was the correct decision (see ADR-0004) — but it is a signal that the SEVERE/ELEVATED/MODERATE/LOW boundaries were tuned (implicitly or not at all) against a score distribution that included a fabricated 25%-weighted term, and have not been checked against the post-benching distribution. | Re-derive the four thresholds against the current (post-reweighting) score distribution — e.g. by percentile or by re-checking against known incident windows (Finance Bill 2024, post-election 2008) the way ADR-0002's regime engine thresholds were validated — rather than assuming the old cutoffs still carve the distribution the same way. Not urgent: the flagship Finance Bill 2024 window's most severe day is unaffected (`ELEVATED` both before and after), but 225 days moving category elsewhere in the series is a real change to the dashboard's reported history. |

## TD-45 — new, discovered 2026-07-05 during ADR-0004's composite-score investigation, RESOLVED 2026-07-18

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-45 | `Bruin/assets/marts/facts/fact_country_pressure_daily.sql` (`composite_pressure_score`, three weighted sub-scores) vs. `Bruin/assets/reporting/political_stress_windows_mart.sql` (also emits a column named `composite_pressure_score`, computed differently) | **RESOLVED.** Two different formulas shared one column name. The fact table's `composite_pressure_score` was renamed `source_composite_pressure_score` inside `political_stress_windows_mart.sql`'s `country_pressure` CTE, then a *new* value — also called `composite_pressure_score` — was computed as `source_composite_pressure_score + signal_rate*5 + weighted_blocking*8 + max_protocol_stress_score*0.04 + elevated_protocol_count*0.18 - (1-avg_sample_quality_score)*1.2`. The dashboard's National Stress Observatory KPI, trend line, and CSV export (`streamlit/pages/national_stress_observatory.py`) all read the *mart's* recomputed value, not the fact table's raw column — easy to miss when reasoning about "composite_pressure_score" across the codebase, since the name alone doesn't disambiguate which formula is in play. Discovered while tracing every consumer of the fact table's score for ADR-0004; not itself a bug (both values were internally consistent and correctly documented in each asset's own comments), but a real naming-collision risk for future changes. | **Resolved as part of TD-66's fix (2026-07-18) — see that entry for full detail.** `political_stress_windows_mart.sql`'s `composite_pressure_score` is now a direct passthrough of the fact table's own column (no recomputation under that name); the mart's OONI-fused recomputation was deleted outright, not renamed. There is exactly one `composite_pressure_score` formula in the codebase now, defined once. The naming collision this row flagged no longer exists. |

## TD-46 — new, discovered and RESOLVED 2026-07-05, same day as ADR-0004's reweighting

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-46 | `streamlit/pages/1_National_Stress_Observatory.py`, `6_Suppression_Event_Explorer.py`, `7_Finance_Bill_2024_Incident_Report.py` — all three called `synthetic_data_notice(["Lumen"])`, gated on `df["legal_pressure_is_synthetic"].any()` | ADR-0004's reweighting (this same day) removed `legal_pressure_score` from `composite_pressure_score` entirely, but the three Lumen-disclosure banners were left firing unchanged — they read "Contains fabricated data: Lumen figures on this page are synthetic... and should not be cited as evidence," which overclaimed once nothing on these pages was Lumen-derived anymore. Checked directly rather than assumed: every service function feeding these three pages (`get_national_stress`, `get_event_explorer`, `get_finance_bill_incident`) was grepped for `legal_pressure_score` — not present in any of the three SELECTs. `legal_pressure_is_synthetic` is fetched by all three, but grepping every `st.dataframe`/chart/`to_csv`/`download_button` call on all three pages found it rendered nowhere — its only use anywhere in these files was the `.any()` gate itself. **RESOLVED, 2026-07-05.** Removed the banner call and the now-unused `synthetic_data_notice` import from all three pages (no rewording needed, since none of the three had any Lumen-derived value to scope a disclosure to). Page 8's "Data Sources & Known Limitations" section remains the authoritative place explaining Lumen's benched status. Verified via headless-Chromium rendering of all three pages against live materialized data (`political_stress_windows_mart_v5`, `protocol_repression_correlation_mart_v4` visible in each trust strip): no banner present, no rendering errors, KPIs/charts/tables unaffected. `components/trust.py`'s `synthetic_data_notice()` function itself was left in place (not deleted) — it is now uncalled anywhere in the codebase, but is a generic, reusable component (takes an arbitrary `sources` list), not Lumen-specific in its implementation, and may still be useful if a future evidence source needs the same kind of scoped disclosure. | Closed. If `synthetic_data_notice` remains uncalled for an extended period, consider removing it as dead code — not urgent, since it's a small, generic, self-contained component with no maintenance cost. |

## TD-47 through TD-52 — new, discovered 2026-07-05 during the OONI app-identity/protocol-layer investigation

Investigation, not implementation, per the project's standing discipline (no code changed). Triggered by a direct question: can CLIO attribute a censorship finding to a specific app (Telegram, WhatsApp, Signal) and a specific protocol layer (DNS/TCP/TLS/HTTP), the way OONI's own raw measurements are structured to support? Traced `test_name` and cause-level protocol detail through every layer, raw → reporting, against live BigQuery data.

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-47 | `stg.ooni_dns_observations.sql:56`, `stg.ooni_tcp_observations.sql:65`, `stg.ooni_tls_observations.sql:67`, `stg.ooni_http_observations.sql:73` (the `UNNEST(...raw_test_keys...)` extraction in each) | High. **Bootstrap subset RESOLVED, 2026-07-05; `lookups` and `tor` subsets remain open (Tier 3, deliberately deferred).** `dnscheck` (~82% of all raw Kenya OONI rows) and `tor` (~3%) produce zero rows in all four staging observation tables because both use a fundamentally different JSON shape than `whatsapp`/`telegram`/`signal`/`psiphon` (which share OONI's flat, per-experiment `measurex`-style shape — one top-level `queries`/`tcp_connect`/`tls_handshakes`/`requests` array each, exactly what the current `UNNEST` paths expect). Confirmed by direct inspection of live raw rows: **`dnscheck`** has no top-level array in that shape at all; its real structure is `bootstrap` (one system-resolver DNS lookup, its query array at `$.bootstrap.queries`, leaf-record-compatible with existing classification logic) plus `lookups` (an object keyed by *dynamic*, runtime-determined resolver URLs — `dot://1.1.1.3/dns-query` etc. — each with its own `queries`/`tcp_connect`/`tls_handshakes`, arguably the actual point of the test: does DNS manipulation resistance differ across resolvers). BigQuery's `JSON_QUERY_ARRAY` can address `bootstrap` (a fixed path) but cannot enumerate `lookups`' arbitrary runtime keys. **`tor`** is a different kind of evidence entirely, not a path problem: `targets` is an object keyed by dynamic `ip:port` strings, each describing reachability of a *named Tor relay/directory-authority/bridge* (`target_name`, `target_protocol` values like `or_port_dirauth`/`obfs4`, not `dns`/`tcp`/`tls`/`http`), plus pre-aggregated `*_accessible`/`*_total` summary counters with no per-observation equivalent in the current schema. Tor's signal answers "is the circumvention tool itself reachable," a different question than "is protocol layer X blocked for app Y" — CLIO's current schema has no column for it at all. **Bootstrap subset fix, 2026-07-05:** confirmed live before changing anything — every `dnscheck` row (1,105,030 of them) has exactly one of `$.queries` / `$.bootstrap.queries` populated (100% at `$.bootstrap.queries`, 0% at `$.queries`, 0 rows with both, 0 with neither), and the shape at `$.bootstrap.queries` matches the existing extraction's expected fields (`hostname`, `query_type`, `engine`, `failure`, `answers[].{ipv4,ipv6,answer_type}`) exactly. `stg.ooni_dns_observations.sql`'s `UNNEST` now does `COALESCE(JSON_QUERY_ARRAY(m.raw_test_keys, '$.queries'), JSON_QUERY_ARRAY(m.raw_test_keys, '$.bootstrap.queries'))` — safe precisely because the two paths are mutually exclusive, confirmed rather than assumed. Verified live post-materialization: 2,854,405 `dnscheck` rows now flow through `stg.ooni_dns_observations` (previously 0); they collapse correctly to `test_family = 'dns'` in `features.protocol_daily_signals` with no code change needed there (as predicted); `dns_nxdomain_events` gained 6,240 real events from dnscheck's system-resolver failures, `dns_bogon_events` gained 0 (correct and expected — dnscheck's bootstrap queries trusted third-party resolvers like Quad9, which don't return bogon IPs the way a censoring in-country resolver might). **Deliberately not propagated further downstream in this pass:** `intelligence.protocol_signal_regimes` and every reporting mart built on it (including the dashboard's flagship `composite_pressure_score`) were left un-rematerialized — recovering ~1.1M previously-invisible DNS observations (the single largest test volume in the dataset) would shift those blended scores by an amount that deserves its own deliberate look before it reaches the flagship KPI, not a silent cascade as a side effect of a Tier 1 fix. Logged as a new open item, TD-54, below. | **(1) DONE.** **(2)** `dnscheck`'s `lookups` subset — the test's actual differentiator (per-resolver DNS censorship resistance), but requires a Python-side flattening step at ingestion (`Bruin/assets/ingest/ooni_raw.py`, converting the map into an array of `{resolver_url, ...}` structs before landing in BigQuery) plus a new resolver-identity column, and a semantic label distinguishing "reached the DNS resolver" from "reached the app target" so `tcp_connect`/`tls_handshakes` rows aren't silently pooled together meaning two different things. Tier 3, deliberately deferred. **Scoped and de-risked 2026-07-06 (design only, not implemented — a focused Tier 3 pass, possibly its own ADR, per the backlog-tiering policy; no code written).** Key correction to the approach above, established by direct investigation: **re-ingestion via `ooni_raw.py` is NOT required, and is in fact impossible in the current Codespace** (the local `data/dev/ooni/**/*.jsonl.gz` source files are absent — only the BigQuery side survives a fresh clone). But the `lookups` object is already fully preserved server-side: verified 100% of all 1,105,030 `dnscheck` rows in `civil_liberties_prod.ooni_measurements` carry both `$.lookups` and `$.bootstrap` in `raw_test_keys`. The reason pure BigQuery SQL can't reach it is only that JSONPath can't enumerate `lookups`' dynamic resolver-URL keys — and a **BigQuery JS UDF can** (`Object.keys(JSON.parse(raw_test_keys).lookups)`), so the whole recovery can happen at the staging layer from data already landed, exactly as TD-47's *bootstrap* subset was recovered with a pure-SQL `COALESCE` — no Python, no re-ingest, no local-data dependency. Prototyped live (20k-row sample) and confirmed the UDF cleanly enumerates the dynamic keys: ~40+ distinct resolvers per session across DoH (`https://9.9.9.9/dns-query`, `https://1.1.1.1/dns-query`, …) and DoT (`dot://8.8.8.8:853/`, `dot://149.112.112.11/dns-query`, …) forms spanning the Google/Cloudflare/Quad9 families. **Confirmed `lookups` shape (sampled live):** `lookups` is `{resolver_url → {agent, failure, failed_operation, network_events[], queries[], tcp_connect[], tls_handshakes[], requests[]}}` — each resolver's `queries`/`tcp_connect`/`tls_handshakes` are structurally identical to the measurex arrays the four existing staging observation assets already extract, so the leaf-record extraction logic is reusable as-is; the only genuinely new elements are (a) the `resolver_url` identity dimension and (b) the semantic label. **Recommended design for the implementation pass:** a new staging asset (e.g. `stg.ooni_dnscheck_resolver_observations`) whose SELECT uses a JS UDF to explode `lookups` into one row per (measurement, resolver_url, protocol-layer observation), carrying `resolver_url` and a `probe_target = 'DNS_RESOLVER'` label (vs the existing bootstrap/app path's implicit `APP_TARGET`), so downstream aggregations never conflate "this DoH/DoT resolver was blocked" with "the app's hostname failed to resolve" — the two-meanings-in-one-column hazard this row warned about. This subset is *the actual point of dnscheck* (per-resolver DNS-manipulation-resistance), distinct evidence from the bootstrap subset TD-47 already recovered, and worth its own before/after measurement against Finance Bill 2024 once built. **(3)** `tor` — needs a dedicated staging asset (one row per `target_name`/`target_protocol`, preserving the `accessible`/`total` summary as its own grain), not a patch to the existing four tables; also a genuine scope question, not just an engineering one — this is reachability-of-circumvention-infrastructure evidence, a different category from CLIO's current app-blocking mission, worth a deliberate decision rather than default inclusion. Tier 3, deliberately deferred. |
| TD-48 | `Bruin/assets/features/protocol_daily_signals.sql:56-63` | Medium. `test_name` (raw app identity — `telegram`, `whatsapp`, `signal`, `psiphon`) is read in but immediately collapsed via a `CASE` into a 3-bucket `test_family` (`messaging` for whatsapp/telegram/signal/facebook_messenger, `circumvention` for tor/psiphon, `dns` for dnscheck, `web` for web_connectivity — which never occurs in this data at all, see TD-47). The raw `test_name` column is never selected again — absent from the final `SELECT` (lines 255-337). From this point in the chain forward, Telegram, WhatsApp, and Signal are indistinguishable (`test_family = 'messaging'` for all three). | Decide whether per-app resolution needs to survive past this asset for any current reporting need (see TD-49/TD-51 for where it would need to land) before changing this file; don't add the column back speculatively. **RESOLVED, 2026-07-06, folded into TD-49 (commit `9ec0302`).** The two turned out to be the same piece of work: TD-49's family separation required `test_family` to survive to the reporting layer as a real dimension, and TD-48's question ("does the family distinction need to be surfaced on the dashboard") is answered by the same change — `test_family`-driven `state_driving_family` is now a column in `reporting.mart_protocol_interference_trends`, read by `get_protocol_regimes()`, and displayed on page 2 with a caption explaining `dns` (dedicated dnscheck resolver testing) vs `messaging`/`circumvention` (DNS/TCP/TLS/HTTP checks embedded in app tests). The finer `test_name` per-app split (Telegram vs WhatsApp vs Signal) is *not* restored — TD-51's per-app panel already serves that need from `fact_protocol_blocking_summary`, which retains full `test_name`; adding it back here too would be the speculative duplication this row warned against. See TD-49's resolution for full detail. |
| TD-49 | `Bruin/assets/reporting/protocol_interference_trends_mart.sql:75,120`; `protocol_repression_correlation_mart.sql`'s internal `protocol_relationships` CTE; `asn_behavior_profile_mart.sql` (`GROUP BY asn, protocol` / `GROUP BY asn`) | Medium. None of these three reporting-layer assets — the ones that actually populate Streamlit — group by `test_family` at all, only by `protocol` (dns/tcp/tls/http). So even the coarse app-family distinction that survives as far as `features.protocol_daily_signals` and `intelligence.protocol_signal_regimes` is dropped a second time before reaching any dashboard page. Confirmed via `grep`: `test_name` and `test_family` appear zero times anywhere in `streamlit/`. | If app-level (or app-family-level) attribution is judged worth surfacing, this is one of the two places it would need to be added back (the other being TD-51's already-built-but-unused mart). **Scope expanded 2026-07-05, inherited from TD-54's resolution (see TD-54 below for the measurements behind this) — this is now a hard requirement of TD-49's implementation, not an optional nicety:** when `test_family` is restored at the reporting layer, the observation-count share computations for `protocol = 'dns'` (`severe_obs_share`/`elevated_obs_share`/`insufficient_obs_share` in `protocol_interference_trends_mart.sql`'s `regime_agg`/`regime_daily` CTEs, lines 92-159 — note these CTEs live in the *reporting mart*, not in `intelligence.protocol_signal_regimes` as an earlier relay of this finding misplaced them) must keep `test_family = 'dns'` (dnscheck's dedicated ~2.85M-observation volume, recovered by TD-47's bootstrap fix) **separate** from `test_family = 'messaging'`/`'circumvention'` (the DNS-layer checks embedded inside app-specific tests) — never pooled into a single share denominator. Pooling them dilutes real app-blocking shares below the fixed rollup thresholds (severe ≥0.01, elevated ≥0.02): a simulation against the Finance Bill 2024 window showed two secondary ELEVATED days (2024-06-20, 2024-06-27) flip to NORMAL and the 06-23 severe margin thin from 0.041 to 0.016 under pooling, purely from dnscheck's volume asymmetry (its `signal_rate` is structurally 0 today — see TD-55), not from any real change in censorship pressure. Until TD-49 ships with this separation, `intelligence.protocol_signal_regimes` and the reporting layer must **not** be rematerialized from the post-TD-47 `features.protocol_daily_signals` — that standing hold is TD-54's resolution. **RESOLVED, 2026-07-06 (commit `9ec0302`, code; this pass, docs + cascade completion). Also closes TD-48** (the two turned out to be one piece of work — surfacing `test_family` at the reporting layer *is* the family-separation mechanism). **Confirmed location before fixing:** the pooled share denominators live in `protocol_interference_trends_mart.sql`'s regime rollup, not in `intelligence.protocol_signal_regimes` (which already carries `test_family` in its grain and output). **Fix (mart v5→v6):** the old `regime_agg`/`regime_daily` CTEs are replaced by a three-stage rollup — `regime_family_agg` (aggregates `GROUP BY measurement_date, protocol, test_family`, so each family's shares are of its own volume only), `regime_family` (per-family shares + confidence), and `regime_daily_agg` (rolls families up to date×protocol with the invariant `share ≥ threshold ⇔ state fires` preserved: **MAX** across families for severe/elevated/below-baseline so a small loud family can't be masked by a large quiet one, **MIN** for insufficient so INSUFFICIENT_DATA fires only when *every* family is majority-insufficient). A new `state_driving_family` column names which family triggered the state, and `confidence_level`/`regime_confidence` now come from that driving family rather than a pooled blend. `state_driving_family` is surfaced through `streamlit/services/marts.py`'s `get_protocol_regimes()` and displayed on page 2 (Protocol Regime Monitor) with an explanatory caption (this is TD-48's "make the family distinction real on the dashboard"). The continuous descriptive `feature_daily` rates (signal_rate etc.) remain deliberately observation-weighted across families — they are volume-honest descriptions, not threshold classifications. **Verified live against Finance Bill 2024:** all six ELEVATED/SEVERE days match TD-54's *un-pooled* baseline exactly (06-20 elev 0.052, 06-22 sev 0.102, 06-23 sev 0.041, 06-27 elev 0.049, 07-02 sev 0.456, 07-05 sev 0.556), every one driven by `messaging` — the two days TD-54's simulation showed flipping to NORMAL under pooling (06-20, 06-27) are preserved, and 06-23's severe margin is back to its full 0.041 (not the pooled 0.016). `bruin validate` clean (60/60). |
| TD-50 | `Bruin/assets/intelligence/protocol_signal_regimes.sql:156-225` vs. `Bruin/assets/features/protocol_daily_signals.sql:135-142` | Medium. `protocol_daily_signals.sql` computes explicit per-cause count columns (`dns_bogon_events`, `dns_nxdomain_events`, `tcp_reset_events`, `tcp_timeout_events`, `tls_reset_events`, `tls_handshake_failure_events`, `http_451_events`, `http_error_events`) — real cause-level attribution (not just "which layer," but "which specific failure within that layer"). None of these columns appear anywhere in `protocol_signal_regimes.sql`'s `SELECT`; only the coarse `protocol` label and a single blended `protocol_stress_score`/`anomaly_score` survive past this point. This is the exact spot where "why specifically" (DNS NXDOMAIN vs. bogon, TCP reset vs. timeout, etc.) becomes permanently unrecoverable downstream — a dashboard viewer can currently learn "TLS had elevated stress this day," never "specifically because of TLS resets." | Decide whether cause-level detail is worth carrying into `intelligence`/`reporting` for either persona (journalist, NGO/legal) before adding it back — this is a bigger lift than TD-48/49 since it means widening an intelligence-layer asset's grain, not just a reporting mart's `GROUP BY`. |
| TD-51 | `Bruin/assets/marts/facts/fact_protocol_blocking_summary.sql` (renamed from `fact_platform_blocking_summary.sql`, see TD-21 below) | **RESOLVED, 2026-07-05.** This mart already carried full, uncoarsened `test_name` and `protocol` in its `GROUP BY`, at monthly grain, but had zero consumers anywhere. **Fix:** added `get_protocol_blocking_summary()` to `streamlit/services/marts.py` (new `MARTS` dataset constant added to `core/constants.py`, following the existing `REPORTING` pattern) and wired it into a new "Per-App, Per-Protocol-Layer Blocking Breakdown" panel on `streamlit/pages/3_Protocol__Stress_Intelligence_Observatory.py` — grouped bar chart (app × protocol, `blocking_signal_count`) plus a detail table including TD-52's four fixed layer columns. Chosen over page 2 because page 2 (Protocol Regime Monitor) and page 3 were already flagged as near-duplicates (TD-16); adding real per-app content to page 3 gives it a genuine reason to exist distinct from page 2, rather than adding a third near-identical page. Verified via headless-Chromium render: five `test_name` values now visible (`dnscheck`, `signal`, `telegram`, `whatsapp`, `psiphon` — `dnscheck` appearing here specifically because TD-47's bootstrap fix landed in the same pass), real nonzero bars matching TD-52's fixed counts, zero tracebacks. | Closed. |
| TD-52 | `Bruin/assets/marts/facts/fact_protocol_blocking_summary.sql` (renamed, see TD-21) | **RESOLVED, 2026-07-05.** Confirmed before fixing: `blocking_detail` is never a bare protocol name for any of the four layers — every branch in `int.ooni_experiment_results.sql` emits a compound `layer.cause` string, and critically, **non-blocking outcomes share the same field** (`dns.ok`, `tcp.ok`, `tls.ok`, `http.ok` all appear live, e.g. `dns.ok` alone accounts for 3.6M+ rows). So a prefix match alone (`STARTS_WITH(blocking_detail, 'dns.')`) would count every observation of that layer, not just blocked ones — the fix needed `AND is_blocking_signal` too. **Fix:** `COUNTIF(STARTS_WITH(blocking_detail, 'dns.') AND is_blocking_signal)` (and same for tcp/tls/http). **Relationship to `blocking_signal_count`, verified live:** since `blocking_detail`'s layer prefix always matches the row's own `protocol` column (confirmed 100% live, all 4 protocols), and this mart's own `GROUP BY` already includes `protocol`, exactly one of the four columns is nonzero per row, and it equals that row's `blocking_signal_count` exactly — the four columns sum to `blocking_signal_count` bit-for-bit, not just "sensibly." Verified against live materialized data post-fix: `SUM(dns_blocking_events)=50,236`, `SUM(tls_blocking_events)=361`, `SUM(tcp_blocking_events)=SUM(http_blocking_events)=0` (genuinely zero — no TCP-reset or HTTP-451 blocking signals exist in Kenya's current OONI data), summing to the full `50,597` `blocking_signal_count` total, previously all zero. | Closed. |

## TD-53 — discovered 2026-07-05 during TD-21's resolution; RESOLVED 2026-07-06 (cost-audit cleanup pass)

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-53 | BigQuery table `marts.fact_platform_blocking_summary` (163 rows) | Low. An orphaned, stale BigQuery table discovered via `bq ls`/`bq show` while confirming TD-21's rename direction: a table literally named `fact_platform_blocking_summary` already existed in `marts`, separate from and older than the live `fact_protocol_blocking_summary` (163 rows vs. 375, older `creationTime`) — a leftover from before an earlier commit changed this asset's `name:` field from "platform" to "protocol" without ever dropping the table the old field name had produced. Nothing produces or reads it today; it is pure orphaned state, not referenced by any asset or by the rename in this pass (which renamed the *file* to match the already-current "protocol" field, deliberately not touching this table). | **RESOLVED, 2026-07-06.** External-consumer check performed before dropping, per this item's own caution: `bq ls --transfer_config` (both `us` and `us-central1`) returned no scheduled queries, and an `INFORMATION_SCHEMA.TABLES WHERE table_type='VIEW'` sweep across all six datasets found only `intelligence.country_readiness` (confirmed by grep not to reference this table). A repo-wide grep found zero references in `Bruin/` or `streamlit/`. Dropped via `bq rm -f -t encoded-joy-485413-k5:marts.fact_platform_blocking_summary`. The live `marts.fact_protocol_blocking_summary` (375 rows, the real asset) is untouched. |

## TD-54 — discovered 2026-07-05 during TD-47's bootstrap-subset fix; hold recorded 2026-07-05, cascade completed safely 2026-07-06 once TD-49 shipped

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-54 | `intelligence.protocol_signal_regimes` and every reporting mart built on it (`reporting.mart_protocol_interference_trends`, `reporting.protocol_repression_correlation_mart`, `reporting.mart_political_stress_windows` — the last of which serves the dashboard's flagship `composite_pressure_score`) | Low (deliberate, not a bug). TD-47's bootstrap fix recovered 2,854,405 previously-invisible `dnscheck` DNS observations — the single largest test-volume gain of any fix in this project's history — into `features.protocol_daily_signals`. That asset was rematerialized as part of verifying TD-47; `intelligence.protocol_signal_regimes` and everything downstream of it were deliberately **not** rematerialized in the same pass, so they still reflect the pre-fix, DNS-family-starved blended stress scores. This is a real, if temporary, definition/materialized-state divergence — the same category of risk this project has flagged before (TD-38, TD-40) — but here it's intentional. **Investigated 2026-07-05 (simulation against live data, no table written), before deciding whether to cascade.** Two structurally different aggregation mechanics were found in the downstream chain: `protocol_stress_score` is `MAX(protocol_stress_score)` across test_family×ASN cells (`protocol_interference_trends_mart.sql:117`) — immune to volume dilution, and confirmed **bit-for-bit identical** before/after on every Finance Bill 2024 date tested; but `protocol_state`/`trend_state`/`confidence_level` derive from raw observation-count shares (`severe_obs_share`/`elevated_obs_share` = qualifying observations ÷ total observations, `regime_agg`/`regime_daily` CTEs, `protocol_interference_trends_mart.sql:92-159`) against fixed thresholds (severe ≥0.01, elevated ≥0.02) — **count-weighted, not rate-normalized**, and directly dilutable. Simulated cascade against the Finance Bill 2024 window: the flagship CRISIS days all hold (2024-06-22, Parliament stormed: SEVERE_ELEVATION retained, severe_share 0.102→0.038; 07-02: 0.456→0.158; 07-05: 0.556→0.214), but two secondary ELEVATED days flip to NORMAL purely from dilution (06-20: elevated_share 0.052→0.019; 06-27: 0.049→0.020, both against the 0.02 threshold), and **06-23's severe margin thins from 0.041 to 0.016** — still above the 0.01 threshold today, but with ~2.6× less headroom; a future incident with a slightly different data mix could plausibly lose a flagship day to the same mechanic. The dilution is one-directional (dnscheck's `signal_rate` is structurally 0 — see TD-55 — so it can only shrink shares, never inflate a false alarm), which is why the damage is confined to threshold-crossing downgrades. | **RESOLVED by decision, 2026-07-05: do not cascade.** `intelligence.protocol_signal_regimes` and the reporting layer stay un-rematerialized from the post-TD-47 features state until TD-49 ships with its newly-expanded hard requirement (see TD-49's row): share computations for `protocol = 'dns'` must keep `test_family = 'dns'` separate from `'messaging'`/`'circumvention'`, never pooled into one denominator. The dilution risk to secondary and flagship-adjacent classifications was judged a real regression risk even though the headline `protocol_stress_score` metric is provably safe. TD-54 is closed **by reference to TD-49's expanded scope**, not left open as an independent item — the standing "do not rematerialize this chain" hold is recorded in TD-49 and enforced by the materialization-staleness allowlist (see ADR-0005, if/when landed). The separate dnscheck classification gap surfaced by this investigation is logged as TD-55. **CASCADE COMPLETED SAFELY, 2026-07-06 (commit `9ec0302`, materialization + allowlist prune; this pass, ordering fix + re-verification).** Once TD-49's family separation shipped, `intelligence.protocol_signal_regimes` and the reporting layer were rematerialized from the post-TD-47 `features.protocol_daily_signals` state, and the four TD-54 hold entries were pruned from `staleness_allowlist.json` in the same commit (per ADR-0005's hygiene rule). The dilution risk this item identified is now **provably gone, not assumed**: the Finance Bill 2024 window was re-measured live against the materialized v6 mart and every ELEVATED/SEVERE day holds at its full un-pooled share (the exact "before pooling" values from this row's simulation: 06-20 0.052, 06-22 0.102, 06-23 0.041, 06-27 0.049, 07-02 0.456, 07-05 0.556), with the two days this simulation predicted would flip to NORMAL (06-20, 06-27) preserved. **One follow-up caught and fixed this pass:** the earlier cascade rematerialized `intelligence.protocol_relationships` *after* its two downstream consumers (`reporting.asn_behavior_profile_mart`, `reporting.protocol_repression_correlation_mart`), leaving two new stale edges that failed the ADR-0005 daily check. Both marts were rematerialized in DAG order to clear them (verified safe first — including vs excluding dnscheck's `dns` family changes no ASN's `censorship_intensity_tier` in `asn_behavior_profile_mart`, 0 flips across all ASNs; and `protocol_repression_correlation_mart` derives its state from the already-family-separated v6 `trend_state`); the staleness check now passes with 0 new violations. |

## TD-55 — discovered 2026-07-05 during TD-54's dilution investigation; RESOLVED 2026-07-06

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-55 | `int.ooni_experiment_results.sql:54-72` (the DNS branch's `result_state`/`blocking_detail`/`confidence_score` CASEs) as applied to `dnscheck` rows; `features.protocol_daily_signals.sql:135` (`COUNTIF(blocking_detail = 'dns.bogon')`) | Medium. `dnscheck`'s `signal_rate`/BLOCKED classification is **structurally always 0** regardless of what the underlying data says — confirmed live: 100% of the 2,854,405 `dnscheck` rows in `int.ooni_experiment_results` carry `is_blocking_signal = false`, so `test_family = 'dns'` contributes zero blocked events on every date in `features.protocol_daily_signals`. This is separate from and independent of TD-54's dilution question (which was about pooling volumes across test families; this is about whether dnscheck's own rows can ever register a signal at all). **Open question for whoever investigates — deliberately not answered here:** is a near-zero rate *expected and correct* because the bootstrap subset's probes resolve the hostnames of inherently well-behaved third-party resolvers (e.g. `dns10.quad9.net`, observed in live samples), or is there an actual gap in how the classification CASEs handle dnscheck's failure vocabulary? One data point already in hand suggests at least part of it is a real gap: 9,046 live rows carry `blocking_detail = 'dns.dns_bogon_error'` — OONI's probe *itself* reporting a bogon detection as the failure string — yet all 9,046 classify as `result_state = 'UNKNOWN'` (confidence 0.40), because the DNS branch's BLOCKED test only applies the bogon regex to the `answer` field (`int.ooni_experiment_results.sql:55`), never to the failure string; and `features.protocol_daily_signals.sql:135` counts only the literal `'dns.bogon'`, which `'dns.dns_bogon_error'` doesn't match, so these rows also never reach `dns_bogon_events`. Whether those 9,046 probe-reported bogons represent real censorship evidence (vs. probe-side artifacts) is exactly the determination the investigation needs to make — not assumed either way here. | Investigate as its own item, not folded into TD-49/TD-54: (1) sample the `dns.dns_bogon_error` rows' raw measurements to determine what the probe actually observed; (2) decide whether dnscheck's failure vocabulary (`dns_bogon_error`, and audit for others) should map to `BLOCKED`/`is_blocking_signal` in `int.ooni_experiment_results.sql`'s DNS branch; (3) if so, this changes `signal_rate` for `test_family = 'dns'` from structural-zero to a real quantity — which interacts with TD-49's share-separation requirement and must be measured against the Finance Bill 2024 window the same way TD-54's simulation was, before materializing. **RESOLVED, 2026-07-06 (commit `9ec0302`, code; this pass, docs + downstream cascade verification).** Sampled the 9,046 `dns.dns_bogon_error` rows in the live raw layer: they are dnscheck **bootstrap** lookups of public DoH/DoT resolver hostnames (Quad9/Cloudflare-family), where the OONI probe engine *itself* detected bogon answers, discarded them, and reported only `dns_bogon_error` as the failure string — so `answers` is null on every such row and the DNS branch's answer-field bogon regex (`int.ooni_experiment_results.sql:55`) can structurally never fire on them. This is a **real gap, not expected-and-correct**: the signal is genuine interference, not a probe artifact — all 4,523/4,523 measurements in the affected sessions bogoned wholesale (zero mixed sessions) while messaging apps' DNS on the *same network-days* resolved normally (e.g. WhatsApp 24,553 OK vs 2 DOWN), the selective anti-encrypted-DNS-resolver enforcement signature dnscheck exists to detect. **Fix:** added a `LOWER(dns_failure) = 'dns_bogon_error' → BLOCKED` arm to the DNS branch's `result_state`/`blocking_detail`/`confidence_score` CASEs, kept as a distinct `blocking_detail = 'dns.bogon_probe_reported'` (vs the regex path's `'dns.bogon'`) to preserve verdict provenance, at the same 0.90 confidence as the regex path; `features.protocol_daily_signals.sql:135` now `COUNTIF(blocking_detail IN ('dns.bogon', 'dns.bogon_probe_reported'))`. **Result:** `test_family = 'dns'` is no longer structural-zero — it now carries 9,046 blocked events and `AVG(confidence_weighted_interference) = 0.0116` (comparable to messaging-dns's 0.0149), which materially changes the TD-54 premise (dnscheck is no longer a near-zero diluter; see TD-49/TD-54 rows for how this interacts with the family separation). **Re-measured against Finance Bill 2024, post-cascade:** every ELEVATED/SEVERE day in the window (06-20, 06-22, 06-23, 06-27, 07-02, 07-05) is driven by `test_family = 'messaging'` (the real app-DNS censorship signal) at its full undiluted share — dnscheck's new bogon signal sits in its own separated denominator and creates no false Finance Bill alarm. |

## TD-56 — discovered 2026-07-05 by the materialization-staleness check's first run (ADR-0005); RESOLVED 2026-07-06 (cost-audit cleanup pass)

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-56 | Seven downstream assets with live tables built from upstream state that no longer exists: `int.ooni_signals` (built 2026-07-05 10:44, upstream `int.ooni_experiment_results` rebuilt later that day in the Tier 1 cascade), `marts.dim_platforms` (built 2026-05-09), `marts.dim_reasons` (2026-05-09, stale against **two** upstreams), `marts.dim_regions` (2026-04-18), `marts.dim_requestors` (2026-05-09), `marts.fact_asn_repression_index` (2026-05-11), `marts.fact_network_blocking_daily` (2026-05-09) | Medium. The first run of ADR-0005's staleness check (2026-07-05) surfaced these seven previously-unknown stale edges — pre-existing silent staleness, not caused by any recent pass, invisible until the check existed. Their upstreams (`stg.lumen_requests`, `stg.acled_conflict_events`, `marts.fact_ooni_censorship_signals`, `marts.fact_country_pressure_daily`, `int.ooni_experiment_results`) have been reworked repeatedly since these tables were last built (TD-40's fix, ADR-0004's reweighting, the Tier 1 OONI cascade), so what these tables serve today reflects a warehouse state up to ~11 weeks gone. Two cautions were on record — **both checked directly, and one was found wrong** (a 9th instance of this project's verify-before-acting discipline): (1) `marts.dim_regions` predates commit `6dbe7ab`'s schema rename, a possible third TD-40/TD-41 casualty — **confirmed moot**, since the disposition below is retire, not rebuild, so its column compatibility never had to be resolved. (2) `marts.fact_asn_repression_index` was said to "feed dashboard pages 5/7 via `asn_behavior_profile_mart`" — **this is factually wrong.** Direct reading of `asn_behavior_profile_mart.sql`'s `depends:`/`FROM` shows it reads `features.protocol_daily_signals`, `intelligence.protocol_relationships`, and `marts.dim_asn` — **not** `fact_asn_repression_index`. A repo-wide grep confirms nothing (no asset, no Streamlit page) reads `fact_asn_repression_index` at all; it is a dead-end, not a dashboard input. | **RESOLVED, 2026-07-06 — all seven RETIRED (delete asset + drop table + prune allowlist), because direct consumer-tracing found every one has zero live consumers.** That is the finding: the check surfaced seven stale tables, and all seven were stale *precisely because nothing depends on them anymore*. Per-asset evidence: **(1) `int.ooni_signals`** — only consumer was `dim_country` (its `country` column), and `dim_country` is itself unconsumed; TD-05 already deleted its dead bucketing. Retired; `dim_country` repointed to `int.ooni_experiment_results` (identical country values, live+fresh) and rematerialized so it is no longer stale. **(2) `marts.fact_asn_repression_index`** — zero consumers (the "feeds pages 5/7" claim disproven at left). Retired. **(3) `marts.fact_network_blocking_daily`** — only consumer was `fact_asn_repression_index` (now retired); the whole `fact_network_blocking_daily → fact_asn_repression_index` chain is dead. Retired. **(4–7) `dim_platforms` / `dim_reasons` / `dim_regions` / `dim_requestors`** — zero external references anywhere (grep across `Bruin/assets` + `streamlit/`); three Lumen-fed (Lumen benched, ADR-0004), one ACLED-fed; the reporting layer bypasses the star schema entirely. Retired per the project owner's explicit steer (retire all four) and the established delete-dead-code pattern (TD-17/18/32/41). **Execution:** external-consumer safety confirmed first (no scheduled queries via `bq ls --transfer_config` in `us`/`us-central1`; only view is `intelligence.country_readiness`, referencing none of these); seven asset files `git rm`'d; seven tables dropped via `bq rm`; `bruin validate` clean (60→53 assets); `dim_country` rebuilt via `bruin run`; all eight allowlist entries pruned (`staleness_allowlist.json` now `[]`); staleness check re-run PASS (59 edges, 0 allowlisted, 0 new, 0 orphaned-allowlist warnings). Two further unconsumed dims remain out of scope — see the cost-audit section (TD-59). |

## TD-57 — new, logged 2026-07-06 as a deliberate trade-off in ADR-0006's pressure-attribution build

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-57 | `reporting/mart_pressure_attribution_daily.sql` (hardcoded 0.75/0.25 weights, duplicating `marts/facts/fact_country_pressure_daily.sql`'s composite formula); the Google periodization CASE/LEAD logic, now in three sites (`int.google_pressure_periodized.sql`, `mart_pressure_attribution_daily.sql`, `mart_pressure_attribution_platform_drivers.sql`); the `events + 3*fatalities` intensity-mass formula in `mart_pressure_attribution_conflict_drivers.sql`, duplicating the conflict score's LOG argument | Low (deliberate, mitigated, tracked rather than hidden). ADR-0006's attribution marts must restate three pieces of upstream arithmetic to decompose them, creating keep-in-sync liabilities: if ADR-0004's weights, the periodization, or the fatality weighting ever change upstream without the attribution assets changing in the same commit, the decomposition silently attributes a formula that no longer exists. Mitigations already in place: every site carries a keep-in-sync comment naming its counterparts, and the weights duplication has a **live alarm** — `attribution_residual` in `mart_pressure_attribution_daily` (composite minus the sum of contributions) is rounding-level (≤0.0001 verified) when the weights agree and becomes visibly nonzero the moment they drift. The periodization and intensity-mass duplications have comments but no equivalent arithmetic tripwire. | Accept until a real second change to any of these formulas occurs (the project's own two-concrete-cases bar for abstraction). If the composite formula or periodization is next modified, either update all named sites in the same commit (the standing discipline) or promote the shared logic into one upstream asset then. ~~A future custom check could assert `MAX(ABS(attribution_residual)) < 0.001` at materialization time instead of leaving the alarm display-side.~~ **Done, 2026-07-06 (guardrails pass):** `mart_pressure_attribution_daily.sql` now carries a Bruin `custom_checks` block (`attribution_residual_within_rounding`) asserting `COUNTIF(ABS(attribution_residual) >= 0.001) = 0` as part of the asset's own materialization — verified live (5 quality checks pass on `bruin run`; current `MAX(ABS(attribution_residual))` = 0.0001 across 761 rows). Weight drift now fails the pipeline run instead of waiting for a dashboard viewer. The periodization and intensity-mass duplications still have comments but no arithmetic tripwire — that part of this item stays open. |

## Cost audit — GCP/BigQuery, performed 2026-07-06

First measured cost pass since ADR-0006's four attribution marts, the protocol-regime mart rewrite, and the daily staleness job shipped. Numbers are **real**, from `region-us-central1.INFORMATION_SCHEMA.JOBS_BY_PROJECT` (180-day retention) and `__TABLES__`, not estimates. Pricing model confirmed **on-demand** (every job carries non-zero `total_bytes_billed`, `reservation_id` = null; us-central1 on-demand = **$6.25/TiB**). All eight datasets are in `us-central1`.

**Headline: absolute spend is tiny — this is a pilot-scale dev project.** Monthly bytes-billed cost: Apr $0.07, May $4.23, Jun $0.04, Jul $2.47 (180-day total ≈ **$6.8**). Warehouse storage ≈ **$1.4/mo** (the `stg` dataset is 64 GiB, 61.5 of it the raw `stg.ooni_measurements` JSON; everything else is sub-3-GiB; the `civil_liberties_prod`/`civil_liberties_staging` raw tables are 0-byte external tables — no duplication waste; `features._dlt_*` bookkeeping is 0 bytes). **The value of this audit is structural, not the current dollar figure** — it identifies where cost *would* scale with data volume, multi-country expansion, or an actually-running `@daily` schedule.

**Ranked cost drivers (90-day bytes-billed, by cost-impact):**

1. **The OONI staging chain — dominant and structural (~$3.2 / 90d, and the only driver that scales badly).** The four observation-extraction assets each re-scan the full ~30 GiB `raw_test_keys` JSON column of `stg.ooni_measurements` on every run, because all are `create+replace` with no incremental filter: `stg.ooni_dns_observations` 181 GiB / 6 runs, `stg.ooni_tcp_observations` 91 / 3, `stg.ooni_http_observations` 60 / 2, `stg.ooni_tls_observations` 60 / 2 (≈30 GiB each every rebuild), plus `stg.ooni_measurements` itself 125 GiB / 11. At the current low rebuild cadence this is ~$3/quarter — but the pipeline's declared schedule is `@daily`; **if that schedule ever actually materializes this chain daily, it is ~4×30 GiB×30 = 3.6 TiB/mo ≈ $22/mo, growing linearly with OONI volume.** This is the single highest-leverage lever. Logged as **TD-58**.

2. **Ad-hoc diagnostic full-scans of `stg.ooni_measurements` (~$1.8 in July alone).** The 61 GiB raw table is unpartitioned and unclustered, so *every* query touching `raw_test_keys` scans the whole column — even `SELECT raw_test_keys ... LIMIT 3` billed 30 GiB. The ~9 largest July SELECTs (30–61 GiB each) were the human OONI-thread investigation queries (TD-47 `lookups` prototyping, `dnscheck` shape checks). Not pipeline cost, but enabled by the missing clustering. Mitigation is part of TD-58 (cluster by `test_name`) plus scan discipline (`--maximum_bytes_billed`, column projection, `TABLESAMPLE`).

3. **The ACLED regime backfill — one-time, but by far the most slot-intensive operation (~31 slot-hours).** 1,547 per-week `MERGE` jobs into `intelligence.acled_pressure_regimes` + `country_initialization_status` (30 GiB billed, but **111,668 + 12,786 slot-seconds**), plus ~12,000 tiny per-week helper SELECTs from the backfill script (these dominate July's 15,578-job count — the count is a quota/concurrency consideration, not a cost one, since they bill almost nothing). The completed backfill is not recurring; steady-state is one week per run via `run_acled_regime_week.py` (TD-38), negligible. **The standing lesson: never casually re-run a full backfill** — it is the most expensive single action available in this repo, in slot-time terms.

4. **Everything else is cents.** All reporting marts, dims, features, and the non-backfill intelligence assets are sub-GiB per materialization. `intelligence.acled_pressure_regimes` steady-state, `features.protocol_daily_signals` (22 GiB / 16 runs — the OONI-derived one, mid-size), and the attribution marts are all immaterial.

**Streamlit dashboard cost is negligible and needs no change.** Every `services/marts.py` query reads a small reporting/marts table (all ≤ a few MiB — `mart_political_stress_windows` 761 rows, etc.) and every one filters `WHERE date_key/measurement_date BETWEEN …`; `run_query` is wrapped in `@st.cache_data(ttl=3600)`. Observed dashboard/SELECT scans against reporting tables bill 0.00 GiB. The date filters would benefit from partitioning *if* these tables were large, but they aren't — so partitioning the reporting layer is not worth the complexity today (noted, not recommended).

**Partitioning/clustering inventory:** **zero tables in the pipeline are partitioned or clustered.** For the tiny reporting/marts/dims tables this is correct (partition metadata overhead would exceed the benefit). It matters only for the OONI chain — see TD-58.

**Materialization-strategy inventory:** 46 of 47 SQL assets use `create+replace`; only `intelligence.acled_pressure_regimes` uses `merge` (it must — the stateful regime engine). For every small table, `create+replace` is the right, simplest choice. The one genuine strategy/size mismatch is the OONI observation chain (large source, append-mostly data, full recompute every run) — TD-58.

## TD-58 — new, discovered 2026-07-06 by the cost audit

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-58 | `stg.ooni_measurements` (61.5 GiB, unpartitioned/unclustered) and the four `create+replace` observation assets that re-scan its `raw_test_keys` column in full (`stg.ooni_dns_observations`, `stg.ooni_tcp_observations`, `stg.ooni_tls_observations`, `stg.ooni_http_observations`) | Medium. The OONI staging chain is the dominant and only badly-scaling cost driver (see cost-audit section): ~30 GiB re-scanned per observation-asset rebuild, ~$22/mo if the `@daily` schedule ever materializes it, growing linearly with OONI volume; and every ad-hoc query against the raw table scans all 61 GiB for lack of clustering (even `LIMIT 3` billed 30 GiB). Two independent levers, different cost/effort profiles. | **RESOLVED (clustering lever), 2026-07-06 (guardrails pass); incremental lever deliberately deferred by decision, with its trigger condition recorded.** All five assets now declare `partition_by: measurement_date` + `cluster_by: [country, test_name]` in their Bruin materialization blocks (`country` is constant at n=1 today but becomes the first-order pruning key under multi-country expansion, at zero extra cost now). BigQuery cannot change a partition spec via `CREATE OR REPLACE`, so the five tables were dropped and rebuilt via one `bruin run --downstream` over the whole OONI chain in DAG order (20 assets, 39 quality checks, 3m39s) — row counts verified byte-identical to pre-drop captures (1,354,848 / 6,583,255 / 2,682,758 / 422,487 / 1,074,908), staleness check PASS with 0 new violations, all golden-file tests re-passed live. **Measured effect:** the `test_name='tor'` diagnostic query shape that previously billed ~30 GiB now bills 5.5 GiB (cluster pruning); a single-date `raw_test_keys` scan bills 0.154 GiB instead of ~30 (partition pruning, ~200×). **Incremental conversion NOT done, per this row's own verify-first condition:** confirmed directly that no GitHub workflow ever executes `bruin run` (all four workflows are checks/lint/auth only) — the declared `@daily` schedule has no executor, so the ~$22/mo scenario is not live. Convert the observation assets to a partition-append strategy **only when a real schedule is wired** (and verify append-only-ness at the `measurement_date` grain at that point); until then `create+replace` at the current low rebuild cadence is the honest, simplest choice. See also the materialization-strategy audit section below. |

## TD-59 — new, discovered 2026-07-06 by the cost audit's orphan sweep

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-59 | `marts.dim_blocking_signals` (0 consumers); `marts.dim_country` (0 consumers, but deliberately kept); `features._dlt_*` bookkeeping tables | Low. TD-56's retire pass cleared the seven *stale* unconsumed assets; the orphan sweep that supported it found two more unconsumed dimension tables that were simply not stale (so the staleness check never surfaced them), plus a minor clutter finding. `marts.dim_blocking_signals` has zero external references (grep across `Bruin/assets` + `streamlit/`) — same category as the four dims retired under TD-56, just not stale. `marts.dim_country` also has zero consumers, but was **deliberately kept and repointed** (off the retired `int.ooni_signals` to `int.ooni_experiment_results`) as the canonical country-normalization dimension — the single most reusable dim for the multi-country future (TD-09 externalization, roadmap Step 4). The `features._dlt_*` tables (dlt pipeline bookkeeping) sit oddly in the `features` analytical dataset rather than a dedicated metadata dataset; 0 bytes, harmless. | Non-urgent. When next touching the marts layer, decide `dim_blocking_signals`: retire it (consistent with the TD-56 four) unless a near-term consumer is planned. Leave `dim_country` in place (its retention is a deliberate scaffolding decision, documented here so a future "retire all unconsumed dims" sweep doesn't remove it without cause). Optionally relocate the `_dlt_*` bookkeeping out of `features`. No cost impact either way (all sub-KiB). |

## TD-60 — new, discovered 2026-07-06 during the multi-country scaffolding pass

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-60 | `Bruin/assets/ingest/ooni_raw.py:49` (`PROBE_CC = "KE"`); trivially also `Bruin/assets/ingest/lumen_raw.py:39` (Kenya in a synthetic-data country list) | Low. The Python ingestion layer hardcodes Kenya where the SQL layer no longer does. Checked deliberately as part of the multi-country pass (it was the one unverified location): OONI's ingest filters to `PROBE_CC = "KE"` at the API-pull level, while ACLED and Google Transparency ingest all countries and filter in (now-templated) SQL, and Lumen is synthetic/benched. The new country-literal CI check covers SQL assets only and cannot reach this. Not fixed now for a stated reason: re-ingestion is currently impossible in this environment (the local `data/dev/ooni/**/*.jsonl.gz` source files don't survive a fresh clone — same finding as TD-47's `lookups` scoping), so a change here would be untestable, and Bruin's variable-injection mechanism for Python assets hasn't been verified. | Fold into the first pass that actually touches OONI ingestion (most likely TD-47's `lookups` build or a second country's onboarding): make `PROBE_CC` environment/variable-driven and verify against a real ingest run. Until then this is the documented second location of the country-hardcoding problem, deliberately out of the CI check's scope. |

## TD-61 — new, confirmed 2026-07-06 during the Streamlit page-structure review

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-61 | `streamlit/pages/4_Protocol_Repression_Correlation_Engine.py.py` (filename) | **RESOLVED, 2026-07-12.** Renamed to `protocol_repression_correlation_engine.py` (no numeric prefix, per the `st.navigation` migration — checked first for any code referencing the old filename; only docs did, none broke). | Closed. |

## TD-62 — new, noted 2026-07-09 during the post-rename `staleness-check.yml` verification run

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-62 | `.github/workflows/*.yml` — `actions/checkout@v4`, `actions/setup-python@v5`, `google-github-actions/auth@v2` | Very low. The manual `staleness-check.yml` run that confirmed TD-13's WIF fix (see TD-13's row) surfaced a platform-level deprecation notice, not a failure: these three actions target Node.js 20, and GitHub's runners are auto-forcing them onto Node.js 24. Unrelated to the rename or the WIF fix — a general action-version-lag notice any workflow using these actions would see. | Non-urgent. Bump to the actions' current major versions (or confirm they already declare Node 24 support) next time any workflow file is touched for another reason; not worth a dedicated pass on its own. |

## TD-63 — logged 2026-07-11 during the licensing/compliance review, confirmed live 2026-07-11 during post-merge compliance verification, **RESOLVED 2026-07-11**

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-63 | `streamlit/pages/1_National_Stress_Observatory.py`, `6_Suppression_Event_Explorer.py`, `7_Finance_Bill_2024_Incident_Report.py`, `8_Methodology_Statistical_Guardrails.py` | **RESOLVED.** Was: High. Confirmed live, not just flagged as unverified: pages 1, 6, and 7 carry zero attribution to ACLED or OONI anywhere in their rendered output — no source name, no link to `acleddata.com`/`ooni.org`, no license statement, no access date, no filters-used or manipulation-disclosure text. A repo-wide grep of `streamlit/` found no `acleddata.com`, `ooni.org`, `CC BY`, or `creativecommons` string anywhere, and there is no shared footer/attribution component (`streamlit/core/layout.py` is a 6-line stub with no functions). Page 8's "Data Sources & Known Limitations" table (added for TD-01's Lumen synthetic-data disclosure) names OONI/ACLED/Google/Lumen and their Real/Synthetic status, but does not satisfy either source's actual citation requirement: ACLED's Attribution Policy calls for the source name with a link, an access date, and disclosure of any filters/subsetting applied; OONI's CC BY-NC-SA 4.0 §3(a) calls for attribution to the licensor, a link to the license, and indication of changes made to the licensed material — none of these elements are present anywhere in the dashboard. This is a real compliance gap now that ADR-0007 treats OONI/ACLED licensing as an active governance boundary rather than a settled non-issue. | **Built:** `attribution_footer(sources, snapshot_at=None)` in `streamlit/components/trust.py` — the natural existing home for provenance-disclosure components (already held `synthetic_data_notice`, `render_trust_strip`), not a new location. Renders source name + link, OONI's license name + link (CC BY-NC-SA 4.0), a real access/refresh date (reused from each page's own `snapshot_at`, already plumbed to `render_trust_strip` — no invented date), and a one-line non-redistribution statement, satisfying ACLED's Attribution Policy and OONI's CC BY-NC-SA 4.0 §3(a). Traced each page's actual mart lineage rather than assuming: pages 1 (ACLED only, via `mart_political_stress_windows`), 2/3/5 (OONI only), 4/6/7/9 (both, via `protocol_repression_correlation_mart`/`asn_behavior_profile_mart`/pressure-attribution marts) — all 9 pages read ACLED- and/or OONI-derived data, so all 9 now render the footer, each scoped to its actual sources (`["ACLED"]`, `["OONI"]`, or `["ACLED", "OONI"]`), not a blanket citation. Page 8 gets it placed directly under its "Data Sources & Known Limitations" table, not just appended at page-end, since that's the section TD-63 named as inadequate; page 8 has no live query so its footer omits the refresh-date segment rather than inventing one. Google Transparency Report (also present on page 9) is deliberately not claimed here — that's TD-64's separate, still-open license question. Verified via headless-Chromium screenshots against live BigQuery data on pages 1 (ACLED-only), 5 (OONI-only), 8 (both, in-section), and 9 (both, page-end) — all render real clickable links and the correct per-page source set, not placeholder text. |

## TD-64 — logged 2026-07-11 during the licensing/compliance review; ingestion mechanism confirmed clean, reuse-terms question still open, 2026-07-11 during post-merge compliance verification

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-64 | `Bruin/assets/ingest/google_transparency_requests.py`, `google_transparency_detailed.py`; Google Transparency Report's own reuse terms | Medium. Google Transparency Report's reuse/redistribution license status could not be determined during the 2026-07-11 compliance review despite a direct search — distinct from ADR-0003's already-resolved question of its correct *grain* (Contextual/Backdrop tier), which is unaffected. Confirmed during post-merge verification, as a partial mitigation: the *acquisition* method is clean — both `raw.google_transparency_requests` and `raw.google_transparency_detailed` read locally-staged CSV files (`google-government-removal-requests.csv`, `google-government-detailed-removal-requests.csv`, filenames matching Google's own dashboard export naming) via `pd.read_csv()`; no scraping library (`requests` against the rendered dashboard, BeautifulSoup, Selenium, Playwright) appears anywhere in the ingest path — this is official CSV export, not scraping. This does not resolve the separate, still-open question of what CLIO is permitted to do with the exported data once acquired (fuse into a report, redistribute, use commercially). | Locate Google's actual reuse/redistribution terms for Transparency Report data (the Transparency Report's own terms-of-service/FAQ, not the CSV download mechanism itself) and score it against the Evidence Source Acceptance Framework's licensing-compatibility dimension (ADR-0008), the same treatment already given OONI/ACLED/STOP/CPJ. Until resolved, treat Google Transparency data with the same non-commercial-default caution ADR-0007 applies to OONI/ACLED. |

## TD-65 — logged 2026-07-12; a live public-repository data leak, found by external report, RESOLVED (removal + full history rewrite) 2026-07-12

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-65 | `docs/03-development/CLIO_Project_Zero_Review.md` (removed from HEAD and from history) | **Critical, RESOLVED (removal + history rewrite — see caveat below for what a rewrite can and can't guarantee).** A full copy of `CLIO_Project_Zero_Review.docx`, including its Commercial Assessment (Section 3: eleven revenue-model evaluations, the year-one revenue projection range, per-engagement government/election-monitoring dollar ranges) and Strategic Assessment (Section 4: named-company competitive-acquisition analysis for OpenAI, Anthropic, Microsoft, GitHub, Palantir, and others), was committed directly to `docs/03-development/` — one of only two folders (`docs/02-architecture/`, `docs/03-development/`) the 2026-07-12 gitignore policy designated intentionally public. Added in commit `2bb9c55` ("add zer review to help in pressure decomposition"), 2026-07-06 04:02 UTC, a single-file, 185-line addition with no other changes in the same commit and no commit body. Reads as an intentional-but-misplaced copy — likely written to the public dev-process folder instead of the private `docs/01-product/`/`docs/05-business/` folders CLAUDE.md already specified for this exact content, rather than a deliberate publication decision. **The file was live and publicly tracked on `main` for approximately 6 days** (2026-07-06 04:02 UTC → 2026-07-12, confirmed present in a fresh GitHub codeload zip of `main` at report time) before this fix. Checked whether `docs/01-product/`/`docs/05-business/` already held this content (per CLAUDE.md's description of the intended split) — they did not: `docs/05-business/` did not exist on disk at all, and `docs/01-product/README.md` stated its planned product-strategy/roadmap files were "Not yet populated." This was not a duplicate; it was migrated, not simply deleted. A broader sweep of `docs/02-architecture/*.md` and `docs/03-development/*.md` (line-count check + targeted grep for revenue/pricing/named-competitor language) found no other instance of the same failure mode. | **Done:** (1) Section 2 (Product Assessment) migrated to `docs/01-product/product-assessment.md`; Section 6 (Recommendations) migrated to `docs/01-product/roadmap.md`, matching that folder's own pre-existing README plan. (2) Sections 3 (Commercial Assessment), 4 (Strategic Assessment), and 5 (Competitive Landscape) migrated to a newly created `docs/05-business/` folder (`commercial-models.md`, `strategic-assessment.md`, `competitive-landscape.md` + README) — confirmed gitignored (`git check-ignore -v`) before trusting the boundary, per the standing project discipline this TD item itself reinforces. Section 1 (Technical Assessment) and Section 7 (Documentation Standards) were not migrated — both are already superseded by newer, more detailed public docs (`architecture-assessment.md`, `testing-strategy.md`, `documentation-standards.md`). Sections 8–9 (Repository Governance, Repository Reorganization) were not migrated — process content, largely already executed (the `docs/00-07` structure exists) or lower-sensitivity; left for a future pass if ever needed, not urgent. (3) `docs/03-development/CLIO_Project_Zero_Review.md` removed via `git rm`, committed on its own (originally as commit `2bb9c55`'s removal — that hash, like every hash cited in this row, is a **pre-rewrite** reference; see below). **Follow-on, 2026-07-12 same day:** investigating this item surfaced a related, previously-flagged-but-unremediated finding — `decision-log.md` itself (public, tracked) contained a real person's private correspondence detail and CLIO's own commercial-negotiating reasoning in two narrative passages. Redacted into `docs/07-governance/decision-log-private-addendum.md`, generalized in the public file. Treated as a follow-on to this item rather than a new TD number, since it's the same content-boundary failure mode (public folder, private content) just in a different file.

**History rewrite, 2026-07-12, same day, Sam's explicit authorization.** Both the leaked file and the redacted `decision-log.md`/`CLAUDE.md` strings were then scrubbed from all of git history, not just HEAD, closing the residual gap the two fixes above deliberately left open. Process: a full mirror backup was made first (`/workspaces/clio-backup-mirror.git`, independent of anything rewritten) as an undo path; the repository's fork count was confirmed 0 and open/closed PR count confirmed 0 via the GitHub API before proceeding, since either would have left a copy of the old history outside this repo's control; `git-filter-repo` (not the deprecated `filter-branch`) removed the leaked file from all 749 commits (one commit, `2bb9c55`, contained only that file and was pruned as empty once it was removed, landing at 748 commits) and then replaced the private strings across all history with the same generalized text now in HEAD, so the historical record reads consistently with the current file rather than showing a placeholder. Verified before pushing: `git log --all` shows zero remaining references to the removed file or the redacted strings, `git fsck --full` clean. Force-pushed (`git push origin --force --all`) and independently re-verified via a completely fresh, separate clone (not the pushing clone) that GitHub now serves the rewritten history — old commit hashes (`2bb9c55`, and this fix's own prior commits `2f478b9`/`e0ab718`) no longer resolve as objects on a fresh clone. **Every commit hash written into this repository's own tracked docs before this rewrite (this TD-65 row's own prior text, ADR references to specific commits elsewhere) is now a pre-rewrite reference and will not resolve with `git show`/`git cat-file`** — a known, accepted side effect of any history rewrite, not fixed retroactively in every file that cites a hash, since doing so would itself require yet another rewrite pass.

**Honest caveat, stated plainly rather than overclaimed:** this closes new and casual exposure going forward — nobody cloning or browsing the repo from this point on can see the leaked file or the redacted strings, and once GitHub's own garbage collection runs, the pre-rewrite commits become unrecoverable from the GitHub UI. It does **not** retroactively un-see anything: the file was publicly tracked for ~6 days (2026-07-06–2026-07-12) and the private strings for longer (since roughly 2026-07-11); anyone who cloned the repository during either window has a local copy of the old history, unaffected by this rewrite, and no technical control on GitHub's end changes that. Zero forks and zero PRs were confirmed at rewrite time, which materially reduces (but by itself cannot fully eliminate — an out-of-band clone leaves no trace this project could ever detect) the population that could hold a pre-rewrite copy. |

## TD-66 — logged 2026-07-12; found while writing the public methodology document, extends TD-45 with two findings TD-45 didn't cover, RESOLVED 2026-07-18

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-66 | `Bruin/assets/reporting/political_stress_windows_mart.sql` (`composite_pressure_score`), vs. `Bruin/assets/reporting/mart_pressure_attribution_daily.sql` | **High, RESOLVED.** TD-45 already logged that `fact_country_pressure_daily.composite_pressure_score` and `political_stress_windows_mart.composite_pressure_score` are two different formulas sharing one column name, and rated it a medium-severity future-change risk, "not a current bug." Drafting the public methodology document surfaced two things that row didn't cover, confirmed directly against live code and a live BigQuery query rather than assumed: **(1)** the mart's four added terms — `signal_rate*5`, `weighted_blocking*8`, `max_protocol_stress_score*0.04`, `elevated_protocol_count*0.18`, minus `(1-avg_sample_quality_score)*1.2` — have no cited derivation anywhere in the asset, unlike the fact table's `0.75`/`0.25`, which cite ADR-0004 explicitly at both definition sites. **(2)** No reporting asset decomposes this second score the way `mart_pressure_attribution_daily` decomposes the first — there is no `mart_pressure_attribution_*` equivalent for the mart's own additional terms. Since `streamlit/services/marts.py`'s `get_national_stress` reads `mart_political_stress_windows.composite_pressure_score`, and `national_stress_observatory.py` renders it as the page-1 KPI, trend line, and CSV export, **the specific number most likely to be the first thing a dashboard visitor sees is not the number CLIO's own Pressure Attribution page (`pressure_attribution.py`) actually decomposes.** Confirmed live for the Finance Bill 2024 window (2024-06-22): `fact_country_pressure_daily.composite_pressure_score` = 5.0989 (`pressure_level` = `ELEVATED`); `mart_political_stress_windows.composite_pressure_score` = 9.0947 (own `suppression_window_class` = `CRITICAL_OBSERVABILITY_WINDOW`) — two materially different numbers, both live, both currently correct per their own asset's documented intent, neither wrong on its own terms. This is a real gap against this project's own Extension-A-equivalent "attributable inference" discipline (`docs/07-governance/methodology-internal.md`, Part 1, Extension A) for the specific score the public is most likely to encounter first — not a bug in either formula, and not something to fix by silently merging or renaming without a decision on which number the dashboard should actually headline. | **Resolved 2026-07-18, per the originally planned fix — delete the mart's OONI-fused recomputation outright, repoint the KPI to the clean fact-table value.** A recalibration test was run before deleting (matching the OONI-fused formula's historical CRITICAL/HIGH/ELEVATED trigger-frequency against the clean composite's own delta distribution, re-run against Finance Bill 2024): the clean composite, once recalibrated, correctly classified the **full** 2024-06-22–06-28 peak week CRITICAL — including 2024-06-25, which the OONI-fused version actually misses. The two detectors differed only on the 06-20/06-21 pre-storming ramp and a handful of isolated July days, with no independent ground truth available to tell real signal from noise in the daily-varying OONI terms (the same per-event capture gap TD-67 already flags for Kenya's ACLED coverage) — an intermediate implementation pass initially read this ambiguity as grounds to preserve the detector under an `experimental_` label rather than delete it, but on review that read overclaimed what the data supported ("carries real detection signal" is not demonstrated when the differing days are themselves unfalsifiable), and the project owner confirmed the plain reading: the crisis recovers cleanly once recalibrated, so delete. **What shipped:** `composite_pressure_score`/`pressure_level` in `political_stress_windows_mart.sql` are now a direct, undecomposed-formula passthrough of `fact_country_pressure_daily`'s documented score (v6, `reporting_version = 'political_stress_windows_mart_v6'`) — the page-1 KPI and the attribution page now read the same number. The OONI-fused formula and its entire downstream chain (rolling baseline, delta, window probability/class) are deleted, not renamed — there is exactly one `composite_pressure_score` formula in the codebase now, defined once in `fact_country_pressure_daily.sql`. `streamlit/pages/national_stress_observatory.py`'s "Suppression Window Probability" section (and its now-meaningless insufficient-history gate) was removed accordingly. A new "OONI Protocol-Stress Signal (independent, not included in composite)" indicator was added next to the main KPI, reading `intelligence.protocol_signal_regimes`' existing `protocol_stress_score` rather than inventing a new metric — this piece is independent of the delete-vs-preserve question and ships either way. TD-45's naming-collision half is closed by the same deletion. **Verified:** `bruin validate` (54/54 clean) both before and after the final rewrite; asset re-materialized live; 2024-06-22 re-confirmed live post-final-fix (`composite_pressure_score` = 5.0989/ELEVATED); `mart_pressure_attribution_daily`'s `attribution_residual` tripwire re-checked unaffected (max abs residual 0.0001, unchanged); full test suite green (8 passed, 3 skipped); and the live Streamlit page rendered headlessly (Playwright/Chromium) end-to-end both before and after the final rewrite, zero console errors/tracebacks — KPI, new OONI indicator, trend chart with SEVERE/ELEVATED/MODERATE reference lines, and a clean transition straight into Protocol Escalation Load with no orphaned section. See `decision-log.md`'s 2026-07-18 entry for the full account, including the process correction (an explicit "report back before moving to the next stage" instruction was initially skipped, caught by an advisor consult before commit, corrected by checking in with the project owner). |

The cost audit's strategy inventory ("46 of 47 SQL assets use `create+replace`") was taken through the explicit two-question test — *is the source large and growing?* and *does the asset have a natural incremental key?* — for every `create+replace` asset, so the leave-alone cases are recorded as decisions rather than omissions:

- **Both yes — the OONI staging chain only** (`stg.ooni_measurements` + four observation assets + `int.ooni_experiment_results` and its passthrough fact): large (61.5 GiB source), growing with OONI volume, clean `measurement_date` key. **Conversion still deferred** because the third, unwritten condition failed: nothing actually runs the pipeline on a schedule (verified: no workflow executes `bruin run`; 90-day job history shows 2–6 rebuilds, not 90). The partition_by shipped under TD-58 makes a future conversion mechanical (partition-append). Trigger: wiring a real schedule.
- **No natural key — leave alone, explicitly:** `asn_behavior_profile_mart` (lifetime one-row-per-ASN snapshot, no date grain — TD-02's finding), all `dims` (`dim_dates`, `dim_country`, `dim_asn`, `dim_censorship_confidence`, `dim_blocking_signals`, `dim_measurement_quality` — static/UNNEST vocabularies), `intelligence.country_initialization_status` (orchestration metadata with its own warning against re-running).
- **Small source — leave alone, explicitly:** everything ACLED-weekly (a few thousand rows/decade), Google Transparency (semiannual, tiny), Lumen (synthetic, benched), all reporting marts and remaining intelligence assets (sub-GiB per materialization, per the cost audit). `create+replace` is the right, simplest strategy for all of these; converting any of them would be complexity with no payoff.
- **Must stay `merge`:** `intelligence.acled_pressure_regimes` (stateful, self-referential — EXECUTION CONTRACT).

## TD-67 — logged 2026-07-12, raised in the Cowork planning environment, checked live the same day, OPEN (methodology caveat, not a bug)

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-67 | `stg.acled_conflict_events` (Kenya protest coverage); `intelligence.acled_pressure_regimes` (CONTESTATION/REPRESSION states, never fired in Kenya's history) | **Medium, open, methodology caveat — affects public/grant material's framing of Kenya's "quiet" periods, not a code bug.** Raised in the Cowork planning environment (not originally in this repo — see `decision-log.md`'s 2026-07-12 entry): a UCDP researcher (Magnus Öberg, Data for Peace Conference, June 2026) presented a case study finding ACLED captures only ~11% of actual peaceful protests in Uppsala, Sweden, even in a near-ideal information environment, with 84% of what it does catch sourced from organizers' own online posts — meaning protest coverage is structurally biased toward well-organized, PR-capable groups. This raises an untested alternative explanation for Kenya's ACLED-derived regime classifier never reaching `CONTESTATION` or `REPRESSION` across its full history (previously read as a genuine true-negative): that these states are exactly the smaller, earlier-stage, less-organized activity Öberg's study shows ACLED misses most, and Kenya's information environment could plausibly be worse for protest-capture than Sweden's. **Checked live the same day, one directional signal only:** ACLED's county-level (`admin1`) breadth during the Finance Bill 2024 crisis weeks (31–36 of 47 counties, weeks of 2024-06-15/06-22) is comparable to or exceeds KNCHR's own independently monitored county counts for the same dates (19 by Jun 20, 35 by Jun 25) — no Nairobi/major-city concentration pattern found in the raw per-county breakdown. A separate genuinely quiet period (nine consecutive `STABLE`/`HIGH`-confidence weeks, 2023-04-01 through 2023-05-27) showed county breadth and event volume scaling down together (6–13 counties/week, 10–17 events/week, vs. 31–36 counties/53–83 events during the crisis weeks) — proportional to real reduced activity, not a flat geographic blind spot. **What this does and does not resolve:** this rules out one specific failure mode (a hard geographic coverage cliff visible even during large, heavily-covered events) but does not test Öberg's actual finding, which is about **per-event capture rate for small, non-escalating, less-organized protests** — a different, finer grain than county-level breadth, and one this check has no ground truth to test against for Kenya (no KNCHR-style itemized event log exists for a quiet period to compare against). The true-negative claim for `CONTESTATION`/`REPRESSION` is neither confirmed nor ruled out by this check. | Do not change the regime classifier's logic or thresholds based on this alone — this is a data-coverage question about the input, not a bug in CLIO's classification code. State the caveat honestly in `docs/07-governance/methodology-internal.md` and any grant/public material describing Kenya's "quiet" periods: county-level breadth shows no coverage gap, but the finer-grained per-event undercount Öberg documents remains untested for Kenya absent a comparable itemized ground truth for a quiet period. If such a ground truth source is ever identified (a KNCHR/Access Now/CPJ itemized log for a specific calm month), re-run this check at the per-event grain, not just county-count grain. |

## TD-68 — logged 2026-08-01, retracted-claim investigation (originally raised as a live-Slack thread with OONI, not in this repo), RESOLVED 2026-08-01

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-68 | `Bruin/assets/intermediate/int.ooni_experiment_results.sql`, `dns` CTE's bogon classification (the answer-regex path, three previously-duplicated CASE arms — distinct from TD-55's separate `dns_failure = 'dns_bogon_error'` path, untouched by this fix) | **RESOLVED, 2026-08-01.** CLIO's most-repeated flagship finding — "177 same-day, high-confidence DNS-layer interference signals inside Kenya on June 25, 2024, concentrated on Signal's DNS resolution" — was retracted 2026-07-29 after a live cross-check against OONI's own public API. Root cause, confirmed against real data: the bogon regex matched any DNS answer shaped like a private/reserved IP, with no check of `answer_type` and no exclusion for known-benign infrastructure. `uptime.signal.org` is Signal's own intentional client-side canary hostname, designed to always resolve to `127.0.0.1` as a benign health check — present in effectively every `signal` test session (761/761 days in CLIO's ingestion window). Confirmed independently by OONI's own Signal-test author (`hellais`), 2026-07-30: "the DNS results with IP 127.0.0.1 are to be considered normal." A second, independent latent bug in the same regex: it ran against `answer` regardless of `answer_type`, so a CNAME target beginning `fc`/`fd`/`fe80` would also have false-positived. | **Fix:** hoisted the bogon check into one derived `is_answer_bogon` boolean (previously duplicated identically across the `result_state`/`blocking_detail`/`confidence_score` CASE arms — TD-57's keep-in-sync pattern, now closed for this specific triplication), gated on `answer_type IN ('A','AAAA')` (fixes the CNAME case) and excluding `hostname = 'uptime.signal.org'` for `test_name = 'signal'` (fixes the canary case). TD-55's separate `dns_bogon_error` probe-reported path is untouched (byte-identical) and reconfirmed still passing. Sampled whatsapp/telegram DNS observations for an analogous fixed canary before finalizing the exclusion list: found none — no single hostname bogons on every session the way `uptime.signal.org` does — but did find 353 real regex-bogon-shaped rows for those two apps, concentrated on two dates (2024-02-25/26) across ~18 distinct real edge hostnames bogoning simultaneously, the same whole-session-at-once signature TD-55 already established as real interference; correctly left unexcluded. **Verified live:** the June 25 2024 same-day count reproduces as 0 (was 177); the regex-bogon signal is 100% `uptime.signal.org` across the full 761-day history (49,883 rows, avg 65.5/day, all now reclassified to `dns.ok`/`dns.down`); the retraction holds across the entire Finance Bill 2024 window (May–July 2024), not just June 25 (0 blocked signal/dns rows throughout). New regression lock: `tests/test_ooni_dns_canary_classification.py` (static SQL-text assertions + 4 live-BigQuery behavioral tests under `RUN_BIGQUERY_TESTS=1`), deliberately a separate file from TD-55's `tests/test_ooni_dns_bogon_classification.py` since the two lock unrelated paths in the same CTE. Rematerialized the full downstream chain via `bruin run --downstream` from `int.ooni_experiment_results` (15 assets, all PASS; row counts unchanged everywhere, as expected for a reclassification, not a filter). `bruin validate` 54/54 clean; the ADR-0005 staleness check PASS, 0 new violations. |

## TD-69 — logged 2026-08-01 (TD-72 item (d) in the Cowork planning environment's numbering), RESOLVED 2026-08-01

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-69 | `Bruin/assets/intermediate/int.ooni_experiment_results.sql`, `tls` CTE; `Bruin/assets/staging/stg.ooni_tls_observations.sql` (new `peer_certificate_sha256s` column) | **RESOLVED, 2026-08-01 — but the originally-reported "before" state was itself wrong, corrected mid-fix once TD-72 (below) was found.** Independently of TD-68, a specific measurement showed `signal_backend_failure: ssl_unknown_authority` connecting to Signal's real backend servers (`cdn.signal.org`, `cdn2.signal.org`, `sfu.voip.signal.org`, `storage.signal.org`) — four hostnames, all presenting a byte-identical certificate. Signal rotated its TLS root CA in 2022; older OONI probe versions have broken certificate-verification logic against the new root. Confirmed directly by OONI's Signal-test author (`hellais`), 2026-07-30, who pointed at OONI's own canonical fix (`oonipipeline/src/oonipipeline/transforms/nettests/signal.py`, `ooni/data` `main` branch) — a fixed two-certificate PEM trust store (old "TextSecure"/Open Whisper Systems root, expired 2023-03-23; new "Signal Messenger, LLC" root, issued 2022-01-26), fetched directly from source rather than retyped. Both fingerprints (SHA-256 over the DER bytes) were independently re-derived here and cross-checked byte-for-byte against the live flagged measurement's presented certificate before being written into the fix. **The originally-reported bug shape — `WHEN handshake_success IS FALSE THEN 'BLOCKED'`, with these rows classifying `BLOCKED` — does not hold against this table's actual data**, discovered while implementing the fix's `exclusion_reason` gate (first attempt gated on `handshake_success IS FALSE`, checked against live data before shipping per standing project discipline, and found to always evaluate to zero rows): `handshake_success` is `NULL` for **100% of rows in `stg.ooni_tls_observations`, for every `test_name`** (`signal`, `whatsapp`, `telegram`, `psiphon`), not just the ones with `ssl_unknown_authority` — see new **TD-72**, a separate, much larger, not-fixed-here bug. These specific rows' real prior classification was `UNKNOWN` (`tls.ssl_unknown_authority`), not `BLOCKED` — they fell through every CASE arm to `WHEN tls_failure IS NOT NULL THEN 'UNKNOWN'`. | **Fix:** `stg.ooni_tls_observations.sql` gained a `peer_certificate_sha256s ARRAY<STRING>` column (base64-decode each `$.peer_certificates[].data`, SHA-256 over the DER bytes) — the raw certificate material already flows through to BigQuery via `raw_test_keys` (confirmed live: `raw.ooni_conflict_measurements` lands as local Parquet, not directly in BigQuery — TD-42's note that "the `raw` dataset does not even exist in BigQuery" is still accurate — but its JSON survives verbatim through `load.ooni_to_gcs` into `stg.ooni_measurements.raw_test_keys`, which `stg.ooni_tls_observations` already unnests per handshake). `int.ooni_experiment_results.sql`'s `tls` CTE now checks each handshake's certificate fingerprints against the two known roots. **The exact gate condition went through three review passes before shipping, not one — each caught by checking against live data rather than trusting the previous pass's reasoning:** pass 1 (`handshake_success IS FALSE`, matching the original bug report) always evaluated to zero rows, leading to the TD-72 discovery; pass 2 (`tls_failure IS NOT NULL`) fixed that but was still too broad — applied to all four result_state/blocking_detail/confidence_score/exclusion_reason arms, it would have (a) let a `signal` row that failed with `connection_reset`/timeout but happened to present a recognized cert skip past its real `BLOCKED`/`DOWN` verdict, and (b) also flipped ~166K already-successful `signal` rows from `UNKNOWN` to `OK` as a side effect — correctly reclassifying `signal` specifically while leaving `whatsapp`/`telegram`/`psiphon`'s equally-affected successful rows still `UNKNOWN`, an asymmetry across apps this fix never intended and that partially, silently did TD-72's job for one app only. Pass 3 (shipped): every one of the four arms gates on the exact condition `tls_failure = 'ssl_unknown_authority' AND test_name = 'signal' AND presents_known_signal_root_ca` — the literal confirmed artifact, nothing broader. A match reclassifies `result_state` to `OK` (from `UNKNOWN`, corrected framing) and sets a new, orthogonal `exclusion_reason = 'KNOWN_SIGNAL_ROOT_CA'` column (nullable, added to all four protocol CTEs for union compatibility), scoped to exactly the 34,146 rows this fix is actually about — the further ~166K already-succeeding rows some earlier gate versions incidentally touched are untouched now, correctly remaining `UNKNOWN`, uniform with the other three apps' equally-affected rows (TD-72's bug, not this one's — see TD-72). **Discriminating check confirming this, live:** `signal`'s TLS `result_state` distribution now shows an `OK` bucket of exactly 34,146 alongside `UNKNOWN`/`DOWN`/`BLOCKED` buckets shaped like the other three apps'; `whatsapp`/`telegram`/`psiphon` show zero `OK` rows each, all successes still `UNKNOWN` — the four apps are shaped consistently, as they should be, since TD-72 was deliberately left untouched for all four. Deliberately not a new `result_state`/`blocking_detail` enum value (would have silently miscounted in `features.protocol_daily_signals`'s `STARTS_WITH(blocking_detail,'tls.') AND blocking_detail NOT IN ('tls.ok','tls.rst')` failure counter), and deliberately not added to `dim_censorship_confidence` (wrong grain — that table buckets score→label, not exception logic; would have re-created the ADR-0001 mess). The original `tls_failure` (`ssl_unknown_authority`) stays visible in `failure_reason` unchanged; no row is ever filtered out of staging or any denominator. **Verified live:** the flagged June 25 2024 measurement's 4 TLS handshakes all now read `result_state='OK'`, `exclusion_reason='KNOWN_SIGNAL_ROOT_CA'`, `failure_reason='ssl_unknown_authority'` (visible, not hidden); **34,146 rows reclassified out of `UNKNOWN` (not `BLOCKED`) across the full history** (all `test_name='signal'`, all `failure_reason='ssl_unknown_authority'`, `exclusion_reason` count matches exactly); the Finance Bill 2024 window alone accounts for 3,193 of those. The genuine real remaining `tls`/`BLOCKED` count system-wide (`tls.rst`/`connection_reset`, untouched by this fix) is 361. Two golden-file fixtures in `tests/test_ooni_tls_root_ca_exclusion.py`: (a) the real June 25 2024 measurement, now `OK` not `UNKNOWN`; (b) an adversarial synthetic fingerprint (not a real row — none exists in Kenya's current dataset), proving the guard is an exact two-value allowlist rather than a blanket `ssl_unknown_authority`-is-fine-for-signal carve-out. **Logged, not fixed in this pass:** TD-71 (every TLS failure mode collapsing to one confidence weight) and TD-72 (the `handshake_success` dead-column bug this fix's own review process uncovered) — TD-72 is the higher-priority follow-up of the two. |

## TD-70 — logged 2026-08-01, IAM least-privilege investigation, premise found incorrect against live infrastructure, no change made

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-70 | `infra/setup-gcp.sh` (grants `terraform-sa` its three live roles); `infra/modules/iam/main.tf` (grants `roles/editor` to a human admin identity, see **TD-14**, not to `terraform-sa`) | **Investigated 2026-08-01, premise did not hold — this project's "treat a bug report's proposed fix as a hypothesis" discipline caught it before any live IAM mutation.** A planning-environment relay task (its own "OTF-07") asked to replace `terraform-sa`'s supposed `roles/editor` project-level grant with a least-privilege set. Checked directly against live IAM (`gcloud projects get-iam-policy encoded-joy-485413-k5`) before writing any change, per standing project discipline: `terraform-sa` does **not** hold `roles/editor` and never has, per this policy's own current bindings — it holds exactly the three roles `infra/setup-gcp.sh` grants it (`roles/bigquery.admin`, `roles/storage.admin`, `roles/iam.serviceAccountUser`). No principal in the live project holds `roles/editor` at all today. The actual `roles/editor` grant on record (TD-14, already tracked, still open) targets a human admin identity via `infra/modules/iam/main.tf`'s `google_project_iam_member.admin` resource — and even that resource does not appear to be currently applied: the live human identity (`njogumwaniki9@gmail.com`) holds `roles/owner` (broader than `roles/editor`, but standard/expected for a project's actual owner, not itself a least-privilege finding), not `roles/editor`. `terraform-sa`'s actual live role set is still broader than strictly least-privilege (`bigquery.admin` and `storage.admin` both include IAM-modification rights beyond what Terraform's own resource management needs), which is a real, smaller-than-described finding worth revisiting on its own. | **No live IAM change made this pass**, per this task's own explicit instruction to stop and report rather than force a fix against a wrong premise, and because a live IAM mutation on the account that manages all Terraform-provisioned infrastructure is exactly the class of hard-to-reverse, shared-system action this project's own risk discipline treats as requiring explicit confirmation, not autonomous action, especially once the originating premise was shown to not match reality. If `terraform-sa`'s role set is to be tightened, scope it from its actual observed Terraform usage (`infra/modules/`, current resource set: IAM binding + whatever `storage`/`bigquery` modules exist) rather than this task's original guess. TD-14 (the real `roles/editor` grant, on the human admin, and apparently unapplied) remains the higher-value item if this area is revisited. |

## TD-71 — logged 2026-08-01 during TD-69's resolution, RESOLVED 2026-08-01 (fourth session that day, partial — corroboration-gated tier explicitly deferred)

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-71 | `Bruin/assets/intermediate/int.ooni_experiment_results.sql`, `tls` CTE, `confidence_score` CASE; new `Bruin/assets/marts/dims/dim_tls_failure_evidence.sql` | **RESOLVED (partial).** Was: the `LIKE '%reset%'`/`LIKE '%timeout%'` arms collapsed every TLS failure mode into one of just two verdicts (`BLOCKED`/`DOWN`) at fixed confidence, with everything else (`ssl_invalid_hostname`, `ssl_invalid_certificate`, `connection_aborted`, `eof_error`, and any future OONI failure string) falling to `UNKNOWN` by omission and a flat `ELSE 0.45` confidence, despite very different evidentiary weight per mode. **Fix:** a new dimension table, `marts.dim_tls_failure_evidence` (tls_failure → tier → standalone_confidence, sourced against ooni/spec's `data-formats/df-007-errors.md`, fetched directly, not inferred), joined into the `tls` CTE's `tls_source` sub-select and consumed via `COALESCE(tls_failure_standalone_confidence, 0.45)` — the `COALESCE` is a deliberate default-low allowlist: any `tls_failure` string absent from the dimension table (today or introduced by OONI in the future) silently keeps the pre-fix 0.45 floor, never inherits an elevated tier. Built from the 6 real, live `tls_failure` strings confirmed in Kenya's data (not assumed from the design's source list): `connection_reset` → 0.60 (up from 0.45; RST injection is the strongest single-failure-mode evidence, already routed to `BLOCKED` by the existing arm, unchanged), `generic_timeout_error` → 0.40 (down from 0.45; least attributable, already routed to `DOWN`, unchanged), `eof_error`/`connection_aborted` → 0.50 (genuinely ambiguous per spec), `ssl_invalid_certificate`/`ssl_unknown_authority` (residual, non-TD-69-excluded rows) → 0.45, unchanged, deliberately not elevated. **One correction made before shipping, against the design's own source list:** `connection_aborted` was originally grouped as a client-side artifact (alongside `connection_already_closed`/`interrupted`) meant to route through `exclusion_reason` rather than carry a confidence value at all — checked directly against ooni/spec before accepting that grouping, and it maps `connection_aborted` to `ECONNABORTED`, a documented genuine network-level failure, not an artifact; `connection_already_closed`/`interrupted` are the actual documented artifacts, and neither string occurs anywhere in CLIO's current TLS data. `connection_aborted` shipped in the ambiguous tier (0.50) instead. **Deliberately NOT shipped this pass:** the corroboration-gated elevation for `ssl_*` failures (checking agreement with OONI's own `anomaly`/`blocking` verdict before promoting `ssl_unknown_authority`/`ssl_invalid_certificate` toward a higher tier) — confirmed live, before deciding, that CLIO's ingested raw OONI measurement JSON has no `anomaly`/`test_keys.blocking` field at any level (both `NULL` for 100% of 50,454 `signal`-test rows; these are OONI fastpath/API-computed fields, never present in the raw probe measurement CLIO ingests) — there is no corroboration signal to gate on today, so `ssl_*` failures stay at the pre-fix 0.45 floor rather than shipping an unconditional elevation, which would repeat the exact false-positive shape this project already fixed twice today (TD-68, TD-69). **A real, measured downstream cascade, caught before being reported done, that corrects (not contradicts) TD-72's own prior finding:** `confidence_score` for `BLOCKED` (reset) rows feeds `features.protocol_daily_signals.sql`'s `confidence_weighted_interference`/`signal_rate` directly, which in turn feeds `reporting.asn_behavior_profile_mart.sql`'s `avg_weighted_blocking` (`AVG(confidence_weighted_interference)`) and, through `normalized_weighted_signal`/`maturity_adjusted_signal`, `behavioral_priority_score` and `behavioral_class`. TD-72's own investigation traced this exact pathway and reported it "confirmed immune" — correct for TD-72's own scope (TD-72 never changed `confidence_score` for `BLOCKED` rows, only moved `UNKNOWN`→`OK`), but that immunity does not extend to a fix that changes the confidence *value* of `BLOCKED` rows, which is exactly what this fix does. **Measured, not assumed:** the Finance Bill 2024 window (2024-05-11 to 2024-07-13) shows zero `protocol_state`/`intelligence_state`/`correlation_state`/`final_confidence_level` tier flips across 100 regime rows and 64 correlation-mart rows (numeric movement small: `stress_divergence` max abs delta 0.077, `rolling_pressure_corr` max abs delta 0.016). Outside that window, full-history: **`reporting.asn_behavior_profile_mart`'s AS33771 (Safaricom Limited) flipped `behavioral_class` from `STABLE` to `VARIABLE_BEHAVIOR`** (`behavioral_priority_score` now 0.0518, just above the 0.05 threshold — driven by `maturity_adjusted_signal`, not `coupled_escalation_days`, which is 0 for this ASN). Blast radius measured across the full 71-ASN mart: AS33771 is the only ASN that crossed a threshold (68 `STABLE`, 1 `HIGH_SIGNAL_PROVIDER`, 1 `ELEVATED_SIGNAL_PROVIDER`, 1 `VARIABLE_BEHAVIOR`); two other ASNs sit close to the 0.05 boundary but did not cross (AS328490 at 0.036, AS30844 at 0.0398). AS36926/AS37061 (TD-72's other two tracked ASNs) confirmed unchanged. No full-history before/after snapshot of this mart was taken prior to rematerializing (only the TLS-partition Finance-Bill-window slice of the three upstream tables was snapshotted) — this is a mechanistically-traced, plausible consequence of the fix, not a byte-for-byte proven delta for this specific mart; any other ASN's `behavioral_class` should be spot-checked before citing externally. New golden-file test `tests/test_ooni_tls_failure_evidence_tiering.py` (6 tests: 2 static, 4 live) brings the suite to 31/31. `bruin validate` (55/55, including the new dimension table) and `ruff check .` both clean. Full 16-asset downstream chain rematerialized clean (21/21 quality checks; one transient DuckDB local-lock-contention failure on `intelligence.validate_ooni_intelligence_contracts`, a documented pre-existing flake, resolved on immediate retry, not a regression). Finance Bill 2024 Incident Report page rendered headless post-fix — clean, zero tracebacks. Full detail in `decision-log.md`'s 2026-08-01 (fourth session) narrative entry and `reports.md`. | **Resolved, partial.** Downstream: any external citation of AS33771/Safaricom's `behavioral_class` as `STABLE` should be corrected to `VARIABLE_BEHAVIOR`; any other ASN's `behavioral_class` cited externally should be spot-checked (mechanism is general, measured blast radius today is one ASN). The `ssl_*` corroboration-gated elevation (an equivalent to OONI's own control-vantage-point corroboration, since CLIO's ingested data carries no `anomaly`/`blocking` field to gate on directly) remains open, unbuilt — a candidate replacement signal (cross-ASN same-day coordination, per this fix's own calibration query) is noted but not designed. Cross-country/global-simultaneity corroboration remains explicitly out of scope. **Externally validated 2026-08-01 (fifth session that day) against OONI's own live API, not just CLIO's own internal tables:** the `tls_failure` extractor this fix's dimension table joins against was checked verbatim against raw OONI JSON for all 113 available re-tiered rows (`connection_reset`/`generic_timeout_error`/`eof_error`/`connection_aborted`) — 113/113 exact matches, zero mismatches. A concrete, evidence-backed candidate for the still-deferred `ssl_*` corroboration gate also surfaced during that session: 99/100 sampled residual `ssl_*` `UNKNOWN` rows would be discarded by OONI's own documented known-bad-probe-version table (`fastpath/core.py`'s `test_version`/`engine_version` gates), suggesting that table — not the absent `anomaly`/`blocking` fields — is the practically available corroboration signal for a future pass. Full detail in `reports.md`'s fifth-session entry and `decision-log.md`'s matching narrative entry. |

## TD-72 — logged 2026-08-01 during TD-69's implementation, RESOLVED 2026-08-01 (third session that day)

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-72 | `Bruin/assets/staging/stg.ooni_tls_observations.sql` (was `SAFE_CAST(JSON_VALUE(tls_json, '$.status.success') AS BOOL) AS handshake_success`); `Bruin/assets/intermediate/int.ooni_experiment_results.sql`, `tls` CTE | **RESOLVED.** Was: `handshake_success` was `NULL` for 100% of the 422,487 rows in `stg.ooni_tls_observations`, across every `test_name` this project ingests (`signal`, `whatsapp`, `telegram`, `psiphon`). Root cause, confirmed by inspecting raw `tls_handshakes` JSON directly, and independently re-confirmed by this fix: these four OONI nettests' TLS handshake objects have a flat shape with no nested `status` object at all — `$.status.success`/`$.status.failure` are OONI schema fields belonging to a *different* data shape, `df-005-tcpconnect` (OONI's TCP-connect format), which genuinely exists in these same tests' own `tcp_connect[]` arrays (confirmed live: `stg.ooni_tcp_observations`'s identical JSONPath is non-NULL for 100% of 2,682,758 rows there) — not, as first guessed, `web_connectivity`'s shape (never ingested here); a copy-paste of the TCP extractor onto the TLS array, not a reference to unrelated test data. Consequence: `int.ooni_experiment_results.sql`'s `tls` CTE's `handshake_success IS TRUE` arm never fired, so a genuinely successful handshake fell through to `ELSE 'UNKNOWN'` instead of `OK` — 386,617 of 422,487 rows (91.5%), all four test types, quantified in this item's original entry and re-verified unchanged at fix time (`psiphon` 49,848/49,914 = 99.9%, `signal` 178,489/213,712 = 83.5%, `telegram` 56,585/57,031 = 99.2%, `whatsapp` 101,695/101,830 = 99.9%). **Fix:** `handshake_success` now derives from `COALESCE(JSON_VALUE(tls_json,'$.status.failure'), JSON_VALUE(tls_json,'$.failure')) IS NULL` — the same expression `tls_failure` already used, computed independently (not by referencing that column's alias, since BigQuery SELECT-list expressions can't see sibling aliases) so the two stay consistent by construction. This mirrors OONI's own canonical TLS model (`probe-cli`'s `ArchivalTLSOrQUICHandshakeResult`, `oonipipeline`'s `measurement_to_tls_observation`), which has no boolean success field at all — only `failure` (null = success); certificate trust stays a fully separate, orthogonal concept (`peer_certificate_sha256s`/`presents_known_signal_root_ca`, TD-69's own machinery, untouched by this fix). **The one real design risk in this fix, caught by tracing the full CASE expression before editing, not assumed:** the `tls` CTE already contained a `WHEN handshake_success IS FALSE THEN 'BLOCKED'` arm (and a parallel `THEN 0.75` in `confidence_score`), written under this project's own "diagnose before fixing" discipline back when TD-69 first found `handshake_success` was dead — both arms were themselves dead code at the time (never fired, since `handshake_success` was always `NULL`). Turning `handshake_success` on without removing them would have made them fire for real for the first time, silently reclassifying every TLS failure mode not already caught by the reset/timeout/cert-exclusion arms (`ssl_invalid_hostname`, `connection_aborted`, `eof_error`, etc.) from `UNKNOWN` to `BLOCKED` — a real classification change with no per-failure-mode review behind it, and squarely TD-71's still-open question, not this fix's scope. Both arms were removed rather than resurrected, with an explanatory comment at each site. **Verified live, before and after rematerializing (full chain, `bruin run --downstream` from `int.ooni_experiment_results`, 16 assets, 21 quality checks, all clean):** `BLOCKED`/`DOWN` counts by `test_name` are byte-for-byte unchanged (signal `BLOCKED` 133, telegram 176, whatsapp 52; psiphon/signal/telegram/whatsapp `DOWN` 59/203/265/79) — proving the fix is provably narrow, only `UNKNOWN`→`OK` moved, and by exactly the pre-measured scale (`signal` `OK` rose from the existing 34,146 TD-69 cert-exclusion rows to 212,635 = 34,146 + 178,489; psiphon/telegram/whatsapp `OK` rows went from 0 to 49,848/56,585/101,695). `stg.ooni_tls_observations.handshake_success` is non-NULL for all 422,487 rows post-fix. Downstream, for the `tls` partition specifically (confirmed immune elsewhere by direct trace, not assumption): `features.protocol_daily_signals` TLS `unknown_rate` avg 0.9584→0.0007, `sample_quality_score` avg 0.3049→0.4964, `high_unknown_flag` true-count 10,664/10,948→1/10,948; `intelligence.protocol_signal_regimes` TLS `confidence_level` LOW-share 98.9%→68.4% (HIGH 48→1,745 rows, MEDIUM 72→1,709); `reporting.protocol_repression_correlation_mart` TLS partition `avg_sample_quality_score` 0.637→0.8168 (all-time), 0.6016→0.7887 (Finance Bill 2024 window); `avg_stress_divergence` 1.1246→1.0105 (all-time), 1.0869→0.9458 (Finance Bill); `avg_rolling_pressure_corr` stayed near-zero both times but its Finance Bill-window peak magnitude grew, `max(abs(rolling_pressure_corr))` 0.1477→0.2188; `reporting.asn_behavior_profile_mart`'s three tracked ASNs (AS33771/36926/37061): `behavioral_class` (`STABLE`, all three) and `dominant_protocol` confirmed unchanged by tracing `behavioral_priority_score`'s full formula (none of its inputs trace to `sample_quality_score`), `data_reliability_score` rose for AS33771 (0.8931→0.9123) and AS37061 (0.7502→0.7699), flat for AS36926 (0.461→0.4806), `latest_confidence_level` flipped LOW→HIGH for AS33771 only. `composite_pressure_score` confirmed untouched (no OONI term since TD-66). New golden-file test `tests/test_ooni_tls_handshake_success_fix.py` (5 tests: 2 static, 3 live) brings the suite to 25/25. `bruin validate` (54/54) and `ruff check .` both clean. Finance Bill 2024 Incident Report page rendered headless against live post-fix data — clean, zero tracebacks, only Streamlit's own benign internal `_stcore` health-check 404s. **Addendum, caught by a follow-up advisor consult after the fix was first reported done:** `confidence_score` also moved for the same rows (0.45→0.70), which crosses `dim_censorship_confidence`'s MEDIUM threshold (≥0.60) — `marts.fact_protocol_blocking_summary` and `reporting.mart_pressure_attribution_ooni_daily` both carry a `medium_confidence_events`/`low_confidence_events` split that shifts accordingly (TLS: `low` 388,341→1,724, `medium` 34,146→420,763, monthly grain; the daily-grain mart is directly rendered on the Pressure Attribution page's per-date OONI corroboration table — confirmed live on the June 25 2024 flagship date: TLS/`signal` `medium_confidence_events` 0→695 of 715). Full detail, including every before/after number, in `decision-log.md`'s 2026-08-01 (third session) narrative entry and `reports.md`. | **Resolved.** Downstream: any previously-cited external figure for TLS-specific protocol-repression correlation, TLS anomaly/stress-divergence, TLS regime confidence, TLS "medium-confidence" event counts from either blocking-summary mart, or `asn_behavior_profile_mart`'s confidence/reliability fields for a TLS-dominant ASN should be treated as superseded and re-verified against the now-corrected tables before being cited again — `composite_pressure_score`, blocked-event counts, and `behavioral_class`/dominant-protocol classification needed no correction (confirmed immune). TD-71 (per-failure-mode TLS confidence weighting) remains open and is now genuinely unblocked, not subsumed by this fix. **Externally validated 2026-08-01 (fifth session that day) against OONI's own live API, not just CLIO's own internal BigQuery tables:** this fix's core premise (`tls_failure IS NULL` ⇒ genuine handshake success) was checked against raw OONI JSON for 100 rows sampled from the ~386,617 this fix moved `UNKNOWN`→`OK` (25 per app) — 100/100 showed `failure` present-and-null in OONI's own `tls_handshakes[]` array at the exact `tls_offset` CLIO recorded, the acceptance bar the validation session set for itself. Zero rows showed the specific failure mode this check existed to catch (an absent `failure` key silently read as null, the mirror image of this fix's own original bug). Full methodology and results in `reports.md`'s fifth-session entry. |

## TD-73 — logged 2026-08-01, fifth session that day, during external-validation work, OPEN

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-73 | `Bruin/assets/staging/stg.ooni_tls_observations.sql`, lines 49-50 (observation_id hash) and 64-65 (`ip_address`/`port` columns) | `ip_address` and `port` are `NULL` for 100% of all 422,487 rows in `stg.ooni_tls_observations`, across every ingested test type — confirmed live. Root cause: `JSON_VALUE(tls_json, '$.ip')`/`'$.port'` read JSONPaths that don't exist on a TLS handshake object; OONI's real field is a single combined `address` string (`"ip:port"`, e.g. `"127.0.0.1:42749"`), confirmed directly against live raw JSON. Surfaced as a byproduct of building this session's OONI-API validation script (needed a way to match CLIO's stored TLS observations against OONI's raw `tls_handshakes[]` array; `ip_address`/`port` turned out to be unusable for that, so `tls_offset`, the table's existing UNNEST-position column, was used instead — see `reports.md`'s fifth-session entry). Distinct from TD-72's `handshake_success` bug (a different field, a different wrong-shape assumption) but the same general failure class: a JSONPath that silently returns NULL instead of erroring when the assumed shape doesn't match. Confirmed narrow: `stg.ooni_tcp_observations.sql` uses the identical `$.ip`/`$.port` pattern but is *not* affected — `tcp_connect[]` objects genuinely have that flat shape (confirmed live, 100% populated, 2,682,758/2,682,758 rows) — and no third occurrence exists anywhere in the codebase. **Confirmed to have zero effect on anything TD-68/TD-69/TD-72/TD-71 touched or this session validated** — `handshake_success`/`tls_failure`/`result_state`/`confidence_score`/`blocking_detail`/`exclusion_reason` in `int.ooni_experiment_results.sql`'s `tls` CTE never reference `ip_address` or `port`. **One real, if minor, downstream consequence was traced, not left unchecked:** `int.ooni_experiment_results.sql`'s `tls` CTE does read `ip_address` for two columns — `COALESCE(server_name, ip_address) AS observation_target` (masked in practice: `server_name` is 100% populated for every TLS row, confirmed live, so the `ip_address` fallback never actually fires) and `ip_address AS endpoint_ip` (not masked: this is always `NULL` for every TLS-protocol row in `int.ooni_experiment_results`). `endpoint_ip` passes through one further layer, to `marts.fact_ooni_censorship_signals.endpoint_ip` — grepped every further layer (`features/`, `intelligence/`, `reporting/`, `streamlit/`) for `endpoint_ip` and found no further consumer; it terminates at that one mart, unused beyond it. So the live, if narrow, effect is: `fact_ooni_censorship_signals.endpoint_ip` is `NULL` for every TLS-protocol row, which would only matter to an analysis that specifically wanted the numeric IP a TLS handshake connected to (as opposed to its hostname, which is fully available via `observation_target`/`server_name`). | Fix the extraction to parse `$.address` (split on the last `:` to separate IP from port — IPv6 addresses may contain colons themselves, so a naive `SPLIT` is not safe; needs a real parse, not copied from the TCP extractor). Given the consumer trace above, this is a real but low-stakes gap (a masked-except-for-one-column dead field, not a live misclassification) — safe to schedule as ordinary cleanup rather than urgent. |

## TD-74 — logged 2026-08-02, dependency-hygiene/documentation-refresh session, RESOLVED 2026-08-02

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-74 | `Bruin/requirements.txt` (duckdb pin) vs. `pyproject.toml`/`uv.lock` | `Bruin/requirements.txt` pinned `duckdb==0.10.0` while `pyproject.toml` required `duckdb>=1.5.0` (resolved to `1.5.0` in `uv.lock`) — a real drift, not investigated before this session as to which pin (if either) was actually authoritative at runtime. **Investigated before fixing, per this project's own standing discipline:** confirmed empirically, not assumed, that neither top-level file governs Bruin's actual Python-asset execution. No repo Python file does `import duckdb` directly; the top-level `pyproject.toml`/`uv.lock` environment was never even installed in this Codespace (no `.venv`, `duckdb` not importable in the system Python — the durable Codespace setup script, `.devcontainer/setup-gcp-tooling.sh`, only installs `google-cloud-bigquery`). Instead, `bruin run` against a `type: python` asset builds an ephemeral, isolated `uv`-managed environment **per `Bruin/requirements.txt`** — confirmed by inspecting the resulting `uv` cache directly (`duckdb-0.10.0.dist-info` present after a real `bruin run` against `reporting.validate_pressure_attribution_contracts`, a `duckdb-parquet`-connected asset), and independently by observing `bruin run`'s own install log naming the exact package/version it pulled. `Bruin/requirements.txt`, not `pyproject.toml`, was the pin actually in effect. | **RESOLVED.** Bumped `Bruin/requirements.txt`'s `duckdb==0.10.0` to `duckdb==1.5.0`, matching `pyproject.toml`/`uv.lock` — no reason found not to: re-ran the identical live test (`bruin run` against `reporting.validate_pressure_attribution_contracts` and, separately, `intelligence.validate_ooni_intelligence_contracts`, both `duckdb-parquet`-connected) after the bump, confirmed `duckdb-1.5.0.dist-info` actually installed and both assets `PASS`ed cleanly end to end. Added an explanatory header comment to `Bruin/requirements.txt` itself (previously undocumented anywhere) stating that this file, not the top-level `pyproject.toml`/`uv.lock`, is what Bruin's own per-asset runtime reads — the root cause of how the drift went unnoticed, since a developer editing `pyproject.toml` alone would have no reason to suspect a second, silently-authoritative dependency file existed. Also corrected the README's DuckDB badge (previously "Local Analytics", which overstated/mischaracterized its role — DuckDB is not a user-facing local-analytics layer in this project at all) and added one explanatory sentence to the README's Architecture Overview section stating DuckDB's real role. Separately, noted but not fixed: the ruff-pin commit (`a3df432`, message references "TD-74" in what was evidently the planning environment's own numbering at the time) was never given a corresponding entry in this file — this session's own next-available number, confirmed by reading this file directly rather than the git log, was also TD-74, a direct instance of the cross-environment numbering collision this project has hit repeatedly; no attempt was made to guess or backfill the ruff-pin item's real content under a different number. |

## TD-78 — logged 2026-08-03 during `llm_report_writer/`'s first live comparison run (11 real windows against BigQuery), fixed in the planning workspace, applied live here 2026-08-03, RESOLVED (mitigated, not eliminated — see below)

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-78 | `llm_report_writer/report_writer_openai.py` (LLM generation output); no fix location — a new advisory module, `coherence_check.py`, was added instead | One generated sentence in an otherwise-clean 11-window live run contained a stray non-Latin-script token mid-sentence ("Kenya was classified as STABLE for the week **սկս** starting 2024-03-23.", Armenian for roughly "start") that still passed all four of `verify_report.py`'s gated checks — `claim_check.py` (factual truth), `causal_language_check.py` (causal/superlative framing), and `completeness_check.py` (disclosure completeness) don't check basic textual coherence, so a garbled token attached to an otherwise-true, causally-clean claim verifies cleanly. This is a real, named gap in the verification design, not a bug in any of the three existing checks — each does exactly what it was built to do. | **RESOLVED (mitigated).** New advisory-only module `coherence_check.py` added (character-level check: flags any whitespace-split token containing a character outside ASCII, a short allowlist of common punctuation/symbols, and Unicode names starting `LATIN`/`COMBINING`). Deliberately non-blocking, same posture as `style_lint.py` — this is a readability signal for a human to skim, not folded into `verify_report.py`'s four gated checks, since retroactively gating on it would be a design change beyond this fix's scope. New test file `tests/test_coherence_check.py` (8 tests) proves it catches the exact TD-78 sentence verbatim and correctly passes clean English prose, accented Latin place names, and common punctuation. Wired as an advisory print (never a rejection) into all three runner scripts: `run_report.py`, `run_baseline_comparison.py`, `run_live_windows.py`. **Live spot-check performed 2026-08-03** (one additional billed API call, user-confirmed before running): regenerating the exact same window (Kenya, 2024-03-23..2024-04-20) reproduced the *same failure mode* with a *different* garbled token (Gujarati "શરૂ", also meaning "start"), inserted at the same position in the sentence, right before the English word "starting" — the new coherence check caught it live, exactly as designed. This recurrence across two independent generations, at the same word position, suggests a systematic model quirk tied to this specific prompt/window rather than one-off noise — worth further investigation if this pattern keeps appearing across more windows, not something this fix resolves at the root (the underlying LLM behavior is unchanged; only detection is new). See `reports.md` (2026-08-03 session, second same-day) for the full transcript of both occurrences. |

## TD-79 — logged 2026-08-03 during `llm_report_writer/`'s first live BigQuery build, fixed in the planning workspace, applied live here 2026-08-03, RESOLVED

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-79 | `llm_report_writer/report_writer_openai.py`, `_get_client()` | `repo_root = Path(__file__).resolve().parent` resolves to `llm_report_writer/` itself, not the actual repo root one level up where `.env` really lives — so `load_dotenv(repo_root / ".env")` was looking one directory too deep. Found live 2026-08-03 during the first live-build session; masked there only because `OPENAI_API_KEY` was already exported directly in that Codespace's shell environment, independent of any `.env` file, so the bug never actually caused a visible failure. No offline test caught it because the whole `_get_client()` function was untestable without either a real API key or extensive mocking of the OpenAI client. | **RESOLVED.** The path computation was extracted into its own pure, side-effect-free `_repo_root()` function (`Path(__file__).resolve().parent.parent`), independently testable without any API key or network call. New test file `tests/test_report_writer_openai.py` (3 tests) locks in the fix: asserts `_repo_root()` is never equal to `llm_report_writer/` itself (the exact TD-79 failure mode), asserts it resolves to `llm_report_writer/`'s actual parent directory, and asserts it's a real directory on disk. `_get_client()`'s only other line changed is calling `_repo_root()` instead of inlining the (buggy) computation — behavior for callers is otherwise identical. |

## TD-80 — logged 2026-08-15 during a Tier 3 external-validation audit (non-DNS OONI classification review), RESOLVED 2026-08-15 (TD-87 Phase 0 fix session)

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-80 | `Bruin/assets/intermediate/int.ooni_experiment_results.sql`, `tcp` CTE, `result_state`/`blocking_detail`/`confidence_score` CASE expressions (lines 156-174) | The TCP timeout arm (`LOWER(COALESCE(tcp_failure,'')) LIKE '%timeout%'`) does not match the literal live value `tcp_failure = 'timed_out'` — a different string, not a substring of "timeout" — so these rows fall through to the generic `tcp_failure IS NOT NULL THEN 'UNKNOWN'` arm (confidence 0.40) instead of `DOWN` (confidence 0.45) alongside the other `generic_timeout_error` rows. Confirmed live: 16 of 22,268 non-null `stg.ooni_tcp_observations.tcp_failure` rows carry the literal value `timed_out`, spread thin across 8 dates from 2023-12-06 to 2025-06-28 (`test_name` whatsapp/telegram only) — not clustered like a canary, a genuine minor OONI-side string variant, not a CLIO extraction bug. Verified against OONI's own raw measurement JSON directly (`api.ooni.org`/`api.ooni.io`, matched by `tcp_offset`): CLIO's stored `tcp_failure='timed_out'` is a byte-exact copy of OONI's own `tcp_connect[].status.failure` field, so the gap is purely in this CASE's pattern list, not the extractor. Same bug class as TD-68/TD-71 (an unguarded/incomplete pattern match with no allowlist), but the opposite direction — a false-negative (undercount into UNKNOWN) rather than a false-positive (over-count into BLOCKED/interference). Also confirmed while investigating this: the TCP `BLOCKED` arm (`LIKE '%reset%'`) has zero matching rows in Kenya's live data (dead code today, not itself a bug). Found during the Tier 3 external-validation audit's non-DNS classification review (`reports.md`, 2026-08-15 session); left unfixed per that session's explicit instruction not to silently patch a newly-found bug mid-audit. **CORRECTION:** this entry's original confidence figures were wrong — the pre-fix TCP CASE gives every non-reset/non-success row (including `generic_timeout_error`'s `DOWN` rows) a flat `0.45`, not `0.40`; there is no `0.40` tier anywhere in this CTE (that figure belongs to the `dns`/`http` CTEs' own UNKNOWN fallback, a different CTE, mistakenly attributed here). | **RESOLVED.** New allowlist dimension table `marts.dim_tcp_failure_evidence.sql` (modeled on `marts.dim_tls_failure_evidence`, same `LEFT JOIN` shape) now supplies `result_state`/`blocking_detail`/`confidence_score` for all 7 known live `tcp_failure` strings, replacing the inline `LIKE` pattern-match CASE for state-routing (not just confidence, unlike the TLS table). `timed_out` now maps to `DOWN`/`tcp.timeout` (grouped with `generic_timeout_error` as the same timeout family, so `features.protocol_daily_signals.tcp_timeout_events` picks up these 16 rows too — an intended consequence of the fix). Verified live, full before/after count comparison: `DOWN` 19,554→19,570 (+16, exactly the fix), `UNKNOWN` 2,714→2,698 (−16), every other `tcp_failure` string's `result_state`/`confidence_score` byte-for-byte unchanged (all still `0.45`). Confirmed the 16 affected rows do **not** fall inside the Finance Bill 2024 window (2024-05-11 to 2024-07-13) — closest dates are 2024-04-12 and 2024-11-16/18/20, both outside it — so this fix has no effect on that flagship window and the golden fixture in TD-87 Phase 0's Task C was safely frozen after this fix landed. The TCP `BLOCKED`/`LIKE '%reset%'` arm is confirmed still dead (0 live matches) and deliberately left in place, documented in `dim_tcp_failure_evidence.sql`'s header. See `reports.md`, 2026-08-15 TD-87 Phase 0 session entry. |

## TD-81 — logged 2026-08-15 (second session that day) during TD-72 Tier 3's Google Transparency Report parsing spot-check, RESOLVED 2026-08-15 (TD-87 Phase 0 fix session, tracked there as "TD-89")

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-81 | `Bruin/assets/ingest/google_transparency_requests.py` (column selection + hardcoded path); `stg.google_transparency_requests` (resulting coverage gap and one data discrepancy) | **Three related findings, same root cause (the ingest script no longer matches Google's real, current export), found once the real export file became available in this Codespace for the first time.** (1) **Confirmed live via direct execution, not inference:** the script's column selection (`df[["time_period", "country", ..., "items_requested_removal", ...]]`) raises `KeyError: "['country', 'items_requested_removal'] not in index"` when run against Google's real current-format export — Google's actual columns are `country_name` and `items_requested`, not `country`/`items_requested_removal`. The script would crash on any future refresh attempt using a current Google export, not silently misparse. (2) **Independently, also confirmed live:** the script's hardcoded path (`data/dev/google/google-government-removal-requests.csv`) does not match where the real file actually lives (`data/dev/google/government-removals_<period>_en_v1/google-government-removal-requests.csv`, nested one level deeper under a dated export-bundle folder) — would raise `FileNotFoundError` before the `KeyError` above even fires in a real run. (3) **Resulting coverage gap, confirmed by direct row-level diff:** the real export's Kenya rows span 21 periods (July-Dec 2012 through July-Dec 2025); CLIO's currently-staged `stg.google_transparency_requests` covers only 13 (Jan-June 2019 through Jan-June 2025) — 8 real periods with real data (7 pre-2019, plus the newest, July-Dec 2025) are simply absent from BigQuery. Since the current script cannot successfully run against the current file (per (1)/(2)), whatever CSV originally produced the currently-staged 13-period table must have had different column names and narrower date coverage than this real file — that original file is not preserved or available anywhere in this Codespace or repo, so its provenance is unknown, not merely undocumented. **A fourth, separate finding, smaller in scope:** a full 268-cell value-level diff (67 overlapping rows × 4 numeric fields) between the real file and the 13 staged periods found 267/268 exact matches and one isolated discrepancy — `number_of_requests` for "January - June 2021 / Court Order directed at 3rd party / Web Search / Defamation" is `1` in the real file, `2` in staged BigQuery (the paired `items_requested_removal=7` matches on both sides, so this is a single-cell value discrepancy, not a row-shape or key-parsing problem); root cause undetermined (possibly a Google-side revision between report editions, possibly a different-vintage source file — not resolvable without the original CSV). **Not affected:** `stg.google_transparency_detailed` and its ingest script (`google_transparency_detailed.py`) — that file's real header row still matches the script's rename dict exactly (no column-name drift), and a full 69-row diff against the real historical detailed CSV found 0 mismatches; only its hardcoded path is stale (same nesting issue as (2), but for the `historical/` subfolder), so it would `FileNotFoundError` too, but would parse correctly once pointed at the right file. Found during the second 2026-08-15 relay session while executing the Task B parsing spot-check that a prior 2026-08-15 session (same day) had left open for lack of a real export file (`reports.md`, second 2026-08-15 entry). | **RESOLVED.** `google_transparency_requests.py` now resolves the CSV path via `Path.glob("government-removals_*_en_v1/google-government-removal-requests.csv")` (picking the most-recently-modified match by mtime, since the bundle folder's non-zero-padded dates don't sort chronologically as plain strings) instead of a hardcoded flat path, and renames `country_name`→`country`/`items_requested`→`items_requested_removal` immediately after reading — scoping the fix to the read only, no downstream consumer's column names changed. Re-ran ingestion end to end (`raw.google_transparency_requests` → `load.google_transparency_requests_to_gcs` [requires `BRUIN_ENV=staging`, distinct from the raw asset's dev-only requirement] → `stg.google_transparency_requests`) and re-diffed against the real CSV on `(time_period, requestor, product, reason)`: **85/85 keys match on both sides, 0 orphaned rows, 0 value mismatches across all 4 numeric fields.** The 8-period coverage gap is closed (21 periods now staged, was 13). The previously-disputed cell (Jan-June 2021, Court Order/3rd party/Web Search/Defamation) now reads `number_of_requests=1` in `stg.google_transparency_requests`, matching the real file exactly (was `2`) — resolved as a byproduct of re-ingesting from the correctly-parsed real file; the original `2`'s provenance remains unknown (whatever stale file produced it is not recoverable). Did not touch `google_transparency_detailed.py`/`stg.google_transparency_detailed`, per this fix's explicit scope — that table/script were already confirmed correct. See `reports.md`, 2026-08-15 TD-87 Phase 0 session entry. |

## TD-82 — logged and RESOLVED 2026-08-15, same session (TD-87 Phase 0 Task B, found during design-doc writeup as an unnumbered "TD-88" candidate in a separate planning environment)

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-82 | `Bruin/assets/marts/facts/fact_protocol_blocking_summary.sql` and `Bruin/assets/reporting/mart_pressure_attribution_ooni_daily.sql`, both `COUNTIF(result_state = 'ERROR') AS error_results` | `ERROR` has never been a member of `result_state`'s accepted values (`int.ooni_experiment_results.sql`'s own column check declares `[BLOCKED, OK, DOWN, UNKNOWN]`) — so `error_results` has been silently, permanently zero in both marts since they were written, and neither mart counted `UNKNOWN` at all, meaning their four-way sum (`blocked + ok + down + error`) always undercounted `total_experiment_results` by exactly the `UNKNOWN` population (937,915 rows live, confirmed before the fix). | **RESOLVED.** Both `COUNTIF(result_state = 'ERROR') AS error_results` renamed to `COUNTIF(result_state = 'UNKNOWN') AS unknown_results` (predicate and output column both renamed — `error_results` was itself a misnomer). Checked downstream: `fact_protocol_blocking_summary`'s `error_results` had no downstream reference anywhere (Streamlit's `get_protocol_blocking_summary` in `streamlit/services/marts.py` never selected it). `mart_pressure_attribution_ooni_daily`'s `error_results` was fetched by `get_ooni_corroboration` (`streamlit/services/marts.py`) but never rendered in `streamlit/pages/pressure_attribution.py`'s displayed table — updated that one reference directly to `unknown_results` rather than adding a deprecated alias, since nothing user-facing depended on the old name. Verified live: pre-fix `SUM(error_results) = 0` in both tables (sanity check); post-fix `SUM(unknown_results) = 937,915` in both, and `blocked+ok+down+unknown` now sums exactly to `total_experiment_results` (10,763,408) in both. See `reports.md`, 2026-08-15 TD-87 Phase 0 session entry. |

## TD-83 — logged and RESOLVED 2026-08-16, same session (TD-91 relay, Task A: live verification of a paper audit's survivorship-bias concern)

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-83 | `Bruin/assets/intermediate/int.ooni_measurement_verdicts_candidate.sql`, `ooni_verdict` CASE and `measurement_failure` sourcing | A post-milestone paper audit of TD-87 Phases 0-2 raised a survivorship-bias concern: `stg.ooni_measurements.failure` (extracted by `ooni_raw.py`'s `obj.get("failure")`) is 0% populated across all 1,354,848 rows, every test type, all time — implausible on its face if real OONI failed measurements were being captured. Live-verified against OONI's own API, not assumed: fetched 2 real Kenya measurements OONI's own backend marks `failure:true` (one `signal`, one `dnscheck`, both inside CLIO's ingestion window), pulled their full raw JSON directly from `api.ooni.io`, and confirmed **no top-level `failure` key exists on the raw measurement envelope at all** (consistent with `df-000-base.md`, which defines no such field) — `obj.get("failure")` was always structurally going to return NULL, not a wrong-path bug with an available fix elsewhere. Both sampled measurements were then confirmed to exist in CLIO's own `stg.ooni_measurements` (report_id-matched) with full raw JSON fidelity — **ruling out survivorship bias**: nothing is silently dropped before landing; every real per-test failure signal already lives in `stg.ooni_measurement_summary`'s test-specific columns from TD-87 Phase 1. The one real, generic, OONI-attached "the probe itself broke" field (`test_keys.failure`/`test_keys.failed_operation`) is structurally present across all 6 test types but populated live ONLY for `psiphon` (863/50,673 rows) — confirmed by direct query, not assumed to generalize. `psiphon_failure`'s real values ("clientlib: tunnel establishment timeout", "StartTunnel called multiple times", etc.) are probe/tunnel-level breakage, matching OONI's own `FAILED` semantics precisely, not `ANOMALOUS` — but Phase 2's original design folded these rows into `ANOMALOUS` via `test_anomaly_flag`, since `measurement_failure` (the intended `FAILED` trigger) was always NULL. | **RESOLVED.** `ooni_verdict`'s `FAILED` condition now also checks `psiphon_failure IS NOT NULL`; `psiphon`'s `test_anomaly_flag` arm changed to always `FALSE` (psiphon has no other anomaly-detection field distinct from probe breakage). Verified live, full before/after `ooni_verdict` distribution by `test_name`: `psiphon` 863 rows moved `ANOMALOUS`→`FAILED` exactly, `dnscheck`/`telegram`/`whatsapp`/`tor` byte-for-byte unchanged. `measurement_failure` itself stays permanently NULL (genuinely dead, not fixed — there is no single alternate path to redirect it to, since the real signal is inherently per-test, already captured elsewhere). See `reports.md`'s 2026-08-16 TD-91 session entry. |

## TD-84 — logged and RESOLVED 2026-08-16, same session (TD-91 relay, Task B: verify the probe-version discard rule is actually applied)

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-84 | `Bruin/assets/intermediate/int.ooni_measurement_verdicts_candidate.sql`, `ooni_verdict` CASE | The same paper audit asked whether `marts.dim_ooni_probe_version_accuracy` (TD-87 Phase 2, the vendored Signal known-bad-probe-version discard rule) was actually wired into classification, or built-but-never-joined — the exact failure shape this project hit once before with `dim_censorship_confidence` (ADR-0001/TD-05). Checked directly: the `LEFT JOIN` to the dimension table on the corrected compound `(test_name, test_version)` key already existed and `probe_accuracy_gate` was correctly computed — the ADR-0001/TD-05 shape had NOT recurred. What was genuinely still open: `probe_accuracy_gate`'s result was computed and reported but never consumed by `ooni_verdict` itself, an explicit, deliberately-deferred decision from the original Phase 2 design ("a decision for whichever later phase actually uses this gate"). | **RESOLVED (decision made and applied).** Chose to EXCLUDE (`ooni_verdict` → `NULL`) rather than flag-alongside-a-computed-verdict for `probe_accuracy_gate = 'DISCARDED_BAD_PROBE_VERSION'` rows — reasoning: the vendored rule mirrors OONI's own real backend behavior (`scores.accuracy = 0.0` is applied BEFORE any anomaly detection is attempted, a discard-before-scoring rule, not a mere confidence markdown), so computing `ANOMALOUS`/`OK` from data OONI's own backend wouldn't trust enough to score would manufacture a classification with no real epistemic backing. `CONFIRMED` still wins ahead of this check (fingerprint matching is independent of behavioral scoring). Verified live: `signal`'s `ANOMALOUS` count moved from 15,679 (31.1%) to 7,601 (15.1%), with exactly 8,078 rows moving to `NULL`/`DISCARDED_BAD_PROBE_VERSION` — matching `test_version='0.2.0'`'s population exactly. Corroborating detail found during verification: 100% of `test_version='0.2.0'` signal rows are `signal_backend_status='blocked'` (0 are `'ok'`) — independent confirmation this vendored rule (`ooni/probe#2344`) is excluding exactly the false-positive-prone population it was built to catch, not an arbitrary slice. See `reports.md`'s 2026-08-16 TD-91 session entry. |

## TD-85 — logged and RESOLVED 2026-08-16, same session (TD-91 relay, Task C: the CONFIRMED guard's write-order exposure)

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-85 | `Bruin/assets/intermediate/int.ooni_measurement_verdicts_confirmed_guard.sql` (wiring), plus new `int.ooni_measurement_verdicts_candidate.sql` | TD-87 Phase 2's three-layer `CONFIRMED` guard originally ran its DAG-level check (layer 2) AFTER `int.ooni_measurement_verdicts` had already materialized (`depends: [int.ooni_measurement_verdicts]`), by deliberate, documented necessity at the time (checking the guarded asset's own output can't be a pre-write dependency of that same asset without a DAG cycle). The paper audit correctly flagged the real cost of that choice: `create+replace` does not roll back a failed run, so a violating table would stay live and queryable (by Streamlit or any direct query) for the entire window between the violation landing and a fixed rerun succeeding — exactly the exposure ACLED's own check-before-write precondition guard (`intelligence.acled_pressure_regimes_precondition_check`) exists to close. Investigated the standard fix (a `_candidate`/publish split) and confirmed it was genuinely cheap before building it, not assumed: `int.ooni_measurement_verdicts` and its upstream `stg.ooni_measurement_summary` are both small, scalar-only tables (222 MiB / 197 MiB, live-measured via `__TABLES__`) — nothing like the 63 GiB raw-JSON-carrying `stg.ooni_measurements` this project's 2026-07-06 cost audit flagged as the pipeline's actual expensive-rebuild risk. | **RESOLVED — full restructure applied (the cheap path held up in practice, not just in theory).** New `int.ooni_measurement_verdicts_candidate.sql` now holds all real computation (previously in `int.ooni_measurement_verdicts.sql` directly). The guard now `depends: [int.ooni_measurement_verdicts_candidate]` and checks that table. The published `int.ooni_measurement_verdicts.sql` is now a trivial `SELECT * FROM candidate` with `depends: [candidate, guard]` — Bruin's DAG will not refresh it until the guard has already passed against the candidate, so a violation blocks the publish step entirely and the previously-published (good) data stays live instead of being silently replaced. This now genuinely matches ACLED's own precondition-check shape (a real, separate upstream table checked before the published asset materializes), resolving the "deviation from the ACLED precedent" this project's own prior session had explicitly, deliberately accepted as the best available option at the time. Verified live: `bruin run --downstream` executes candidate → guard → publish in correct order automatically; published row count matches the candidate exactly (1,354,848 = 1,354,848); 0 guard violations; whole-pipeline `bruin validate` clean at 61 assets. `tests/test_ooni_confirmed_guard.py` (layer 3, the static structural check) repointed to the candidate file, where the real `CONFIRMED`-producing logic now lives. See `reports.md`'s 2026-08-16 TD-91 session entry. |

## TD-92 — logged 2026-08-15, OPEN (found during the weekly OONI aggregation build, ADR-0011)

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-92 | `docs/02-architecture/decision-log.md` (2026-08-16 TD-91 narrative entry) and `technical-debt-inventory.md`'s own TD-84 row | Both entries state Signal's `ooni_verdict='ANOMALOUS'` rate as "15.1% (7,601/50,454)" after the TD-84 exclude-not-flag fix. That denominator (50,454) is Signal's full row count *including* the 8,078 rows the same fix just excluded from the numerator (`DISCARDED_BAD_PROBE_VERSION`, `ooni_verdict = NULL`). Re-derived live while building `features.ooni_weekly_signals` (ADR-0011): the internally consistent "scored rate" — excluding discarded rows from both numerator and denominator, which is what "excluded from scoring" should mean — is **17.9% (7,601/42,376)**, not 15.1%. This is a real inconsistency in how a live-verified number was reported (mixing an excluded-numerator with an included-denominator), not a live table or dashboard defect — no materialized column or Streamlit page currently reads either percentage; both only exist as prose in these two tracked docs. | **OPEN, not fixed here** — per this session's own rule (report unrelated bugs found mid-build, don't fix them silently). A future session should decide whether to correct the two prose citations above to 17.9% (with a note on why 15.1% was wrong) or add an explicit "% of all rows including discards" framing if that denominator was actually the intended meaning — either is a two-line prose edit, not a code change, since `int.ooni_measurement_verdicts`'s actual computed values were never wrong; only the derived percentage narrated in these two docs was. **Update (2026-08-15, TD-93 session): this row's own numbers are now further stale, not just wrong-denominator** — TD-93's fix moved 4,682 of the 7,601 ANOMALOUS rows this row's percentages were computed from to `FAILED`, so both 15.1% and 17.9% now describe a population (7,601 ANOMALOUS) that no longer exists in the live table (now 2,919). Still not fixed here, same reasoning — a future prose-cleanup pass should address both issues (denominator AND population) together. |

## TD-93 — logged and RESOLVED 2026-08-15, same session (external-validation follow-up to the 2026-08-15 "does CLIO's own OONI verdict layer agree with OONI's own real classification?" investigation)

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-93 | `Bruin/assets/intermediate/int.ooni_measurement_verdicts_candidate.sql` (`ooni_verdict` CASE), `Bruin/assets/staging/stg.ooni_measurement_summary.sql` (new column) | A matched-pair external-validation check (66 measurements, OONI's own `/api/v1/measurements`, matched by `report_id`) found Signal's `test_anomaly_flag` — keyed only on `signal_backend_status = 'blocked'` — overcounted `ANOMALOUS`: 78.8% agreement with OONI's own real `anomaly` boolean, with all disagreement (after separating out the already-understood TD-84 bad-probe-version population) tracing to NXDOMAIN failures against two hostnames outside OONI's own current spec (`ts-029-signal.md`, fetched live) — `textsecure-service.whispersystems.org` (Signal's pre-rebrand legacy backend domain) and `api.directory.signal.org`. Live per-month, per-hostname data confirmed both are structurally dead (the first shows a clean one-time retirement signature, 100% success through 2023-10 then 100% failure every month from 2023-12 through 2025-06; the second is 100% dead across CLIO's entire ingestion window, zero successes ever), while OONI's own four currently-tested endpoints (`cdsi`/`chat`/`sfu.voip`/`storage.signal.org`) resolve successfully >99% of the time throughout. Scaled to full history: this accounted for 4,753 of CLIO's 7,601 historical Signal `ANOMALOUS` rows (62.5%) under a naive per-value read, though the eventual fix (below) is narrower and more precise than a blanket value match — an earlier candidate (excluding ALL failures against these two hostnames, not just NXDOMAIN) was tested against the same 66-measurement sample and found WRONG (60.6% agreement, worse than doing nothing) before the final, narrower rule was reached: OONI's own backend still trusts `generic_timeout_error`/`connection_reset`/`network_unreachable` against these same legacy hostnames as real anomaly signals; only NXDOMAIN (a much stronger "this domain has no DNS records" signal) is untrustworthy noise. | **RESOLVED.** Added `signal_legacy_endpoint_nxdomain_only` to `stg.ooni_measurement_summary.sql` — TRUE only when `signal_backend_failure = 'dns_nxdomain_error'` AND none of OONI's four current-spec endpoints' own queries also show `dns_nxdomain_error` in the same measurement (so a genuine current-endpoint NXDOMAIN still correctly reaches `ANOMALOUS`). Consumed in `int.ooni_measurement_verdicts_candidate.sql`'s `ooni_verdict` CASE ahead of `test_anomaly_flag`, same precedence pattern as `psiphon_failure` (TD-91) — routes to `FAILED`, matching OONI's own literal terminal classification for these rows (verified: not a pre-scoring discard the way TD-84's rule is, since OONI's backend does attempt and complete scoring for these, landing on `failure=true`, not a discard). Re-tested against the same 66-measurement matched-pair sample with the final rule: **100% substantive agreement** with OONI's real classification (up from 78.8%). Full-history impact: Signal `ANOMALOUS` 7,601 → 2,919 (-4,682, -61.6%); `FAILED` 0 → 4,682 (new); `OK` (34,775) and `NULL`/discarded (8,078) both unchanged. Live acceptance check re-run (fresh `/api/v1/aggregation` fetch, not cached): mean absolute daily-rate difference between CLIO and OONI's real anomaly rate across the full 2024-05-01–2024-07-31 window dropped to 0.61 percentage points (max 9.76pp on any single day), from wild pre-fix divergence (CLIO showing 20-68% daily rates in May against OONI's real ~0%); the real anomaly-peak day (2024-06-25) now matches OONI exactly (11.05% both sides). `intelligence.ooni_acled_lag_correlation`'s one `MODERATE_RELATIONSHIP` row in the Finance Bill 2024 window (signal/ANOMALOUS/lag+1/week 2024-07-13, `corr=-0.578`, flagged as a likely artifact in ADR-0011) is now gone entirely — the window is 100% `WEAK_OR_NO_RELATIONSHIP`/`ZERO_VARIANCE_WINDOW`, matching `protocol_repression_correlation_mart`'s own Finance Bill finding for the first time. Golden fixture re-frozen (`tests/fixtures/ooni_weekly_golden/finance_bill_2024.json`'s `ooni_verdict_weekly` key) after reporting the full before/after delta (window total: 1,844 → 1,268 ANOMALOUS, -31.2%; `dnscheck`/`psiphon`/`telegram`/`whatsapp` byte-for-byte unchanged, confirmed before re-freezing, not assumed) — see `tests/test_ooni_weekly_aggregation_golden.py`'s own docstring for the itemized weekly deltas. Verified: `bruin validate` 63/63 clean; offline suite 19 passed/17 skipped (unchanged); live suite 36 passed (unchanged count, different content); `dnscheck`/`telegram`/`whatsapp`/`psiphon`/`tor` `ooni_verdict` distributions confirmed byte-for-byte unchanged. See `reports.md`'s 2026-08-15 TD-93 entry for the full methodology, including the failed intermediate rule that was tested and rejected before the final one. |

## TD-95/TD-96 — logged 2026-08-17 by the Fable UX/IA audit (`docs/02-architecture/streamlit-ux-redesign-audit-2026-08-17.md`), both RESOLVED 2026-08-17 (build session prior to this one; backfilled into this inventory now since neither had a row here yet)

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-95 (Phase 0) | `streamlit/pages/{asn_behavioral_intelligence,suppression_event_explorer,finance_bill_2024_incident_report,protocol_intelligence}.py`, `streamlit/pages/methodology_statistical_guardrails.py` | Every OONI-derived finding was attributed at Kenya-country grain from probe location alone, with dashboard copy in places overclaiming a confirmed Kenya-based block where the real evidence is "unreachable from a Kenyan vantage point, network path position unknown." | **RESOLVED (Phase 0 only — copy/disclosure, not the schema half).** Overclaiming language reframed on the four pages found to have it; a new `st.warning()` disclosure added to the Methodology page's "Data Sources & Known Limitations" section. **Phase 0's own schema half (a `path_attribution_state` column on the relevant marts) remains open, deliberately deferred** — a rematerialization-scale change, bigger than a copy fix. |
| TD-96 | `streamlit/core/constants.py`, `streamlit/core/theme.py`, `streamlit/components/status.py` | Three Tier-1 dashboard color/contrast bugs found by a 2026-08-15 Opus design pass and independently re-verified against live code with computed WCAG contrast ratios: `CORRELATION_STATES["WEAK_OR_NO_RELATIONSHIP"]` was green (falsely reassuring, since real correlation data is almost always weak); `REGIME_STATES["CRISIS"]`/`PRESSURE_LEVELS["SEVERE"]` shared a color measuring 2.95:1 contrast against the app background (below the 3:1 WCAG floor) and was non-monotonic (darker than two less-severe states in the same ramp); all seven `*_color()` helpers silently returned a gray fallback on any unmapped value with no maintainer-visible signal. | **RESOLVED.** `WEAK_OR_NO_RELATIONSHIP` → `#6B8CAE` (muted steel-blue, 4.76:1 vs. bg, visibly distinct from the existing `#6B7280` "no data" gray). `CRISIS`/`SEVERE` → `#FF3B5C` (5.58:1 vs. bg, now above both `ESCALATION` and `CONFLICT`, fixing the monotonicity bug; four brighter same-hue reds were tried and rejected, all landing within ~1.06–1.34:1 contrast of `ESCALATION` — hue was shifted toward crimson instead, paired with a new `⚠` icon marker in `components/status.py`'s `_SEVERITY_ICONS` since hue alone isn't reliable for colorblind readers). All seven color helpers now log a `WARNING` on fallback via a shared `_color_or_fallback()` wrapper, visible behavior unchanged. Verified numerically (WCAG contrast formula) and visually (PIL-rendered swatches — no browser tooling available in this environment) in the session that resolved this; re-verified live via `AppTest` against real BigQuery data in this session with zero exceptions across all 8 pages. |

## TD-97 — logged and RESOLVED 2026-08-17, same session (build session following the 2026-08-17 Fable UX/IA audit's R1 recommendation)

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-97 | `streamlit/services/marts.py` (new `get_ooni_weekly_signals()`), `streamlit/core/constants.py` (new `FEATURES` dataset constant), `streamlit/pages/national_stress_observatory.py` (new "OONI Weekly Signal vs. ACLED Regime" section) | `features.ooni_weekly_signals` and `intelligence.ooni_acled_lag_correlation` (built 2026-08-15, ADR-0011) were real, populated, current tables with zero downstream consumers anywhere in the pipeline — confirmed live (5,500 and 550 rows respectively at audit time) via direct grep and BigQuery queries, not inferred. ADR-0011 deferred wiring by explicit design, pending project-owner review of what the numbers actually look like; that review is what prompted this fix. | **RESOLVED, R1 scope only (the `ooni_weekly_signals` descriptive chart) — R3 (a display for `ooni_acled_lag_correlation`, the harder null-result-design half) deliberately not built, per the audit's own explicit design-only deferral.** Added `get_ooni_weekly_signals(start_date, end_date)` querying `features.ooni_weekly_signals` at its real `week_start_date x test_name` grain. New section on National Stress Observatory, nested inside the existing ACLED regime-classification block: a per-protocol selector plots `anomalous_rate` and `blocked_rate` as two separate lines (never merged into one score, per ADR-0011's explicit non-merge rule), overlaid with Saturday-anchored regime-color bands (`add_vrect`, deduped from the page's daily-broadcast `regime_valid` frame to one band per real week — the initial naive approach of one vrect per broadcast day would have stacked semi-transparent opacity up to 7x per week and rendered every week visibly darker than intended; caught before shipping). A sparse/low-sample count caption and a static `WEAK_OR_NO_RELATIONSHIP`-colored badge (reusing TD-96's `correlation_color()`) cite ADR-0011's own documented historical finding (correlations weak almost everywhere, the one `MODERATE_RELATIONSHIP` week resolved as a TD-93 artifact) rather than recomputing a live number against the lag-correlation table — a deliberate judgment call to stay inside R1's scope and avoid a new date-alignment bug in logic the audit explicitly deferred to R3. Verified live via `AppTest`: real, varying data confirmed for `signal`/`telegram`/`whatsapp`/`dnscheck` (e.g. `signal` std-dev of `anomalous_rate` = 0.197 across 109 weeks) and a correctly-flat line for `psiphon` (zero variance in both series across the entire window — itself a genuine finding per ADR-0011, not a bug); badge and sparse-week caption both confirmed rendering with real numbers, zero exceptions. |

## TD-98 — logged and RESOLVED 2026-08-17, same session (build session following the audit's R2 recommendation)

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-98 | `streamlit/pages/protocol_repression_correlation_engine.py` (canonical, merged), `streamlit/pages/suppression_event_explorer.py` (deleted), `streamlit/app.py`, `streamlit/pages/welcome.py`, `README.md`, `docs/02-architecture/data-modelling.md` | The former Protocol ↔ Repression Correlation Engine and Suppression Event Explorer pages both queried `reporting.protocol_repression_correlation_mart` (confirmed directly in `services/marts.py`'s query bodies, not just declared headers) and told substantially the same weak-correlation story in two separate nav entries — a visitor had to know in advance which of two near-duplicate pages had the view they wanted. | **RESOLVED.** Read both pages in full before merging (per the audit's own instruction to re-verify rather than assume) and confirmed a real, non-overlapping difference worth preserving: the Correlation Engine is protocol-centric (one protocol drilled down over time), Suppression Event Explorer was date-centric (all protocols snapshotted on one date) — not identical content, just the same underlying mart viewed two ways. Merged as two tabs on the retained `protocol_repression_correlation_engine.py`: "Protocol Drill-Down" (original Correlation Engine content, unchanged) and "Date Snapshot" (original Suppression Event Explorer content, unchanged, including its TD-95 vantage-point caveat, carried over rather than silently dropped). Both underlying queries (`get_protocol_correlation`, `get_event_explorer`) kept as-is and unmerged, to avoid changing either one's column set or behavior — only page-level presentation was consolidated. `suppression_event_explorer.py` deleted (not left as dead unregistered scaffolding); its `st.Page()` entry removed from `app.py`'s navigation (9 pages → 8); `welcome.py`'s page-guide row merged into the Correlation Engine's own row; `README.md`'s page list, page-count ("8 pages" → "7 pages"), and directory-tree comment updated; `data-modelling.md`'s mart-to-page table updated. Checked for other cross-references (`grep -rln` across `streamlit/`, `docs/02-architecture/`, `docs/03-development/`, `README.md`) before finalizing — none left pointing at a page that no longer exists. Verified live via `AppTest`: both tabs render with zero exceptions, both selectors (`corr_engine_protocol`, `corr_engine_event_date`) interact correctly, the TD-95 caption and `attribution_footer` both confirmed present in the merged page's rendered output. |

## TD-99 — logged and RESOLVED 2026-08-17, same session (five smaller items bundled from the same audit, F3/F4/F5/F6/F7)

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-99 | `streamlit/components/charts.py`, `streamlit/components/tables.py` (deleted), `streamlit/core/layout.py` (deleted), `streamlit/core/constants.py` (`PAGES` list, deleted), `streamlit/core/filters.py`, `streamlit/pages/{asn_behavioral_intelligence,methodology_statistical_guardrails}.py`, `streamlit/pages/asn_behavioral_intelligence.py` (labels) | Five small, independent findings from the same audit: (F3) `empty_figure()`/`add_threshold_lines()`/`render_table()`/`latest_by_group()` had zero adopters anywhere in `streamlit/pages/`; (F4) `core/layout.py` was an empty stub, never imported; (F5) `core/constants.py`'s `PAGES` list had zero consumers and was stale (still listed "Protocol Regime Monitor," retired under TD-16); (F6) the sidebar date-filter rendered identically on every page, including two (`asn_behavioral_intelligence.py`, `methodology_statistical_guardrails.py`) whose data has no date dimension, with no feedback that the control did nothing; (F7) ASN Behavioral Intelligence's KPI/chart labels ("Maturity Signal," "Behavior Priority," raw `behavioral_class` values) read as copy-pasted column names rather than translated prose, inconsistent with the rest of the page and the rest of the layer. | **RESOLVED, all five.** (F3) `add_threshold_lines()` adopted at its two real call sites (`national_stress_observatory.py`'s pressure-level thresholds, the merged Correlation Engine's rolling-correlation thresholds) — required extending its signature with optional per-line `labels`/`opacity` first, since the original single-opacity/no-label signature would have silently dropped the existing annotation text at both call sites (checked before adopting, not assumed compatible); `empty_figure()`/`render_table()`/`latest_by_group()` had no clean 1:1 adoption site without a larger empty-state UX redesign (out of proportion for a "minor cleanup" item) and were deleted along with `components/tables.py` outright, rather than left as unused-but-maintained scaffolding. (F4) `core/layout.py` deleted (confirmed zero imports first). (F5) `PAGES` deleted from `core/constants.py`, replaced with a one-line comment pointing at `app.py`'s `st.navigation()` call as the actual, already-correct source of truth. (F6) `core/filters.py`'s `render_sidebar()` gained an optional `show_date_filter` parameter (default `True`, unchanged behavior everywhere else); the two affected pages now pass `show_date_filter=False` and show a one-line caption explaining the filter doesn't apply, instead of two dead date-input widgets. (F7) KPI labels and chart-trace labels on the ASN page translated to plain English ("Behavioral Priority Score," "Evidence Reliability," "Evidence Maturity," "Measurement Coverage," "Blocking Signal Intensity," "Behavioral Pattern"); the ranking-chart caption rewritten in plain language. Verified: `python3 -m py_compile` across every touched file; `grep` confirmed zero remaining references to any deleted symbol/file; live via `AppTest` across all 8 remaining pages plus `app.py` itself, zero exceptions; the repo's one test file (`tests/test_contracts.py`) still passes (4/4). |

## TD-100 — logged 2026-08-17, RESOLVED (ADR-0013 build)

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-100 | `Bruin/scripts/agreement_check/`, `.github/workflows/ooni-agreement-check.yml`, `audit.ooni_agreement_runs` | No automated, standing mechanism existed to catch a CLIO figure diverging from OONI's own live, published data — every real catch to date (TD-68, TD-93, TD-97) was accidental (a Slack exchange, a one-off matched-pair audit, a human looking closely at one chart), never a standing process. | **RESOLVED.** Built per ADR-0013: a fixed known-answer panel (144 entries across signal/whatsapp/telegram/psiphon) plus a rotating, category-stratified sample, checked against OONI's live `/api/v1/measurements` API, `workflow_dispatch`-only. Live-verified end to end (2026-08-20 infrastructure; 2026-08-30 panel/allowlist population). The weekly schedule was deliberately never enabled — see ADR-0013's Status line for the full reasoning (CLIO is pilot-stage on a historical backfill, not continuously ingesting; a fixed timer would mostly re-confirm an unchanging result against a rate-limited external API). |

## TD-101 — logged 2026-08-20 during ADR-0013's baseline audits, RESOLVED 2026-08-22

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-101 | `Bruin/assets/intermediate/int.ooni_measurement_verdicts_candidate.sql` (dnscheck, telegram, whatsapp branches) | A first live baseline (dnscheck) found the test type structurally could never produce a `FAILED` verdict (20/20 disagreement in the first live sample). The gap generalized to `telegram` and `whatsapp` too — all three test types were `UNCHECKED` against OONI's real classification per ADR-0013's oracle-status taxonomy. | **RESOLVED, all three.** Dnscheck fixed via a narrow bogon carve-out (bogon-driven failures stay `ANOMALOUS`, everything else routes to `FAILED`) — real row movement 4,889, Finance Bill 2024 fixture re-frozen (dnscheck ANOMALOUS 493→0, an artifact correction; no shipped deliverable ever cited the old figure). Telegram investigated and closed with **zero code changes** — no real FAILED-worthy population exists for Kenya, extended to 47/47 matched-pair agreement across every rare boundary condition. Whatsapp's fix folded into TD-105 below. |

## TD-102 — logged 2026-08-20 during ADR-0013's baseline audits, RESOLVED 2026-08-22

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-102 | `stg.ooni_measurement_summary.sql`, `int.ooni_measurement_verdicts_candidate.sql` (signal branch) | Signal's `ANOMALOUS`-vs-OONI-`FAILED` disagreement (already partly fixed once via TD-93) had a second, live, unresolved shape at real scale: 50.5% disagreement, splitting cleanly by probe `test_version`. | **RESOLVED.** `test_version=0.2.3` traced to a one-time OONI data-quality patch (a dead Signal endpoint, documented in OONI's own devops runbook) and discarded accordingly. `test_version=0.2.2` traced to a separate, permanent defect (a different dead endpoint) — built, but the first attempt was too aggressive (discarding 2,200 rows version-wide when only 415 were actually flagged blocked, catching 1,785 rows of genuine "not blocked" evidence); caught by the build session's own second look and corrected in a follow-up to discard only the actually-blocked subset. Both shapes independently confirmed against OONI's live API as an exact match. |

## TD-103 — logged 2026-08-20 during ADR-0013's dnscheck baseline audit, RESOLVED (root-caused; informs design, not a code fix)

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-103 | `docs/02-architecture/adr/0013-ooni-agreement-check-external-reference-oracle-guardrail.md` (oracle-status taxonomy) | OONI's own `failure`/`anomaly`/`confirmed` fields show zero variance for `dnscheck` specifically, confirmed globally (200 measurements, 15 countries) — the oracle doesn't cover this test type at all. Root cause: `dnscheck` has no entry in OONI's own fastpath scorer's dispatch chain; it falls through to an "unsupported test" branch that hardcodes `accuracy=0.0`. | **RESOLVED as a design finding, not a code bug.** Adopted project-wide: an oracle-status taxonomy per test type (VALIDATED / DEGENERATE-EXEMPT / UNCHECKED), recorded before spending a baseline audit on a test type. `dnscheck` is marked `DEGENERATE-EXEMPT` and routed to its own internal validation path (TD-47/TD-101) instead of ADR-0013's external guardrail. |

## TD-104 — logged 2026-08-20 during TD-101's dnscheck build, RESOLVED (informational)

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-104 | OONI's own dnscheck target configuration (external to this repo) | Three dead or typo'd hostnames found in OONI's own dnscheck target list, accounting for a large share of one bootstrap-failure value (`dns_nxdomain_error`, 33.1% of the distribution). | **RESOLVED (informational, no CLIO-side change).** A probe/target-list artifact on OONI's side, not a CLIO bug — cleared and documented before shipping TD-101's dnscheck fix, so it wasn't mistaken for a CLIO-side classification gap. |

## TD-105 — logged 2026-08-20/21 during ADR-0013's whatsapp baseline audit, RESOLVED 2026-08-22, three-bug scope

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-105 | `stg.ooni_measurement_summary.sql` (`whatsapp_web_accessible` recomputation), `int.ooni_measurement_verdicts_candidate.sql` (whatsapp branch) | Whatsapp's `ANOMALOUS` classification systematically false-positived, accounting for 86.5% of its historical ANOMALOUS volume at its peak. | **RESOLVED, three-bug scope.** Bug (a): an obsolete probe version's (0.9.0) own artifact — a plain-HTTP-leg check OONI's own backend never reads — fixed by recomputing `whatsapp_web_accessible` from `$.requests[]`'s own `failure IS NULL` instead of trusting the raw probe field; ANOMALOUS dropped 9,350→1,349. Bug (b): `whatsapp_endpoints_blocked_count` — characterized, advisor-consulted (OONI's own scorer disables this exact rule), verified (0/81 sole-driver agreement), built as a clean deletion; ANOMALOUS dropped 1,349→835. Bug (c) candidate (registration-server) investigated and found to be mostly a real, correctly-working signal at the time — dropped from this fix's scope, checked twice, both directions (later revisited and fixed separately as TD-126). Combined: whatsapp's ANOMALOUS rate dropped 9,350→835 (-91.1%), every step characterized and verified before being built. |

## TD-106 — logged 2026-08-21 during TD-101's telegram investigation, RESOLVED (informational)

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-106 | OONI's own telegram target configuration (external to this repo) | Telegram carries dead/placeholder IPv6 and TCP-connect targets in its own test configuration, discovered as a parallel finding to TD-104 during telegram's investigation. | **RESOLVED (informational, no CLIO-side change).** Confirmed not affecting any live CLIO classification — documented alongside TD-104 rather than investigated further. |

## TD-107 — logged 2026-08-31, IN PROGRESS (this documentation-sync effort)

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-107 | `docs/02-architecture/decision-log.md`, `technical-debt-inventory.md` (this file), `implementation-roadmap.md`, `CLAUDE.md`, `docs/02-architecture/adr/0013-*.md` | Two full weeks of real, completed engineering work (ADR-0013 in full, TD-100 through TD-127) existed only in a separate private planning workspace and had never reached this repository's own tracked docs — a real transparency-claim risk given this project's public-facing grant application invites reviewers to check claims against this repo, and names `technical-debt-inventory.md` directly. | **RESOLVED, both phases complete.** Phase 1 (2026-08-31): ADR-0013 in full, TD-100 through TD-107, TD-116 through TD-127, plus the `implementation-roadmap.md` "Step -1" section and matching `decision-log.md`/`CLAUDE.md` updates. **Phase 2 (2026-08-31, same day): TD-108 through TD-115** synced below, plus ADR-0013's panel-transparency subsection and `README.md`'s Validation History paragraph. Both phases' source material originated in a separate private planning workspace and have now been fully ported into this repository's own tracked docs. |

## TD-108 through TD-115 — items originating from review of externally-maintained documents

The following three entries (TD-108, TD-110, TD-115) originated in a review of two public-facing PDF/DOCX deliverables maintained outside this repository, in a separate planning workspace. They are recorded here — stripped of file paths and document-editing detail, since those files don't exist in this repository's tree — for the pipeline-side findings, corrections, and cross-references they produced. TD-116, TD-112, TD-113, and TD-114 (already tracked elsewhere in this file) were all discovered as byproducts of this review.

### TD-108 — stale TLS-correlation claim and a subsequent TLS-in-window characterization

RESOLVED. An external deliverable asserted a TLS-driven correlation claim for a specific Kenyan ASN that had already been found not to hold; the claim was removed from that document. A follow-up question — whether a separate 82-row concentration of TLS `BLOCKED` signals inside the Finance Bill 2024 window (2024-06-15 to 2024-07-15) was itself a real signal — was characterized directly against this repository's live tables and found **not interference-shaped**: explained by (a) a testing-volume rise during the window rather than a real rate increase, (b) 37.8% of the 82 rows carrying an already-known-bad probe version (`signal`/`test_version 0.2.0`, flagged `is_known_bad_version=true` in `marts.dim_ooni_probe_version_accuracy`) that is filtered in one consumer pipeline (`int.ooni_measurement_verdicts`, per TD-102) but not in this one (`int.ooni_experiment_results.sql`'s `tls` CTE), and (c) the single statistically-valid elevated row (446 valid tests, one row at z=3.02) matching the expected false-positive rate under multiple comparisons, not an outlier. The probe-version filtering gap this surfaced is tracked separately as **TD-116** (already in this file).

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-108 | `Bruin/assets/intermediate/int.ooni_experiment_results.sql` (`tls` CTE); `features/protocol_daily_signals.sql`; `intelligence/protocol_signal_regimes.sql` | Characterization of an 82-row TLS BLOCKED concentration inside a specific 31-day window; found not interference-shaped, explained by testing volume, one uncontrolled probe version, and expected false-positive rate. | Closed. See TD-116 for the actionable pipeline-scope-gap follow-up. |

### TD-109 — `features.ooni_weekly_signals` data horizon (informational, no action)

`features.ooni_weekly_signals`'s data horizon ends 2025-06-28. This matches this project's declared scope — historical analytics over a fixed window, not live/continuous monitoring — rather than indicating staleness. No pipeline action needed.

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-109 | `features.ooni_weekly_signals` and its consumers | Data horizon ends 2025-06-28, consistent with this project's declared analysis window, not a staleness gap. | None. Informational only. One low-priority open question: whether the National Stress Observatory dashboard panel makes the analysis window visually obvious on its own (an axis range or caption), independent of prose elsewhere describing the scope. |

### TD-110 — DNS-layer and protocol-stress claims in an external deliverable, re-verified against live data

RESOLVED. Two claims in an external public-facing deliverable (zero DNS-layer interference signals in a specific window; a flat protocol-stress reading in the same window) were re-verified directly against this repository's live tables across several rounds of characterization. The first claim was confirmed cleanly. The second required real correction: an initial read of "z-score exactly 0.0 every day" as reassuring was wrong — a `LEAST(1.0, (ABS(z)/4.0) * sample_quality_score * coverage_score)`-style zero-effect-size score is mechanically indistinguishable from a genuinely flat input, and the underlying DNS `BLOCKED` verdict in this table reads zero in roughly two-thirds of all months in its history (see **TD-113**), which means a single month reading zero carries little evidential weight on its own regardless of whether the number itself is computed correctly (it is). Two design gaps were found as byproducts and are tracked separately: the `ZERO_VARIANCE` gate's inability to fire on a flat-zero series (**TD-112**), and a scale-conflation issue in `regime_confidence` (**TD-114**).

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-110 | `Bruin/assets/intermediate/int.ooni_experiment_results.sql`; `Bruin/assets/features/protocol_daily_signals.sql`; `Bruin/assets/intelligence/protocol_signal_regimes.sql` | Two claims about DNS-layer and protocol-stress behavior in a specific window, re-verified against live data. First confirmed clean. Second required reframing away from an affirmative "zero blocking" claim toward a disclosed base-rate limitation, given the detector's bogon-only scope and a two-thirds-of-months zero base rate. | Closed as a verification exercise. TD-112, TD-113, TD-114 (this file) are the actionable pipeline-side follow-ups it produced. |

### TD-111 — evidence/measurement lineage mapping (research idea, not built)

A proposed methodological instrument for making assumption-entry points inspectable per published claim (e.g., "this aggregation assumes event types are severity-interchangeable"). Not a bug — a research idea, reviewed and scoped down substantially from its original proposal. Adopted scope: one register entry, by hand, for the highest-risk existing published number — the six-term `mart_political_stress_windows` "Current Pressure" score (four uncited weights) — as a falsification test. If that single entry surfaces nothing not already known, the idea stops there rather than expanding into a larger framework.

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-111 | Proposed: one new file, candidate location `docs/07-governance/`, scope TBD pending the falsification test | Research idea for an instance-level (not type-level) assumption/lineage register, supplementing rather than replacing existing methodology documentation. | Not started. Next and only currently-approved step: write one register entry for the "Current Pressure" score and evaluate against the stopping condition before committing to any further scope. |

### TD-112 — `ZERO_VARIANCE` gate cannot fire for a genuinely flat-zero input series

Open. `Bruin/assets/features/protocol_daily_signals.sql`'s `zero_variance_flag` (around lines 325-329) requires `ABS(signal_rate - baseline_signal_rate_30d) > 1e-9` as part of its firing condition — a guard against a single-point deviation producing a spuriously large z-score when historical variance is near zero. For a genuinely flat-zero series (today's rate equals a flat-zero baseline exactly), that clause can never be true, so the flag reads `FALSE` regardless of how degenerate the underlying variance actually is. This is a naming/design mismatch: `intelligence/protocol_signal_regimes.sql` consumes this flag's `FALSE` value as "variance here is adequate to trust this row's z-score," which is not what the flag actually verifies. Not yet confirmed to have corrupted any specific published number (that question is tracked under TD-110, closed above); this entry tracks the gate's own logic independent of that.

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-112 | `Bruin/assets/features/protocol_daily_signals.sql:325-329` (`zero_variance_flag`); consumed by `Bruin/assets/intelligence/protocol_signal_regimes.sql`'s `statistically_valid` derivation | Flag is a spurious-spike guard, not a variance-adequacy gate, despite being consumed as the latter. Cannot fire for "baseline flat, today also flat," exactly the case degenerate variance most needs flagging in. | Not yet actioned. Candidate fix (not yet vetted): add a variance-adequacy signal independent of today's-value comparison (e.g. flag directly on `stddev_30d = 0` or at-floor), and rename or split the existing flag to reflect what it actually detects. |

### TD-113 — DNS `BLOCKED` verdict base rate (characterization, informational)

`int.ooni_experiment_results`'s DNS `BLOCKED` verdict reads zero in roughly two-thirds of all months across the table's history (long silences separated by short bursts is this series' normal shape — gaps of 2, 5, and 10 months all appear). A detector that reads zero most months cannot support "we found zero" as meaningful evidence about any one particular month without this base rate stated alongside it. Ingestion health and classifier liveness during the longest (10-month) gap were both independently confirmed (large volumes of DNS `OK` rows; TLS produced `BLOCKED` rows in the same span via a different code path), ruling out a pipeline outage as the explanation.

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-113 | `marts.fact_ooni_censorship_signals` / `Bruin/assets/intermediate/int.ooni_experiment_results.sql`, DNS protocol, all countries/dates | A detector that reads zero in most months cannot support "we found zero" as meaningful single-month evidence without this base rate disclosed. | Not yet actioned. One discriminating query recommended, not yet run: split the historical DNS `BLOCKED` dates by detection path (`dns.bogon` vs. `dns.bogon_probe_reported`) and month, to test whether long gaps reflect two detection paths with different temporal availability rather than a real change in blocking behavior. Should inform how confidently any future document cites a DNS-BLOCKED "zero" for a specific window. |

### TD-114 — `regime_confidence` conflates two incompatible scales

Open. `Bruin/assets/intelligence/protocol_signal_regimes.sql` (`regime_confidence`, roughly lines 125-139) uses fixed sentinel constants (0.05-0.20) for rows disqualified by a data-quality flag, and a computed 0-1 effect-size score for valid rows — these are not the same kind of number, and a reader or a `WHERE regime_confidence > x` filter cannot tell "nothing happened, measured well" (a valid computed `0.0`) apart from "row disqualified" (a sentinel `0.05`-`0.20`) from the column alone. Compounding this, the separate `confidence_level` column (roughly lines 108-122) keys only off sample-quality/baseline-days inputs, not off `regime_confidence` or the underlying z-score at all — so a row can plausibly read `confidence_level = 'HIGH'` and `regime_confidence = 0.0` simultaneously, two confidence expressions structurally disagreeing about the same row.

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-114 | `Bruin/assets/intelligence/protocol_signal_regimes.sql:108-139` (`confidence_level`, `regime_confidence`) | One column conflates a disqualification sentinel and a computed effect size; a second, independent column can disagree with it on the same row. Not yet shown to have produced a wrong published number, but makes `regime_confidence` uncitable as "confidence" in any public document as currently structured. | Not yet actioned. One confirming query recommended: check `confidence_level` for rows where `regime_confidence = 0.0` in a known window, to see whether the predicted disagreement actually occurs. Candidate fix (not yet vetted): split into two columns — a disqualification-reason enum, and a nullable computed effect-size score — so no single field means two different things. |

### TD-115 — Signal-test anomaly spike, applied as independent corroboration, not attribution

RESOLVED. A Signal-test anomaly spike on a specific date in an external deliverable's date range was investigated across several rounds directly against this repository's live tables and cross-referenced against independently published third-party network-disruption reports. Conclusion: not citable as a Signal-specific interference finding (a documented, independently-reported nationwide disruption on the same day is a sufficient alternative explanation; app-specificity against WhatsApp/Telegram controls did not reach statistical significance; the TLS failure signature could not distinguish interference from backend strain). What the investigation did support, and what was applied to the external deliverable: the finding that CLIO's own Signal-layer measurement, from a distinct data stream and layer, timed its anomalies almost entirely within the externally-documented disruption's window — independent corroboration of timing, explicitly not a claim of Signal-specific blocking.

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-115 | `int.ooni_measurement_verdicts`, Signal test, Kenya | Multi-round investigation of a Signal-test anomaly spike; found not citable as interference, but citable as independent timing corroboration of a separately-documented event, with mandatory caveats. | Closed. No further action unless new evidence emerges. |

## TD-116 — logged during TD-102's build, OPEN, design gap not yet actioned

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-116 | Signal TLS reset-detection path (separate from `int.ooni_measurement_verdicts_candidate.sql`'s Signal ANOMALOUS branch, reads the same underlying measurements) | The known-bad-probe-version exclusion built for TD-102 reaches only the Signal-test ANOMALOUS-classification pipeline, not a separate TLS reset-detection path that reads the same underlying measurements. Some TLS BLOCKED rows in the Finance Bill window carry a probe version already flagged unreliable elsewhere. | **OPEN, not yet actioned.** No live claim currently depends on the unfiltered rows. A future session should extend TD-102's version exclusion to the TLS reset-detection path, or confirm explicitly why that path doesn't need it. |

## TD-117 — OPEN, Low

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-117 | `README.md` (embedded screenshots) | Repo screenshots are stale, predating a recent dashboard/classification fix pass. | **OPEN.** Not yet inventoried against current dashboard state — a future session should re-screenshot and confirm which images are affected before replacing them. |

## TD-118 — OPEN, Low

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-118 | Streamlit charts (specific pages not yet named) | Planned optics tweaks to a couple of Streamlit charts. | **OPEN.** Specific charts and changes not yet named or scoped — a future session should identify them before implementing anything. |

## TD-119 — OPEN, deliberately deferred, Low

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-119 | This project's relay-prompt-based development workflow (no specific file) | No MCP tooling exists to reduce context/token duplication across this project's relay-prompt-based development workflow (the private-planning-workspace-to-repo handoff pattern this very TD-107 effort is an instance of). | **OPEN, deliberately deferred.** Deferred until prototyped elsewhere — not scoped as work for this project yet. |

## TD-120 — logged during Phase 1 documentation sync, OPEN, informational (characterization, not a bug)

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-120 | `Bruin/assets/intermediate/int.google_pressure_periodized.sql` | `int.google_pressure_periodized` broadcasts each half-year Google Transparency Report period forward from its period-start date, not backward across the calendar months the period label names. | **OPEN, informational.** Doesn't affect any verified live number, but could mislead anyone reading a period label as its own coverage window. A future session should consider clarifying the column/label naming or documenting the broadcast direction explicitly wherever this table is surfaced. |

## TD-121 — logged during Phase 1 documentation sync, OPEN, informational (characterization, not a bug)

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-121 | ACLED staging layer (`stg.acled_conflict_events` and everything downstream) | CLIO's only staged ACLED table is uniformly weekly-aggregate grain, with no daily-resolution or actor-level data anywhere in the warehouse. | **OPEN, informational.** Confirms a hard ceiling already implicit in the regime engine's weekly design (ADR-0002) — not a defect, a documented limit on achievable granularity for any future ACLED-derived feature. |

## TD-122 — RESOLVED

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-122 | Finance Bill 2024 report deliverable (published prose) | Em-dash density in a published Finance Bill 2024 report deliverable ran well above this project's normal-writing band (10.87/1,000 words vs. a stated 1-4/1,000 target). | **RESOLVED.** Fixed via a punctuation-only rewrite (commas/colons/periods/parentheses), deliberately leaving section-heading separators and a few genuine mid-paragraph uses in place. Verified: 2.43/1,000 post-fix; every number, percentage, date, and figure confirmed byte-identical against the pre-edit text. |

## TD-123 — RESOLVED, same day as found

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-123 | One Streamlit page's data-fetch layer (surfaces OONI corroboration alongside ACLED-derived pressure attribution) | One Streamlit page never received an earlier disclosure-language pass applied to a sibling page, even though both surface OONI corroboration alongside ACLED-derived pressure attribution. | **RESOLVED, same day as found.** The page's data-fetch layer now requires and selects the same vantage-point disclosure field as its sibling; the page carries the same disclosure sentence, byte-for-byte, as its sibling. |

## TD-124 — RESOLVED, live-verified (ADR-0013 panel-seeding pass)

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-124 | `Bruin/scripts/agreement_check/` (`check_panel()`) | The OONI-agreement guardrail's own panel-check logic was tautological — it compared a panel entry's recorded CLIO verdict against itself, so a real CLIO-side regression could never be caught (including for either of ADR-0013's own two intended permanent regression locks). | **RESOLVED.** Fixed via a cost-conscious snapshot-and-refresh model: a new `--mode refresh-panel` CLI mode queries live verdicts narrowly and caches them, gated by a free BigQuery metadata staleness check rather than a live query on every check. Real refresh cost for all 144 panel entries: ~91 MiB, ~$0.0005. Proven live via deliberate fault injection — a corrupted entry correctly triggered regression detection, a backdated refresh timestamp correctly triggered the staleness gate — both reverted after. Note per ADR-0013's Status line: TD-93's own regression lock could not actually be seeded (TD-102's discard fix removes those measurements from classification entirely, leaving no live verdict to pin), so this design is down to one working permanent lock (TD-68) in practice. |

## TD-125 — RESOLVED, live-verified (ADR-0013 baseline audit, psiphon)

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-125 | `Bruin/scripts/agreement_check/config/allowlist` (or wherever the allowlist file lives), psiphon branch of `int.ooni_measurement_verdicts_candidate.sql` | Psiphon shows a systematic FAILED-vs-OONI-ANOMALOUS divergence (93 instances, every distinct failure-driver value). | **RESOLVED as a documented, deliberate divergence, not a code fix — by explicit project-owner decision.** Investigated and allowlisted rather than changed. Precise scoping proven: three deliberately near-miss synthetic signatures still correctly fire as new disagreements, confirming the allowlist entry doesn't silently swallow a different future problem. |

## TD-126 — RESOLVED, live-verified

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-126 | `stg.ooni_measurement_summary.sql` (new `whatsapp_registration_accessible` column), `int.ooni_measurement_verdicts_candidate.sql` (whatsapp branch) | Whatsapp's `ANOMALOUS` OR-chain's third disjunct (`registration_server_status`) still trusted a raw, probe-submitted field OONI's own backend scorer disables reading — the same cited upstream probe issue (`ooni/probe-engine#341`) already used to justify part of TD-105. This is the "bug (c)" candidate TD-105 investigated and set aside at the time. | **RESOLVED.** Population fully characterized before fixing: exactly 2 of 278 sole-driver rows affected. Fixed by recomputing `whatsapp_registration_accessible` from the matching request's own `failure IS NULL`, matching TD-105 bug (a)'s precedent exactly. Whatsapp ANOMALOUS dropped 835→833. Confirmed the fix reached the actual dashboard-facing weekly-signals table, not just the verdict layer. |

## TD-127 — CLOSED, characterized, no fix

| ID | Location | Description | Recommended action |
|---|---|---|---|
| TD-127 | `int.ooni_measurement_verdicts_candidate.sql` (whatsapp branch, `whatsapp_endpoints_status` disjunct) | Whatsapp's OR-chain's first disjunct (`whatsapp_endpoints_status`) was the one condition never checked against OONI's real scoring logic — investigated expecting a fourth instance of the TD-105/TD-126 pattern. | **CLOSED, no fix — the opposite pattern from TD-105/TD-126.** OONI's own fallback mechanism for endpoint reachability only runs after registration+web already passed, and a successful registration/web request's own transport success is enough to satisfy it before any of the real messaging endpoints are considered — OONI's own answer is the weaker signal here, not CLIO's. Recomputing this "the TD-105/TD-126 way" would make CLIO worse, since no per-endpoint request data exists to recompute from. Documented as a known, deliberate, more-sensitive-than-OONI divergence (9 of 51,394 whatsapp rows, systematic but small) rather than fixed. With this, all three branches of whatsapp's classification logic have been checked against OONI's real scoring source. |

## Summary by severity

| Severity | Count |
|---|---|
| Critical | 13 (12 resolved: TD-01, TD-02, TD-04, TD-05, TD-13, TD-36, TD-38, TD-40, TD-65, TD-68, TD-69, TD-72 — TD-65 resolved by removal-from-HEAD only, git history not rewritten by explicit decision; TD-68/TD-69 resolved 2026-08-01, both caused a public retraction of a flagship finding before the fix; TD-72 discovered 2026-08-01 while implementing TD-69 and resolved later the same day — a structurally dead `handshake_success` column that was misclassifying ~91.5% of the entire TLS observation table as `UNKNOWN` instead of `OK`; 0 new, open) |
| High | 22 (1 resolved for Layer B, Layer A deferred to TD-39: TD-08; 1 partially resolved: TD-47, bootstrap subset only; TD-09 resolved for the SQL layer 2026-07-06, Python residual → TD-60; 2 resolved 2026-07-11/12: TD-63, TD-37 — TD-37 reclassified up from Low the same day it was resolved; 1 resolved 2026-07-18: TD-66, logged 2026-07-12; 4 new, all logged 2026-08-17 by the Fable UX/IA audit: TD-95 (Phase 0 only resolved — overclaiming dashboard copy reframed and a methodology disclosure added; the schema half, a `path_attribution_state` column, remains open), TD-96 (resolved — three dashboard color/contrast bugs), TD-97 (resolved — the OONI-weekly/ACLED-regime overlay wired in, R1 scope only), TD-98 (resolved — Suppression Event Explorer merged into the Correlation Engine as a second tab); 5 new, all resolved, ported from ADR-0013's own build/panel-seeding thread 2026-08-31 (TD-107 Phase 1): TD-100 (the OONI-agreement guardrail itself, ADR-0013, built and live-verified), TD-101 (dnscheck/telegram/whatsapp oracle-coverage gap closed), TD-102 (signal's second, version-split disagreement shape fixed), TD-105 (whatsapp's 3-bug ANOMALOUS false-positive scope, 9,350→835), TD-124 (the guardrail's own tautological panel-check bug fixed via a snapshot-and-refresh model)) |
| Medium | 44 (2 resolved/retired: TD-41, TD-51; 14 resolved: TD-16, TD-22, TD-23, TD-24, TD-26, TD-27, TD-42, TD-52, TD-48 (folded into TD-49), TD-49, TD-54, TD-55, TD-56; TD-58 resolved (clustering) 2026-07-06 with incremental deferred-by-decision; 1 resolved 2026-07-18: TD-45; TD-81 resolved 2026-08-15 (TD-87 Phase 0 fix session) — `google_transparency_requests.py`'s ingest script now matches Google's real export schema/path, coverage gap and disputed cell both closed; 1 new, resolved same day: TD-82, the dead `error_results` COUNTIF in `fact_protocol_blocking_summary`/`mart_pressure_attribution_ooni_daily`, renamed to `unknown_results`; 3 new, resolved same day 2026-08-16 (TD-91 relay session, live-verifying a paper audit of TD-87 Phases 0-2): TD-83 (psiphon_failure moved ANOMALOUS→FAILED, survivorship bias ruled out), TD-84 (probe_accuracy_gate wired into ooni_verdict, exclude-not-flag), TD-85 (CONFIRMED guard's write-order exposure closed via candidate/publish split); 1 new, resolved same day 2026-08-15 (external-validation follow-up session): TD-93 (signal's NXDOMAIN-against-legacy-hostname overcount fixed, ANOMALOUS 7,601→2,919, external agreement 78.8%→100%); 3 open: TD-50, TD-64, TD-67 — TD-67 logged 2026-07-12, methodology caveat not a code bug; 7 new, ported 2026-08-31 (TD-107 Phase 1): TD-103 (dnscheck oracle-degeneracy root-caused, informs ADR-0013's taxonomy, resolved as a design finding), TD-107 (this documentation-sync effort — RESOLVED, both phases; see also Phase 2 additions below), TD-116 (Signal TLS reset-detection path not covered by TD-102's version exclusion, OPEN), TD-122 (Finance Bill 2024 report em-dash density fixed, resolved), TD-123 (a Streamlit page's missing disclosure-language pass, resolved), TD-125 (psiphon's systematic FAILED-vs-ANOMALOUS divergence, documented/allowlisted, resolved), TD-126 (whatsapp's third OR-chain disjunct, registration_server_status, fixed, resolved); 5 new, ported 2026-08-31 (TD-107 Phase 2, from externally-maintained-document review): TD-108 (stale TLS-correlation claim removed, 82-row TLS BLOCKED concentration characterized as not interference-shaped, resolved), TD-110 (DNS-layer/protocol-stress claims re-verified, one required real reframing, resolved), TD-112 (`ZERO_VARIANCE` gate cannot fire on a genuinely flat-zero series despite being consumed as a variance-adequacy gate, OPEN), TD-114 (`regime_confidence` conflates a disqualification sentinel with a computed effect-size score, can structurally disagree with the separate `confidence_level` column, OPEN), TD-115 (Signal-test anomaly spike reframed from attribution to independent timing corroboration, resolved)) |
| Low | 34 (4 resolved: TD-21, TD-43, TD-53, TD-61; 1 new, open: TD-44; 1 new, resolved same day: TD-46; 1 open-by-design, tripwire half done 2026-07-06: TD-57; 3 open: TD-59, TD-60, TD-62; 1 reclassified up to High, confirmed live 2026-07-12: TD-37, no longer counted here; 1 new, investigated and closed with no code change 2026-08-01: TD-70; 1 new, resolved 2026-08-01 (partial — corroboration-gated tier explicitly deferred): TD-71; 1 new, open, logged 2026-08-01 during external-validation work: TD-73, a `stg.ooni_tls_observations.ip_address`/`.port` extraction gap, confirmed to have zero effect on TD-68/TD-69/TD-72/TD-71; 1 new, resolved same day: TD-74, `Bruin/requirements.txt`'s duckdb pin drift from `pyproject.toml`/`uv.lock`; 1 new, resolved (mitigated) 2026-08-03: TD-78, `llm_report_writer/`'s verification-design coherence gap; 1 new, resolved 2026-08-03: TD-79, `report_writer_openai.py`'s masked `.env`-path bug; TD-80 resolved 2026-08-15 (TD-87 Phase 0 fix session) — `int.ooni_experiment_results.sql`'s TCP `timed_out` pattern-match gap fixed via `marts.dim_tcp_failure_evidence`, 16 rows now correctly DOWN instead of UNKNOWN; 1 new, open, logged 2026-08-15 during the weekly OONI aggregation build: TD-92, a denominator inconsistency in how Signal's ANOMALOUS rate is narrated in `decision-log.md`/this file's own TD-84 row — 15.1% vs the internally consistent 17.9%, a prose-only issue, no live table affected; 1 new, resolved 2026-08-17: TD-99, five smaller items bundled from the Fable UX/IA audit — F3/F4/F5/F7 Low-rated, F6 Medium-rated by the audit but tracked as one bundled TD item here; 8 new, ported 2026-08-31 (TD-107 Phase 1): TD-104 (OONI's own dnscheck target-list dead hostnames, informational, resolved with no CLIO-side change), TD-106 (OONI's own telegram target-list dead hostnames, informational, resolved with no CLIO-side change), TD-117 (stale README screenshots, OPEN), TD-118 (planned Streamlit chart optics tweaks, OPEN, not yet scoped), TD-119 (no MCP tooling to reduce relay-prompt context duplication, OPEN, deliberately deferred), TD-120 (Google Transparency period-broadcast direction, informational, OPEN), TD-121 (ACLED's uniformly-weekly grain ceiling, informational, OPEN), TD-127 (whatsapp's first OR-chain disjunct, whatsapp_endpoints_status, characterized and CLOSED with no fix — a deliberate, more-sensitive-than-OONI divergence); 3 new, ported 2026-08-31 (TD-107 Phase 2, from externally-maintained-document review): TD-109 (`features.ooni_weekly_signals`'s 2025-06-28 data horizon, consistent with declared scope, informational), TD-111 (an evidence/measurement-lineage-mapping research idea, scoped down to a single falsification-test register entry, not started), TD-113 (DNS `BLOCKED` verdict reads zero in ~2/3 of all months historically, a base-rate characterization relevant to any future "we found zero" claim, not yet actioned)) |
| **Total** | **115** |

Note: TD-13 was reclassified from High to Critical on 2026-07-04, then fully resolved the same day after a second on-disk key copy (`infra/terraform-key.toml`) was discovered and closed out. TD-36 was added and resolved the same day. TD-37 was added the same day as a low-severity side finding from the TD-13 investigation, then reclassified High and confirmed live 2026-07-12 during an A6 (public repository readiness) pass — see that section's entry above and `decision-log.md`'s 2026-07-12 entry. TD-22 was resolved the same pass. TD-38 was added the same day as a critical finding surfaced while designing TD-04's guard — a live, currently-dormant corruption risk in `pipeline.yml`'s documented steady-state default — and was resolved the same day via a new steady-state driver script (commit `d0ffefa`). TD-04 itself was designed and fully applied the same day (commit `2080b64`). TD-08's Layer B golden-file tests landed the same day (commit `b3ae94d`); TD-39 was added the same day as the infrastructure gap blocking the stronger Layer A design. TD-40 was found the day after (2026-07-05) during ADR-0002 step (e) verification and resolved the same day; TD-41 was found alongside it as a second casualty of the same root-cause commit, and retired outright (file deleted, commit `48368f0`) per explicit project-history context establishing path B and its satellites as residual, pre-path-A-rigor artifacts. TD-02's diagnosis was corrected the same day (2026-07-05) — the original "add a join key" framing was wrong, since `asn_behavior_profile_mart` has no per-date grain to join on — and resolved via a grain-honest split rather than a join fix. TD-44 and TD-45 were both logged 2026-07-05 during ADR-0004's composite-score reweighting investigation (dropping Lumen's term from `composite_pressure_score`) — TD-44 (pressure_level thresholds not re-derived post-reweighting, low severity, no currently-known misclassification of a named incident) and TD-45 (a pre-existing naming collision between the fact table's and the mart's same-named `composite_pressure_score` columns, medium severity as a future-change risk, not a current bug) — neither is fixed yet. TD-66 was logged 2026-07-12, while drafting the public methodology document, as an extension of TD-45 rather than a duplicate: TD-45 only flagged the naming collision; TD-66 adds that the mart's four additional terms have no cited weight derivation (unlike the fact table's ADR-0004-cited 0.75/0.25) and that no reporting asset decomposes the mart's own recomputed score the way `mart_pressure_attribution_daily` decomposes the fact table's — meaning the specific number the dashboard actually headlines on page 1 is not the number CLIO's own attribution page decomposes. Rated High, not Medium, because it's a live gap against CLIO's own attributable-inference discipline in the number most likely to reach an external reader first. TD-46 was logged and resolved the same day: the Lumen disclosure banners on pages 1, 6, and 7 overclaimed once the reweighting shipped (nothing on those pages was Lumen-derived any longer), confirmed directly by grepping every SELECT and every rendered dataframe/chart/export on all three pages before removing the banners outright. TD-47 through TD-52 were found the same day during a diagnostic investigation into whether OONI's app-identity (test_name) and protocol-layer cause detail survive CLIO's pipeline, triggered by the project owner's question about attributing findings to specific apps (Telegram, WhatsApp, Signal) and specific protocol layers (DNS/TCP/TLS/HTTP).

**Tier 1 of that thread shipped 2026-07-05** (real code, not diagnostic-only): TD-52 (dead predicate fixed), TD-51 (mart wired into a new Streamlit panel), TD-47's bootstrap subset (dnscheck DNS recovery), and TD-43 (stale comment), plus TD-21 (reconciled in the same file as TD-51/52) in the same pass, per standing project discipline of updating tracked docs in the same commit as the fix. TD-47's `lookups` and `tor` subsets remain open, Tier 3, deliberately deferred (unchanged from the original diagnostic). TD-48, TD-49, and TD-50 (Tier 2/3, app-family and cause-level detail collapses further down the chain) are unchanged, still open. Two new items were logged along the way, in keeping with this project's practice of surfacing side-findings rather than silently folding them in or ignoring them: TD-53 (an orphaned, pre-existing stale BigQuery table found while confirming TD-21's rename direction — not created by this pass, not fixed in it, flagged for operator sign-off since dropping a live table is destructive) and TD-54 (a deliberate, temporary definition/materialized-state gap: TD-47's DNS-observation recovery was not cascaded into `intelligence.protocol_signal_regimes` or the reporting layer in this pass, to avoid silently shifting the dashboard's flagship `composite_pressure_score` without a dedicated before/after measurement).

**TD-54 was then investigated and resolved by decision the same day (2026-07-05):** the promised before/after measurement was performed by simulation (no table written) against the Finance Bill 2024 window, finding the headline `protocol_stress_score` provably immune (a `MAX`, bit-for-bit unchanged) but the `protocol_state`/`trend_state` rollup count-weighted and genuinely dilutable — two secondary ELEVATED days flip to NORMAL and the 06-23 severe margin thins 0.041→0.016 under pooling. Decision: **do not cascade**; the fix requirement (keep `test_family = 'dns'` out of the pooled share denominators for `protocol = 'dns'`) is folded into TD-49 as a hard requirement of its eventual implementation, and TD-54 is closed by reference to it rather than left open in parallel. The investigation also surfaced TD-55 (dnscheck's structurally-zero `signal_rate`, including 9,046 live probe-reported `dns_bogon_error` rows that never classify as blocking) — logged as its own open item, deliberately not investigated further in that pass.

**TD-66 was resolved 2026-07-18** (commit pending as of this doc update), closing TD-45 alongside it as the same fix. The dashboard's page-1 KPI now reads `fact_country_pressure_daily.composite_pressure_score` directly rather than a shadow recomputation under the same name, and the shadow recomputation itself was deleted outright. A recalibration backtest against the Finance Bill 2024 window, run before deleting, found the clean composite alone correctly classified the full crisis week once recalibrated — the two detectors differed only on ambiguous ramp/tail days with no ground truth available to adjudicate, which was judged insufficient to justify keeping an undocumented formula alive under a different name. See that entry above for the full account, including a process correction recorded in `decision-log.md`'s 2026-07-18 entry.

**The materialization-staleness guardrail (ADR-0005) is now standing infrastructure (script + allowlist committed 2026-07-05, `cdc429a`; ADR, scheduled CI workflow, and this TD-56 entry completed 2026-07-06):** a daily GitHub Actions check that fails on any *new, undocumented* dependency edge where a downstream table is older than its upstream — the TD-38/TD-40/TD-54 failure category, mechanized. Its first run surfaced 12 stale edges: 5 deliberate (the TD-54/TD-49 hold, now machine-enforced rather than prose-only) and 7 previously unknown (logged as TD-56 above). Resolving a TD referenced by the allowlist must prune the matching allowlist entries in the same commit.

**The pressure-attribution decomposition shipped 2026-07-06 (ADR-0006, Project Zero Review Recommendation #4):** four new leaf reporting assets (`reporting.mart_pressure_attribution_daily` / `_conflict_drivers` / `_platform_drivers` / `_ooni_daily`) plus Streamlit page 9, decomposing `composite_pressure_score` into its two real arithmetic terms with per-row confidence qualifiers and evidence pointers, OONI surfaced as same-day corroboration (it is *not* a composite input — the platform term is Google Transparency data), and the regime overlay alongside. Verified end-to-end against the Finance Bill 2024 window through the rendered page. TD-57 (above) tracks the deliberate keep-in-sync duplications this introduced. The TD-54/TD-49 hold assets were not touched (each new asset materialized individually; staleness check re-run clean, 73 edges, 0 new violations).

**Tier 2 of the OONI thread shipped 2026-07-06 (commit `9ec0302` for code; this pass for the ordering fix, re-verification, and these doc updates), closing the thread that ran from TD-47:** TD-55 (dnscheck's structurally-zero DNS blocking classification), TD-49 (family-separated share denominators in the reporting rollup), and TD-48 (surfacing `test_family` as a real reporting dimension — the same work as TD-49) all resolved, and TD-54's held cascade completed safely on top of them. TD-55's fix — mapping the probe engine's own `dns_bogon_error` verdict to BLOCKED (a real anti-encrypted-DNS-resolver enforcement signal, confirmed by sampling: whole-session bogoning of public DoH/DoT resolvers while app DNS resolved fine on the same network-days) — turned `test_family = 'dns'` from a structural-zero into a real 0.0116-interference signal, which *removed* TD-54's original premise that dnscheck was a near-zero diluter. Independent of that, TD-49's family separation is the correct semantics regardless (dedicated resolver testing and app-embedded DNS checks are different measurements and must not share a share denominator), and it is what makes the cascade safe: the Finance Bill 2024 window re-measured live shows every ELEVATED/SEVERE day at its full un-pooled share, driven by `messaging`, with the two days TD-54's simulation predicted flipping to NORMAL preserved. The cascade's one loose end — `intelligence.protocol_relationships` having been rematerialized out of DAG order, after its two downstream marts, tripping the ADR-0005 staleness check (the "lint error" this pass was asked to find) — was fixed by rematerializing `asn_behavior_profile_mart` and `protocol_repression_correlation_mart` in order (verified safe first: 0 `censorship_intensity_tier` flips from dnscheck inclusion); staleness now passes with 0 new violations, and the four TD-54 allowlist entries are pruned. **Still open from the thread:** TD-47's `lookups` (per-resolver DNS manipulation-resistance, the actual point of dnscheck — needs Python-side map-flattening at ingest) and `tor` subsets (Tier 3), and TD-50 (cause-level detail collapse at the intelligence layer, Tier 2/3).

See `implementation-roadmap.md` for how these are sequenced. Verification items (TD-01, TD-03) came first because they changed the shape of several other items; TD-13 and TD-36 blocked Step 3(b) (resuming the ACLED backfill) and are now both resolved. TD-04, TD-38, TD-40, and TD-02 are now all resolved; TD-41 is retired. TD-08's golden-file tests (Layer B) are in progress and reconfirmed passing after this pass's changes (unaffected, since none of this touches ACLED); TD-39 documents why Layer A (a true SQL-rerun regression test) is deferred pending a separate test-isolation infrastructure decision. TD-01 remains the highest-priority open item; the OONI thread's Tier 1/Tier 2 items (TD-47 bootstrap, TD-48, TD-49, TD-51, TD-52, TD-54, TD-55) are now all resolved, leaving only its Tier 3 stretch items — TD-47's `lookups` and `tor` subsets, and TD-50's cause-level detail — deliberately deferred.

**Cost-audit + cleanup pass, 2026-07-06 (this pass):** the project's first-ever GCP/BigQuery cost measurement, plus execution of the clutter already on record. Cost audit (full findings in the "Cost audit" section above): spend is pilot-scale (~$6.8 over 180 days, on-demand, us-central1), the OONI staging chain is the dominant and only badly-scaling driver (logged **TD-58**), Streamlit and storage are negligible, and the ACLED backfill is the most slot-intensive but one-time operation. Cleanup executed (not just reported): **TD-53** (dropped the 163-row orphan `marts.fact_platform_blocking_summary`), **TD-56** (all seven stale marts retired after direct consumer-tracing found every one has zero live consumers — including correcting TD-56's own factually-wrong claim that `fact_asn_repression_index` feeds pages 5/7; it feeds nothing), **TD-42** (retired `lumen_requests_to_gcs`'s false `depends: raw.lumen_requests` declaration rather than dignifying a benched pathway), and **TD-41's** leftover orphan table `marts.fact_conflict_events` (asset was deleted before, table wasn't). Two new items logged: **TD-58** (OONI staging cost — cluster/partition + optional incremental) and **TD-59** (two more unconsumed dims: retire `dim_blocking_signals`, keep repointed `dim_country`). End state verified: `bruin validate` clean (60→53 assets), staleness check PASS with an empty allowlist (59 edges, 0 allowlisted, 0 new, 0 orphaned-allowlist warnings). Nine BigQuery tables dropped in total. Deliberately **not** done this pass, per the task's explicit scope: TD-47's `lookups` subset was left as designed-only, and no STOP/Access Now ingestion was started — both paused until this audit closed.

**Guardrails + multi-country scaffolding pass, 2026-07-06 (the pass following the cost audit):** executed the audit's follow-ups and made the cleanup durable. Shipped: **TD-58's clustering lever** (all five OONI staging assets partitioned by `measurement_date`, clustered by `(country, test_name)`; tables dropped and rebuilt via one DAG-ordered `bruin run --downstream`; measured 5.5 GiB vs ~30 GiB on the diagnostic query shape, 0.154 GiB on date-filtered scans; incremental conversion explicitly deferred — no schedule actually runs the pipeline); **TD-09's SQL-layer resolution** (20 country-literal sites — four more than previously on record — routed through the existing `{{ var.country }}`/`{{ var.iso2 }}` variables; `dim_country.sql` designated the single legitimate literal location; `dim_countries` reference table rejected at n=1; new CI check `country-literal-check.yml` makes it durable; Python residual logged as **TD-60**); **TD-57's residual tripwire** (now a materialization-time `custom_checks` assertion, verified live at max residual 0.0001); **a TD-55 regression lock** (`tests/test_ooni_dns_bogon_classification.py`: 4 static SQL-text assertions that run in credential-less CI today + 1 live-BigQuery behavioral test under `RUN_BIGQUERY_TESTS=1`); **a contracts validator for ADR-0006's four attribution marts** (`reporting.validate_pressure_attribution_contracts`, mirroring the intelligence-layer pattern with composite-grain keys; verified passing live: 761/15,820/69/12,175 rows, all grains unique); and the **materialization-strategy audit** section above (every `create+replace` leave-alone case recorded as a decision). Environment note: both Python contract validators fail under `bruin run` in a fresh Codespace until the gitignored `.bruin.yml` gains a `duckdb-parquet` connection entry (any local path works) — fixed locally this pass, worth folding into `.devcontainer/setup-gcp-tooling.sh` if it recurs. Verified end state: `bruin validate` 54/54, staleness check PASS (59 edges, 0 new), full test suite 11 passed live, country-literal check PASS (and verified to fail on a planted literal).

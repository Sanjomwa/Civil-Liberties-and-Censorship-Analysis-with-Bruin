# CLIO Implementation Roadmap — Engineering Reset

Status: Phase 3 deliverable, CLIO Engineering Reset. Evolutionary improvement, not a rewrite. Every item below references a specific finding in `architecture-assessment.md`, `technical-debt-inventory.md`, or `methodology-consistency-review.md` — nothing here is invented independently of those three documents. This roadmap is scoped to engineering health; the separate commercial/product roadmap from the Project Zero Review (`docs/01-product/roadmap.md`) is not repeated here and should be read alongside it, since a few items depend on each other across the two documents.

## Step -1 — The OONI external-agreement guardrail (ADR-0013), reprioritized to the top 2026-08-17

This step didn't exist in the roadmap's original sequencing. It was inserted ahead of Step 0 on 2026-08-17, after a pattern became impossible to ignore: **every real OONI-classification bug this project has ever found — TD-68 (the DNS bogon canary hostname that inflated the flagship Finance Bill 2024 figure from 0 to 177), TD-93 (Signal's ANOMALOUS verdict majority-driven by a dead legacy hostname, 62.5% of its historical population), and TD-97 (dnscheck's two nominally-independent signal series found evidentially coupled)** — was caught by accident. A Slack exchange with OONI's own team, a one-off matched-pair audit during an unrelated milestone review, and a human looking closely at one rendered chart, respectively. No standing process had ever run the validation method that caught the first two — comparing CLIO's own verdicts against OONI's real, published classification for the same measurements — until this step existed.

**Status: infrastructure complete and fully live-verified.** ADR-0013 documents the design and its full build history. The guardrail itself (TD-100) is built: a fixed known-answer panel (144 entries, seeded 2026-08-30) plus a rotating, category-stratified sample, checked against OONI's live API, landing in `audit.ooni_agreement_runs` and a `workflow_dispatch`-only GitHub Actions workflow. Running the baseline audits this guardrail enabled found and fixed real bugs, not just validated the design: TD-101 (dnscheck/telegram/whatsapp had never been externally validated at all — dnscheck fixed via a bogon carve-out, telegram closed with zero code changes after confirming no real FAILED-worthy population exists, whatsapp folded into TD-105), TD-102 (Signal's second, version-split disagreement shape — two separate dead-endpoint defects, one a one-time OONI patch, one permanent), TD-105 (whatsapp's ANOMALOUS rate corrected 9,350→835 across three independently characterized bugs), and TD-126 (whatsapp's third and final OR-chain disjunct fixed the same way). One disjunct, TD-127, was characterized and deliberately left unfixed — the opposite pattern from the others, where OONI's own oracle turned out to be the weaker signal, not CLIO's. **As a direct result, every OONI test type this project currently exposes on its dashboard now has a verified-correct classification** — `signal`, `whatsapp`, `telegram`, and `psiphon` are `VALIDATED`; `dnscheck` is `DEGENERATE-EXEMPT` (OONI's own backend scorer has no dispatch entry for it at all, confirmed via TD-103, so it's validated internally instead, via TD-47/TD-101, not against this external oracle); `tor` carries zero rows in this corpus and was never exposed.

Panel-seeding itself surfaced and fixed a bug in the guardrail's own logic (TD-124 — the panel check was comparing a recorded verdict against itself, so it could never actually catch a regression) and closed out the allowlist population for psiphon's known, deliberate divergence (TD-125).

**The weekly cron this design originally specified was deliberately never turned on — not a delay, a standing decision.** CLIO is still pilot-stage, running against a historical backfill, not continuously ingesting live OONI data. A fixed weekly timer would mostly spend OONI's own rate-limited public API budget re-confirming a result that hasn't changed, which is a poor use of a relationship this project depends on (OONI's own team caught the first real incident this guardrail exists to prevent a repeat of, directly, in conversation). The workflow stays `workflow_dispatch`-only indefinitely; the operating procedure is to run it by hand, once per test type, right after either CLIO begins ingesting live/new data on an ongoing basis, or someone changes the classification/agreement SQL this harness checks. Event-triggered, not calendar-triggered.

**What follows this step:** TD-107, the effort to bring this repository's own public tracked docs current with everything described above (this section, and everything else this step's work produced, existed only in a separate private planning workspace until this pass). TD-107's Phase 1 covers ADR-0013 and TD-100 through TD-107/116-127; **TD-108 through TD-115 are confirmed to exist and are deliberately deferred to a future Phase 2**, not forgotten and not guessed at here.

## Step 0 — Verification, before anything else (hours, not days)

These are investigations, not redesigns, and they change the shape of several later steps.

1. **CONFIRMED (2026-07-04).** No asset under `Bruin/assets/reporting/` references `intelligence.acled_pressure_regimes` or `intelligence.country_readiness`, in either the declared `@bruin depends:` header or the SQL body — verified by reading all four reporting assets in full, not just grepping headers. `political_stress_windows_mart.sql` and `protocol_repression_correlation_mart.sql` both source ACLED pressure from `marts.fact_country_pressure_daily`, which in turn reads directly from `stg.acled_conflict_events`, bypassing `int.acled_event_classification`, `features.acled_pressure_signals`, and `intelligence.acled_pressure_regimes` entirely. Path A is not merely unconsumed at the reporting layer — it is bypassed at the very first step after staging. The dashboard's national pressure score is built entirely from Path B. This closes the single highest-leverage open question in the reset. Step 3 below now has a confirmed starting point rather than an open question: the choice is between wiring Path A in, replacing Path B with it, or formally deprecating Path A, and that choice is no longer blocked on investigation.
2. **Confirm the full blast radius of the Lumen synthetic-data finding.** Trace every mart and page that touches `stg.lumen_requests`-derived data to know exactly what is affected before deciding how to flag or fix it (TD-01). Still open.

## Step 1 — Mechanical fixes (days)

Low-risk, low-effort, high value-per-hour. No architectural judgment required.

- Regenerate `Bruin/requirements.txt` from the same source as `uv.lock`, resolving the `duckdb` version drift (TD-06).
- Bump the devcontainer's Python base image to match `pyproject.toml`'s `>=3.12.3` requirement (TD-07).
- Delete `streamlit/services/freshness.py`, `streamlit/components/charts.py`, `streamlit/components/tables.py`, and `logs/query_log.sql` (TD-17, TD-18, TD-32) — all confirmed dead code with no callers.
- **RESOLVED (2026-07-12).** Fixed the top-level `README.md`'s stale repository-structure diagram (TD-22), regenerated from `git ls-files` rather than the local working tree — part of a broader A6 (public repository readiness) pass; see `decision-log.md`'s 2026-07-12 entry for the full scope, including a real deployment-credential gap (TD-37) found and deliberately not fixed unilaterally.
- Rename `streamlit/pages/3_Protocol__Stress_Intelligence_Observatory.py` to remove the double underscore, and standardize on `use_container_width=True` across all pages (TD-26, TD-27).
- Reconcile the `fact_platform_blocking_summary.sql` filename/name mismatch (TD-21).
- Disable `DEBUG_SQL_LOG` by default in `backfill_acled_pressure_regimes.py`, making it opt-in (TD-25).

## Step 2 — Provenance and correctness fixes (1–2 weeks)

These require judgment but not architectural change.

- **RESOLVED (2026-07-05, commit `69233b4`).** Added a real, per-row `is_synthetic` flag at `stg.lumen_requests`, propagated through every downstream consumer (both the live-reporting branch and the currently-unconsumed dead-end branch, for consistency), surfacing as a visible warning on every Streamlit page a Lumen-derived number reaches, plus a permanent limitations section on the Methodology page. Verified end to end against materialized tables and by rendering all affected pages. Surfaced a related, separately-tracked DAG-wiring finding (TD-42, not fixed as part of this).
- **RESOLVED (2026-07-05, commit `eb6993e`).** Fixed the `CROSS JOIN` in `get_finance_bill_incident()` (TD-02) — not by adding a join key (there was none to add: `asn_behavior_profile_mart` has no date grain at all) but by splitting into two independent, unjoined dataframes at their real respective grains. Verified against live data: undistorted row counts (124 correlation rows, 3 ASN rows, vs. the old fabricated 372) and a rendered-page screenshot confirming page 7's charts now show the real, non-tripled numbers.
- Add a runtime guard to `intelligence.acled_pressure_regimes` refusing execution on precondition violation (TD-04), regardless of what Step 0's verification finds — this fixes a silent-corruption risk independent of whether the asset turns out to be wired into reporting.
- Stop swallowing secret-parsing exceptions silently in `bq.py` (TD-19) and stop surfacing raw stack traces to end users (TD-20).

## Step 3 — Methodology unification (2–4 weeks, Step 0 now confirmed)

- **RESOLVED (2026-07-05, commit `ab681b0`).** ADR-0001/TD-05's confidence-scheme duplication is closed — but not as originally planned. Investigation found "migrate the inline literals to the dimension table" was the wrong fix: those literals are continuous inputs to downstream statistics, not competing labels, and the dimension table was dead code anyway. The real bug was three disagreeing hardcoded categorical re-bucketing thresholds; fixed by pointing the two live sites at a (now-governing) `dim_censorship_confidence` instead, leaving the raw continuous literals untouched. Verified twice, independently, via real materialization and live Streamlit rendering. See ADR-0001 for the full corrected diagnosis.
- **COMPLETE (2026-07-04, ADR-0002).** Wired `intelligence.acled_pressure_regimes` (path A) into the reporting layer. All five steps done: (a) confirmed actual backfill state, surfacing and resolving TD-13 and TD-36 along the way; (b) completed Kenya's historical backfill (1,523/1,523 weeks, 0 errors), producing the first real evidence for this ADR — clean detections of the Jan 2008 post-election violence, the Aug 2022 election, and the Finance Bill 2024 protests; (c) added and applied the TD-04 runtime precondition guard; (d) landed TD-08's golden-file tests (Layer B); (e) **design corrected mid-implementation, based on direct investigation**: the original "replace path B" framing assumed a clean swap, but path B's daily continuous composite score and path A's weekly categorical regime classification turned out to be different kinds of things entirely — so the actual change shipped **additively**: `fact_country_pressure_daily` gained a `regime_*`-prefixed `LEFT JOIN` to path A (Saturday-week-anchored, verified empirically, not assumed), both reporting marts pass those columns through unchanged, and the dashboard gained a new, separate regime-classification panel — no existing column, score, or chart touched. Path B's retirement is deliberately deferred to a future decision after an observation period. This work also surfaced **TD-40**, a separate and still-open critical bug (`fact_country_pressure_daily`'s `acled` CTE groups by an always-`NULL` `event_date`, meaning the *next* materialization for any reason would silently zero the composite score) — deliberately left unfixed, verified around instead via a non-materializing query, and now the single most urgent open item. See ADR-0002 for full detail. Real evidence from the Finance Bill 2024 comparison also sharply raises the urgency of TD-01 and TD-02 below: path B's peak reading for that entire window never exceeded `ELEVATED`, even the week Parliament was stormed, partly propped up by TD-01's synthetic Lumen data.
- Add golden-file regression tests tied to the Finance Bill 2024 incident and at least one other known case, covering both the OONI and ACLED classification/regime logic (TD-08, per `testing-strategy.md` Priority 1). This is what would have caught the `upgraded_assets` regression (TD-30) before it had a chance to be merged.

## Step 3b — OONI app-identity and protocol-layer attribution (new, 2026-07-05)

Six items (TD-47 through TD-52) surfaced from a single diagnostic question: can CLIO attribute a finding to a specific app (Telegram, WhatsApp, Signal) and a specific protocol layer (DNS/TCP/TLS/HTTP)? Investigated, not yet implemented. Deliberately tiered by cost and blast radius rather than tackled in discovery order — see the decision-log entry dated 2026-07-05 ("the manner of tackling the TD backlog") for the reasoning behind this tiering as a general policy, not just this thread's sequencing.

**Tier 1 — cheap, isolated, do first (days, not weeks):**
- TD-52: fix `fact_platform_blocking_summary`'s dead cause-breakdown columns (a `STARTS_WITH`-style predicate fix; the asset is currently unconsumed, so zero blast radius).
- TD-51: wire that same mart into a new Streamlit panel — the data already has full per-app, per-protocol-layer resolution at monthly grain; no new ingestion needed.
- TD-47(1): `dnscheck`'s `bootstrap` subset — a corrected/additional fixed JSONPath recovers one real system-resolver DNS result per measurement; leaf-compatible with existing classification logic.
- TD-43: the stale doc-comment cross-reference (trivial, one line).

**Tier 2 — DONE, 2026-07-06.** TD-55 (dnscheck's blocking classification — a real gap, fixed: `dns_bogon_error` now maps to `BLOCKED`), TD-48/TD-49 (one piece of work — `protocol_interference_trends_mart` v5→v6 now aggregates within `test_family` before rolling up, keeping `dns` separate from `messaging`/`circumvention`, with a new `state_driving_family` column on the dashboard), and TD-54's held cascade (completed and re-verified against Finance Bill 2024 once TD-49 shipped, four allowlist entries pruned). A genuine new staleness violation surfaced and was fixed in the same pass (an out-of-order rematerialization of `intelligence.protocol_relationships`) — ADR-0005's first real catch. TD-45, TD-44, and TD-42 (below) remain open, unrelated to this thread:
- TD-45: **RESOLVED 2026-07-18**, as part of TD-66's fix below — the colliding column is gone (`mart_political_stress_windows`'s former OONI-fused recomputation, which shared the `composite_pressure_score` name, was deleted outright, not renamed).
- TD-66 (logged 2026-07-12, extends TD-45): **RESOLVED 2026-07-18.** `mart_political_stress_windows.composite_pressure_score` no longer recomputes under the fact table's column name; it is now a direct passthrough of `fact_country_pressure_daily.composite_pressure_score`, so page 1's KPI and the attribution page now decompose the same number. The OONI-fused recomputation (and its downstream rolling-baseline/delta/window-class chain) was deleted, not preserved — a recalibration backtest against Finance Bill 2024, run before deleting, found the clean composite alone correctly classified the full 06-22–06-28 crisis week once recalibrated; the two detectors differed only on ambiguous ramp/tail days with no ground truth available to adjudicate, judged insufficient to keep an undocumented formula alive under any label. TD-45's naming-collision half is closed by the same deletion. See `decision-log.md`'s 2026-07-18 entry for the full account, including a process correction along the way.
- TD-44: re-derive the `pressure_level` thresholds against the post-reweighting distribution.
- TD-42: reconcile or formally write off the Lumen ingest DAG-declaration mismatch, given Lumen is benched.
- TD-68 (logged and **RESOLVED 2026-08-01**): the DNS bogon regex's answer-shape check had no `answer_type` guard and no exclusion for `uptime.signal.org`, Signal's own intentional loopback canary — the confirmed root cause of the retracted "177 same-day DNS signals" flagship finding. Fixed; see the inventory entry and the 2026-08-01 decision-log entry for full verification detail.
- TD-69 / TD-72 item (d) (logged and **RESOLVED 2026-08-01**): the `tls` CTE had no exception for Signal's 2022 TLS root-CA rotation, misclassifying `ssl_unknown_authority` against Signal's real backend servers — originally believed `BLOCKED`, corrected mid-fix to `UNKNOWN` (the real "before" state; see the inventory entry). Fixed via a certificate-fingerprint allowlist and a new orthogonal `exclusion_reason` column; surfaced TD-71 (the broader TLS-failure-mode confidence-collapse question, still open) and, more significantly, TD-72 — a structurally dead `handshake_success` column causing ~91.5% of the *entire* TLS observation table to misclassify `UNKNOWN` instead of `OK`, system-wide across all four ingested test types, not a signal-specific issue.
- TD-72 (logged 2026-08-01, **RESOLVED later the same day, third session**): `handshake_success` read the wrong OONI JSONPath (`$.status.success`, a TCP-connect-format field, not a TLS one) and was always `NULL`. Fixed by deriving it from `tls_failure IS NULL` instead; a dead `WHEN handshake_success IS FALSE THEN 'BLOCKED'` CASE arm that would have come alive as an unreviewed side effect was deliberately removed rather than resurrected, keeping the fix scoped to exactly "successes move from `UNKNOWN` to `OK`," nothing else. Verified provably narrow live (BLOCKED/DOWN counts byte-for-byte unchanged before/after); full downstream chain (16 assets) rematerialized clean; before/after numbers for every affected downstream field (`protocol_daily_signals`, `protocol_signal_regimes`, `protocol_repression_correlation_mart`, `asn_behavior_profile_mart`) recorded in the inventory entry and `decision-log.md`. `composite_pressure_score`, blocked-event counts, and `behavioral_class` confirmed immune **to TD-72's own scope** — TD-71 (per-failure-mode TLS confidence) remained open, now genuinely unblocked rather than subsumed. No further OONI-thread item was blocked on this any longer.
- TD-71 (logged 2026-08-01, **RESOLVED later the same day, fourth session, partial**): per-failure-mode TLS confidence tiering, via a new dimension table (`marts.dim_tls_failure_evidence`) joined into the `tls` CTE's `confidence_score` CASE with a default-low-allowlist `COALESCE`. 5 of 6 tiers shipped (`connection_reset` 0.60, `generic_timeout_error` 0.40, `eof_error`/`connection_aborted` 0.50, `ssl_*` residual unchanged at 0.45); the `ssl_*` corroboration-gated elevation was deliberately not built — confirmed live that CLIO's ingested raw OONI JSON carries no `anomaly`/`blocking` field to gate on. One design correction made before shipping (`connection_aborted` verified against ooni/spec directly as a genuine failure, not the client-side artifact an earlier draft grouped it with). **Corrects TD-72's own "confirmed immune" finding for `reporting.asn_behavior_profile_mart`:** that finding held for TD-72's own scope (no `confidence_score` change for `BLOCKED` rows) but not for TD-71, which does change it — measured live, AS33771 (Safaricom)'s `behavioral_class` moved `STABLE`→`VARIABLE_BEHAVIOR` (the only ASN, of 71, to cross a threshold). Zero classification-tier flips confirmed in the Finance Bill 2024 window specifically (100 regime rows + 64 correlation-mart rows checked). See the inventory entry and `decision-log.md`'s fourth 2026-08-01 entry for full detail, including the DNS protocol-stress re-derivation (Part 1 of the same session) that found the earlier `SEVERE_ELEVATION` DNS claim for June 22-23 2024 does not survive re-derivation.
- TD-72/TD-71 external validation (2026-08-01, **fifth session that day, validation-only, no reopening required**): both fixes checked against OONI's own live API, not just CLIO's internal tables. TD-72's core premise (`tls_failure IS NULL` ⇒ genuine success) verified 100/100 against raw OONI JSON; TD-71's `tls_failure` extractor verified verbatim 113/113; all 361 real `BLOCKED` rows checked exhaustively, with every one of the 80 apparent OONI disagreements individually traced to OONI's own known-bad-probe-version discard table (not a real classification contradiction). Surfaced one new, unrelated, low-severity finding (TD-73, `ip_address`/`port` extraction in `stg.ooni_tls_observations`, logged not fixed) and a concrete lead for TD-71's still-deferred `ssl_*` corroboration gate (OONI's probe-version table, not the absent `anomaly`/`blocking` fields). See `decision-log.md`'s fifth 2026-08-01 entry and `reports.md` for full methodology and per-stratum results.

**Tier 3 — real investigations/features, scope properly before touching code:**
- TD-47's `lookups` subset: **scoped and de-risked, 2026-07-06** (design validated against live data, not yet built). Overturned its own original plan — Python-side re-ingestion is impossible in a fresh Codespace clone and unnecessary anyway, since the `lookups` object survives fully in BigQuery and a JS UDF can enumerate its dynamic resolver keys directly. Ready to build whenever this tier is picked up. Its real compute cost (JS UDF, ~40x row-volume multiplication) is deliberately unmeasured pending the cost audit below.
- TD-47's `tor` subset: still open, needs its own dedicated staging asset and a genuine scope decision (is circumvention-infrastructure reachability part of CLIO's mission).
- TD-50: whether to restore cause-level detail (`dns.nxdomain` vs. `tcp.rst`, not just "which layer") through the intelligence layer — a genuine value-vs-cost question for the two established personas, not just a wiring job.
- TD-47(2): `dnscheck`'s `lookups` subset (per-resolver DNS censorship-resistance testing) — a real feature needing Python-side flattening at ingestion and a new resolver-identity column; scope as its own small design note before implementing.
- TD-47(3): `tor`'s reachability data — as much a scope decision (is circumvention-infrastructure reachability part of CLIO's evidence mission at all) as an engineering one; route through the same evidence-admission reasoning ADR-0003 already established, don't default to inclusion.
- TD-39: Layer A test infrastructure. **Split decision recorded 2026-07-06** (scoped, not yet built): wire CI credentials (WIF into `tests.yml`, cheap, proven pattern) soon — today zero behavioral tests run in CI at all without a local `RUN_BIGQUERY_TESTS=1`. Defer the dataset-parameterization half (touching the ARCHITECTURE-FREEZE'd `acled_pressure_regimes.sql`) until a material classification-SQL change is actually planned, most plausibly the `lookups` build above — see the 2026-07-06 decision-log entry for full reasoning.
- **An OONI-side golden-file test, the counterpart to TD-08's ACLED golden tests — scoped 2026-07-06, not yet built.** Recommended design: Layer B, aggregate-count fixtures (not per-row, given ~11M rows) over the Finance Bill 2024 window plus a quiet control window, plus three whole-history invariants already verified live (9,046 probe-reported bogons, TD-52's sum identity, dnscheck's now-nonzero `dns` family signal). Estimated half-day build on existing patterns — see the decision-log entry for full design detail before starting.
- **Streamlit page-structure consolidation (pages 2/3) — recommended 2026-07-06, not yet built.** Pages 2 and 3 still share one mart via a confirmed alias (`get_protocol_stress_intelligence()` ≡ `get_protocol_regimes()`, TD-16); their genuinely distinct content (page 2's heatmap + `state_driving_family`, page 3's TD-51 per-app panel) would sit naturally as tabs in one page. Bundle TD-26/TD-27/TD-61's hygiene fixes into the same pass. Page 1 (composite + regime panel) and page 4 were reviewed and confirmed to earn their current structure — no change recommended there. Page 7 (Finance Bill) should get its incident-window/provider-class arguments parameterized next time it's touched, but a full incident-report template should wait for a second real incident (two-concrete-cases bar).

## Step 4 — Portability and hygiene (parallel with Step 3, lower urgency)

- **DONE (2026-07-05 script/allowlist, 2026-07-06 ADR + scheduled workflow, commit `8fa3e8c`): the materialization-staleness guardrail (ADR-0005).** A daily GitHub Actions check (`.github/workflows/staleness-check.yml` → `Bruin/scripts/staleness_check/check_materialization_staleness.py`) that fails on any *new, undocumented* dependency edge whose downstream table's timestamp is older than its upstream's — the TD-38/TD-40/TD-54 failure category, mechanized. Built on `bruin`'s internal pipeline-JSON output for the DAG and each dataset's `__TABLES__` metatable for timestamps (corrected from the originally-assumed `INFORMATION_SCHEMA.TABLES.last_modified_time`, which doesn't exist). Deliberate gaps live in a TD-referenced allowlist next to the script; resolving those TDs must prune the matching entries in the same commit. **Follow-on — DONE (2026-07-06, cost-audit cleanup pass): TD-56.** All seven previously-unknown stale marts were retired, because direct consumer-tracing found every one has zero live consumers; the inventory's own caution about `fact_asn_repression_index` feeding dashboard pages 5/7 was checked directly and found wrong (it reads `features.protocol_daily_signals`, not the repression index). Allowlist is now empty (`[]`); staleness check passes with 0 new violations. See TD-56's inventory row and the 2026-07-06 decision-log entry for per-asset reasoning.

- **DONE (2026-07-06): first GCP/BigQuery cost audit (see `technical-debt-inventory.md`'s "Cost audit" section).** On-demand, us-central1, ~$6.8/180d — pilot-scale. The OONI staging chain (`stg.ooni_measurements` + four `create+replace` observation assets, each re-scanning ~30 GiB of `raw_test_keys` per rebuild, unpartitioned/unclustered) is the dominant and only badly-scaling driver — logged **TD-58** (cluster/partition first; incremental conversion only if the `@daily` schedule is actually wired to run it). Streamlit and storage are negligible; the ACLED backfill was the most slot-intensive but one-time operation. **TD-58's clustering lever shipped 2026-07-06 (guardrails pass):** all five OONI staging assets now partition by `measurement_date` and cluster by `(country, test_name)`; measured 5.5 GiB vs. ~30 GiB on the test-type diagnostic query shape, ~200× on date-filtered scans. The incremental lever remains deliberately deferred — verified no workflow executes `bruin run` on a schedule, so the `@daily` scaling scenario is not live; leave-alone reasoning for every other `create+replace` asset is recorded in the inventory's "Materialization-strategy audit" section. Cleanup executed in the same pass: **TD-53** (orphan table dropped), **TD-42** (false Lumen `depends:` retired), and TD-41's leftover orphan table dropped. **TD-59** (new): retire `dim_blocking_signals`, keep repointed `dim_country`.

- Make the local-ingestion root path configurable rather than hardcoded to a Windows path in both `scripts/local_ingest_ooni.py` and `scripts/download_ooni.ps1` (TD-11).
- Replace the manual resume-checkpoint literal in `download_ooni.ps1` with a persisted state file (TD-12).
- Migrate away from long-lived downloadable service-account keys in `infra/setup-gcp.sh` toward Workload Identity Federation (TD-13).
- Scope the IAM module's `roles/editor` grant down to specific roles (TD-14).
- **DONE (2026-07-06, guardrails pass): externalized country literals across all SQL assets via the existing Bruin variables (TD-09).** 20 literal sites found (4 more than previously on record); 17 scope filters routed through `{{ var.country }}`/`{{ var.iso2 }}` (already declared in `pipeline.yml`, already used by 40+ sites — this completed an established idiom, not a new one); `dim_country.sql`'s normalization CTE designated the single legitimate literal location. A `dim_countries` reference table was explicitly rejected at n=1 country (two-concrete-cases bar). Durability: a new push/PR CI check (`.github/workflows/country-literal-check.yml` → `Bruin/scripts/country_literal_check/check_country_literals.py`, script + TD-referenced allowlist, same pattern as ADR-0005) fails any new country literal in a SQL asset. The project-ID half was already done (this row's earlier claim was stale). Python-level residual (OONI ingest's `PROBE_CC = "KE"`) logged as TD-60, deferred until OONI ingestion is next touched — re-ingestion isn't runnable in this environment today.

## Step 5 — Documentation reconciliation (parallel, low risk)

- Archive `docs/data-modelling.md`, `docs/civil-liberties-reporting-playbook-Kenya.md`, and `docs/readme1_1.md` into a `docs/_archive/` folder inside the repository itself once it becomes a live git project, per the disposition in `docs/00-overview/documentation-plan.md` (TD-23, TD-24, TD-31).
- Populate the Methodology Changelog and Metric Dictionary described in `documentation-standards.md` incrementally, starting with whatever changes as a direct result of Steps 2 and 3 above — do not attempt to backfill the full history at once.

## Step 6 — Entity resolution and attribution (after Steps 2–3, several weeks)

- **DONE (2026-07-06): the source-level half of pressure attribution (ADR-0006, Project Zero Review Recommendation #4).** Four new leaf reporting assets plus Streamlit page 9 decompose `composite_pressure_score` into its two real arithmetic terms (ACLED conflict 0.75, Google Transparency platform 0.25 — **not** OONI, which the build's inventory pass confirmed feeds no composite term and is instead surfaced as same-day corroboration) with per-row confidence qualifiers and pointers to the specific classified rows behind a date's reading. This became buildable now because its real prerequisites were the confidence and provenance fixes (TD-01, ADR-0001, ADR-0004), all resolved — not entity resolution. Verified end-to-end against the Finance Bill 2024 window through the rendered page. See TD-57 for the deliberate keep-in-sync duplications accepted in the process.
- **Remaining: build the `dim_entities` canonical mapping table (TD-10),** enabling the **actor-level** half of attribution — *who* did the pressuring, not just which source and how much. Requires event-level ACLED data with actor fields (the current weekly-aggregate export has none). This step stays here, not earlier, because it depends on the confidence and provenance fixes above being in place first — attribution built on top of an unresolved confidence ambiguity or unflagged synthetic data would inherit both problems invisibly.

## Step 7 — Near real-time observatory capability (architecture and planning only, 2026-07-11)

Prompted by the grant-readiness and flagship-report planning in `docs/01-product/2026-07-11-execution-roadmap-grant-and-flagship.md`. No implementation is authorized by this step yet — it records the architectural decisions so implementation can start from a settled design rather than an open question.

**Finding: only OONI's data genuinely supports near-real-time refresh.** OONI probes submit measurements continuously; the only current gap is that nothing schedules the pipeline to consume that continuously — confirmed directly, not assumed: the 2026-07-06 cost audit found no GitHub workflow ever executes `bruin run`, so the OONI staging chain currently rebuilds 2–6 times per 90 days despite a declared `@daily` schedule. TD-58's clustering and partitioning work was explicitly preparing for this trigger ("conversion still deferred... trigger for revisiting: a real scheduled `bruin run` gets wired up") — this step is that trigger being pulled. ACLED, STOP, and Google Transparency Report are periodic by the nature of how each source itself produces data (ACLED's own weekly coding cadence; Google's semiannual reporting windows; STOP's export-based, non-continuous update pattern) — forcing any of them to refresh faster than their own source cadence would fabricate precision none of them actually have, repeating the exact grain-honesty mistake this project already caught and corrected at TD-40 and in ADR-0003's Contextual/Backdrop tier. "Near real-time observatory" means the OONI protocol layer updates continuously on its own dedicated surface, while every other source stays honestly labeled at its own real cadence.

**Architectural implications, decided now, built later:**
- A real, scheduled trigger for the OONI staging chain only (daily), using TD-58's already-shipped clustering/partitioning.
- Conversion of OONI staging to incremental/append materialization — the natural next step per the 2026-07-06 materialization-strategy audit, justified only once a real schedule exists (it does not yet).
- A dedicated near-real-time protocol-stress surface, separate from the composite-score/weekly-regime dashboard — this is a smaller change than it sounds, since ADR-0006 already established that OONI feeds no arithmetic term of `composite_pressure_score` at all. Making OONI real-time does not require touching the composite score's own weekly cadence.

**AI-layer implication:** the AI Architecture doctrine's Phase 6 (Country Intelligence Profiles) and Phase 7 (Election Monitoring Workflows) both assume some continuous or near-real-time capability as a prerequisite — this step is a genuine dependency of that later roadmap, not an independent track. Cost discipline (the doctrine's Principle 4) becomes load-bearing here specifically: the AI narration/interpretation layer should run on a coarser, state-transition-triggered cadence, not be re-invoked on every new raw OONI measurement — the Artifact Registry's "no repeated analytical recalculation if a valid artifact exists" prohibition is the mechanism that should enforce this, once Phase 1 of that doctrine is actually built (see Part D of the execution roadmap — it isn't yet).

**Sequencing:** (1) wire the scheduled OONI trigger; (2) convert to incremental materialization; (3) build the dedicated near-real-time surface; (4) wire AI narration with threshold-triggered, not continuous, regeneration. Do not revisit ACLED's, STOP's, or Google's cadence ahead of this — only if one of those sources' own update behavior genuinely changes first.

## What this roadmap deliberately does not include

No rewrite of the OONI path or ACLED path A's core logic — both are genuinely good and are the standard, not the problem. No new shared abstraction for data source integration (a base contract, a plugin system) until Google Transparency and Lumen have been brought up to a comparable methodological standard one at a time — there are not yet enough well-understood cases to generalize from safely. No multi-country parameterization before Step 4, since doing it earlier would multiply, not amortize, the hardcoding problem it is meant to fix.

## Cross-reference to the commercial roadmap

`docs/01-product/roadmap.md` (from the Project Zero Review) sequences commercial priorities — packaging the Finance Bill report, building the pressure-attribution layer, the AI narrative layer, the API. Steps 2 and 3 above (the CROSS JOIN fix and the confidence-scheme unification) are prerequisites for that roadmap's own Immediate-horizon items, since both directly affect the accuracy of the first sellable report. Read the two roadmaps together before sequencing actual work.

**Update, 2026-07-11:** that commercial roadmap is now superseded in direction, not in its specific technical prerequisites, by `docs/01-product/2026-07-11-execution-roadmap-grant-and-flagship.md` and the working assumptions ADR-0007 records — CLIO's near-term priority is grant readiness and a flagship-report portfolio, not direct commercialization of OONI/ACLED-derived products, per the licensing findings in `docs/07-governance/licensing-compliance-register.md`. The technical prerequisites named above (CROSS JOIN fix, confidence-scheme unification) remain correct and already resolved; what they now feed is grant and partnership credibility, not a sale.
